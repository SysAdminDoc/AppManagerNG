# AppManagerNG — Roadmap

**Status:** Living document — update on every version bump.  
**Baseline:** v0.1.0, forked from [App Manager](https://github.com/MuntashirAkon/AppManager) @ `3d11bcb` (post-v4.0.5), 2026-04-30.  
**Last updated:** 2026-05-17 — Iter-56 follow-through closed the T1 **Android Developer Verification — BR/ID/SG/TH Enforcement** row. NG now detects the `developer_verifier` service through a new compat helper, marks App Details verifier status as unknown when Android exposes the service but no stable public per-app preflight verdict, warns before install/session commit (including split-APK chooser flows), and appends `PackageInstaller` developer-verification failure reasons to dialogs/transcripts when Android returns `EXTRA_DEVELOPER_VERIFICATION_FAILURE_REASON`. See [docs/sideload-verification.md](docs/sideload-verification.md) ([S172], [S173], [S309], [S310]).

**Prior (iter-55, 2026-05-17 earlier):** closed the Eng-Debt **Android 17 16 KB Page-Size Compatibility** row. NG now links its JNI library with 16 KB ELF page-size flags, parses `PT_LOAD.p_align` for every APK native library shown in App Details / scanner output, and blocks reproducible release APKs that contain misaligned or unverifiable `.so` files. The release gate also checks uncompressed native ZIP entries for 16 KB data-offset alignment. See [docs/audits/2026-05-17-android17-16kb-native-page-size.md](docs/audits/2026-05-17-android17-16kb-native-page-size.md) ([S148], [S336], [S337]).

**Prior (iter-54, 2026-05-17 earlier):** closed the T8 **Freeze / Operation Audit Log** row as an existing-op-history completion plus two missing UX links. NG already had a Room-backed `OpHistoryActivity` with filters, risk/reversible chips, export/share, rerun preflight, target opening, rollback guidance, and batch/profile/installer journaling; this pass added the Settings -> Privacy entry point and an explicit per-row "Recovery guidance" action for reversible operations. Automatic inverse replay remains scoped to the separate Per-App Rollback row ([S144], [S145], [S231], [S232]).

**Prior (iter-53, 2026-05-17 earlier):** closed the T5 **Shizuku Permission Auto-Revoke Warning** row. Direct App Info clear-data dialogs and the main-list batch clear-data dialog now warn when the target is AppManagerNG, Shizuku Manager, or an installed app that declares `rikka.shizuku.ShizukuProvider`; direct privileged clear-data captures `Shizuku.checkSelfPermission()` before/after and deep-links to Settings -> Mode of operation when AppManagerNG's own grant disappears. Batch/profile clear-data logs the same revocation signal for result review ([S139]).

**Prior (iter-52, 2026-05-17 earlier):** closed the Eng-Debt **Hidden-API Compatibility Harness** row. The roadmap's old `app/src/main/assets/api/api-versions-*.json` claim was stale, so NG now checks in the missing androidTest baseline at `app/src/androidTest/assets/api/api-versions-appmanagerng-hiddenapi.json`, regenerates it from `hiddenapi/src/main/java` via `scripts/generate-hidden-api-baseline.ps1`, verifies source-file coverage in a JVM test, and adds an instrumented runtime probe that reflects active-SDK hidden classes/members and writes a JSON diff report before failing on required missing APIs ([S137]).

**Prior (iter-51, 2026-05-17 earlier):** closed the T8 **Hail-Style Auto-Freeze QuickSettings Tile** row. Profiles with the `freeze` action enabled can now be selected from the profile-list popup as the Quick Settings freeze profile; the exported `QuickFreezeTileService` declares the platform QS tile permission/action, requires device unlock before running from the lock screen, and starts the existing `ProfileApplierService` in ON/freeze state ([S130], [S334], [S335]).

**Prior (iter-50, 2026-05-17 earlier):** parked the Eng-Debt **Apktool 3.0.2 "Remastered" Migration** row as stale for current source. NG has no `org.apktool` / `brut.apktool` dependency or Apktool 2.x integration to migrate; APK editing today uses ARSCLib, apksig, and Google smali/baksmali. Apktool 3.0.2 remains the target if T12 later adds an Apktool-backed decode/rebuild backend, but adding the library now would only add unused runtime deps and a second smali fork ([S332], [S333]).

**Prior (iter-49, 2026-05-17 earlier):** parked the T6 **JobScheduler Quota Stop-Reason Surfacing** row as a dependency of **Scheduled Auto-Backup** rather than a standalone code item. A fresh source/Gradle sweep still found no `androidx.work`, `WorkManager`, `JobScheduler`, `JobService`, or Schedules-screen implementation in NG; Android's current Android 16 docs still make the stop/pending-reason APIs relevant once the scheduler exists ([S45], [S124], [S125]).

**Prior (iter-48, 2026-05-17 earlier):** closed the T5 **Shizuku Trusted-WLAN Auto-Start Awareness** row: Operating Mode and the replayable onboarding Shizuku card now detect Android 13+ + Shizuku Manager 13.6.0+ with a stopped binder and surface a "Configure auto-start in Shizuku" action. The launch intent first tries the roadmap-tracked `moe.shizuku.privileged.api/.AUTO_START` component, then falls back to Shizuku's launcher/app-info surface because the v13.6.0 source manifest does not expose the component in every build ([S121], [S331]).

**Prior (iter-47, 2026-05-17 earlier):** closed the T4 **Mode Self-Test "Doctor"** row: Settings -> Privileges now has an active Mode Doctor report that probes root grant/manager/Sui, Shizuku binder/UserService/permission, ADB toggles/pairing, LocalServer `id -u`, SELinux domain, and ABI state with fix hints.

**Prior (iter-46, 2026-05-17 earlier):** closed the T5 **Privileged-Shell Journal + DeathRecipient Replay** row: batch operations now persist intent/executing journal state, mark Shizuku/Sui binder death as interrupted, leave crash/kill entries for reattach, and surface a retry/clear recovery dialog from the main screen.

**Prior (iter-45, 2026-05-17 earlier):** closed the T9 **Privileged Op Audit Log** row: the existing Room-backed operation history was audited as the intended append-only log/viewer/export/retention surface, and new metadata now records normalized exit codes plus the remembered LocalServer bootstrap signature in details and JSON/CSV export.

**Prior (iter-44, 2026-05-17 earlier):** closed the T4 **Support Info Bundle Composer** row: Settings -> Troubleshooting now creates a zero-network `support-info-<device>-<timestamp>.txt` bundle with device/build, privilege-provider, feature-flag, remembered LocalServer bootstrap-signature, and scrubbed logcat-tail context shared through the system chooser.

**Prior (iter-43, 2026-05-17 earlier):** closed the T4 **LocalServer Bootstrap Smoke Test** row: Settings -> Privileges now has a runnable LocalServer bootstrap smoke test that performs the privileged-shell handshake plus `id -u` probe, then shows/copies the same single-line bootstrap signature format on both success and failure paths.

**Prior (iter-42, 2026-05-17 earlier):** closed the T1 **`floss` vs `full` Build Flavors (F-Droid Clean Path)** row: AppManagerNG now has a default `floss` distribution flavor that disables optional third-party/network-report surfaces at compile time while preserving local ADB/localhost networking, plus a `full` flavor for GitHub Releases / Obtainium users who opt into online scan reports and debloat-definition updates.

**Prior (iter-41, 2026-05-17 earlier):** closed the T8 **App Shortcut: Freeze / Force-Stop / Clear Cache Per-App** row: AppManagerNG now publishes dynamic launcher shortcuts for recent installed apps when the active privilege path can freeze, force-stop, or clear cache, and App Details can pin force-stop / clear-cache shortcuts alongside the existing freeze shortcut flow.

**Prior (iter-40, 2026-05-17 earlier):** closed the T11 **APK Share-Target Receiver** row as a stale implementation gap: `PackageInstallerActivity` already accepts shared APK/APKM/XAPK intents, `ApkQueueItem.fromIntent()` turns shared `file:` / `content:` / `package:` URIs into installer queue items, and the installer already surfaces tracker, dependency, checksum, and signature-mismatch context before install.

**Prior (iter-39, 2026-05-17 earlier):** closed the iter-22/T8 **Broadcast Intent API** row: AppManagerNG now declares a signature-only automation permission and an exported `AutomationReceiver` for `io.github.sysadmindoc.AppManagerNG.action.*` operations, routing package work through `BatchOpsService`, profiles through `ProfileApplierService`, and documenting the contract in `docs/intent-api.md`.

**Prior (iter-38, 2026-05-17 earlier):** closed the T7 **Finder: Relevance-Based Search Scoring** row: Finder now sorts literal package-name, component-name, and tracker-name searches through a Levenshtein-based `FinderRelevanceScorer`, preferring exact/simple/token matches while preserving original scan order for unrelated filter hits.

**Prior (iter-37, 2026-05-17 earlier):** closed the T7 **Filter: Permission Flags** row: `PermissionsOption` now supports grant-state, custom-permission-source, fixed-permission, and raw runtime-permission-flag predicates backed by shared `FilterablePermissionInfo` state.

**Prior (iter-36, 2026-05-17 earlier):** closed the T7 **Finder: Uninstalled App Backups** row: Finder now opts into validated backup-only app rows from AppManagerNG's backup metadata DB for package/user pairs not returned by PackageManager, so filters can match archived uninstalled apps while preserving the visible user/state result line.

**Prior (iter-35, 2026-05-17 earlier):** closed the T7 **Finder: Description-Field Search** row: Finder's existing `bloatware` filter now exposes description predicates over the debloat-list prose, so users can find packages by human purpose text from `debloat.json` rather than only package/app labels.

**Prior (iter-34, 2026-05-17 earlier):** closed the T5 **Android 16 Capability Dropping UI** row: Settings -> **Privileges** now includes a "Capability dropping (--drop-cap)" diagnostic that probes the active privileged shell's UID and kernel-reported `CapEff` field, then reports root, dropped, present, unavailable, or unknown states.

**Prior (iter-33, 2026-05-17 earlier):** closed the T5 **Privilege Health-Check Screen** row: Settings now exposes a persistent **Privileges** screen that reports configured/inferred mode, working UID, mode self-test result, root manager detection with ZygiskNext/Sui markers, Shizuku binding status + API-version/minimum check, USB/Wireless ADB state, remote server/service state, and AppManagerNG battery-optimization status with the same privileged one-tap auto-fix path used by Troubleshooting.

**Prior (iter-32, 2026-05-17 earlier):** closed the T5 **Auto-Update Debloat Definitions** row and duplicate iter-addition row: a default-off Settings → Privacy opt-in now runs a launch-time updater gated by the existing Internet feature flag, fetches a pinned AppManagerNG raw-GitHub manifest, verifies `debloat.json` / `suggestions.json` by byte length + SHA-256, schema-smoke-tests the payloads, caches them in app-private storage, and falls back to bundled assets offline.

**Prior (iter-31, 2026-05-17 earlier):** closed the T5 **Cross-User Package State Detection** row and T7 **Finder: Multi-User Scope** row: `ApplicationItem` now keeps enabled/disabled/uninstalled user-id buckets, the main list/user picker surfaces per-user state such as `u0 enabled, u10 disabled`, and Finder loads all selected users with visible user/state metadata per result.

**Prior (iter-30, 2026-05-17 earlier):** closed T5's **Auto-fix Battery Optimization (Root/ADB Path)** row: new `SelfBatteryOptimization` centralizes NG's own Doze-exemption state and privileged `DEVICE_POWER` auto-fix, profile routine execution and long-running backup/import/restore batch operations now attempt the root/ADB `deviceidle` whitelist path before work begins, and the Troubleshooting battery entry reuses the same helper.

**Prior (iter-29, 2026-05-17 earlier):** closed the T5 **Installer APK SHA256 Toast** row: `PackageInstallerCompat` now hashes the exact bytes streamed into each install session, carries the digest through the pending-user-action handoff, and `PackageInstallerActivity` shows a checksum dialog before launching Android's system install confirmation prompt.

**Prior (iter-28, 2026-05-17 earlier):** closed the T5 **USB Debugging Prompt in Shizuku Setup** row: Wireless ADB setup now preflights Developer Options' USB-debugging flag, prompts users to enable both USB debugging and Wireless debugging before pairing, and updates Shizuku / Wireless ADB explanatory copy so the Canta-model silent pairing failure path is visible before `adb pair` / `adb connect`.

**Prior (iter-27, 2026-05-17 earlier):** landed the **Shizuku Android-17 runtime warning** (`ShizukuBridge.hasAndroid17CompatibilityRisk(Context)` + onboarding banner that starts Wireless ADB setup), the **Shizuku release-feed watcher CI workflow**, and the **ML-DSA certificate algorithm display-name map** for Android 17 post-quantum APK-signing OIDs. The Shizuku row is now mitigated on NG's side but remains open for Android 17 device verification and a future fixed-version floor from upstream Shizuku.

**Prior (iter-26, 2026-05-17 earlier):** follow-through landed the full **Android 17 targetSdk=37 audit batch** (5 sub-audits, all clean: `usesCleartextTraffic` enforcement, `ACCESS_LOCAL_NETWORK` runtime permission, BAL hardening + `MODE_BACKGROUND_ACTIVITY_START_ALLOWED` migration, ECH default-on, ML-DSA Keystore OID recognition) plus a **Shizuku 13.6.0 Android-17 compatibility audit** (now updated after iter-27 implementation). Engineering Debt Register's targetSdk=37 row is **closed** — NG's path to the platform-stable bump in June 2026 is source-side clear except the 1 deferred iter-20 static-final-reflection finding and the external Shizuku fixed-version verification. Three CI/docs hygiene items also landed: **minSdk-21 cascade analysis**, **Markdown link-checker workflow**, and a **JaCoCo coverage rollout plan**.

**Prior (iter-25, 2026-05-17 earlier):** three commit-ready in-progress fixes (`fix(finder)`: regex predicates no longer Pattern.quote'd into literals; `security(installer)`: install-transcript URI redactor closes userinfo / query / fragment leak; `fix(onboarding)`: root-manager probe captures Application context before background post). Stood up `docs/architecture/` (01-privilege-providers, 02-backup-format, 03-hidden-api-bypass + README), closing T11 "Architecture Documentation" row. External sweep added five iter-25 sources: **Shizuku 13.6.0 Android 17 Beta 3 regression** (issues #1965/#1967 — Application Management page blank; new T5 watch row), **Neo-Backup 8.3.18** (supersedes 8.3.17 at iter-18 [S135]). Android 17 stable confirmed for **June 2026** (Beta 3 platform stability locked 2026-03-26).

**Prior:** 2026-05-16 — Iter-23 "Now" tier fully shipped: NG governance docs (CONTRIBUTING.md, AGENTS.md), `main`-branch CI coverage, release artifact matrix, dependency-scan version reconciliation, ADB CVE patch-level banner, package-visibility dossier, minSdk-21 ceiling ledger, and NG-rebranded issue/PR templates with support-bundle intake.
**Next revision due:** v0.6.0 release.
**Project context index:** [`PROJECT_CONTEXT.md`](PROJECT_CONTEXT.md) is the canonical entry-point for new contributors / AI sessions. The 2026-05-17 research consolidation pass (artifacts in [`.ai/research/2026-05-17/`](.ai/research/2026-05-17/)) queued an iter-24 backlog of three commit-ready fixes (Finder regex predicate fix, install transcript URI redactor, onboarding fragment-detach race) plus governance items — see [`.ai/research/2026-05-17/FEATURE_BACKLOG.md`](.ai/research/2026-05-17/FEATURE_BACKLOG.md).

**Related research:**
- [Android power-tool competitive research](docs/research/2026-05-02-android-power-tools.md) (2026-05-02)
- [Capability extension — datasets, privacy intelligence, modern Android APIs](docs/research/2026-05-09-capability-extension.md) (2026-05-09)
- [Roadmap extension phase 2 — reliability, migration, automation, form factors, i18n, observability](docs/research/2026-05-09-roadmap-extension-phase-2.md) (2026-05-09)
- [Observability, telemetry & testing audit](docs/research/2026-05-09-observability-testing-audit.md) (2026-05-09)

---

## Guiding Principles

AppManagerNG is a "friendlier front door" to App Manager's engine. Three commitments shape every prioritization call:

1. **Progressive disclosure, not feature removal.** Power-user depth stays; it just doesn't ambush newcomers. The Pro Mode toggle is the primary mechanism.
2. **Upstream as reference, not competitor.** Pull fixes and features from upstream aggressively. Diverge only where UX philosophy demands it.
3. **Rootless first.** Shizuku-based operations are first-class citizens alongside root. The majority of Android users have no root; they shouldn't hit a dead end.

Hard constraints:
- License: **GPL-3.0-or-later** throughout. Every new dependency must be compatible.
- minSdk **21** (Android 5.0). Many modern APIs need compat paths or conditional activation.
- No AI-generated code contributions (inheriting upstream policy; NG will publish its own CONTRIBUTING.md in v0.2.0).

---

## Committed Version Targets

| Version | Theme | Key Deliverables |
|---------|-------|-----------------|
| **v0.2.0** ✅ | Identity | `applicationId` + namespace rename → `io.github.sysadmindoc.AppManagerNG`; new release keystore; GitHub Actions release pipeline; NG-specific CONTRIBUTING.md |
| **v0.3.0** ✅ | UX Refresh | Material 3 dashboard; Pro Mode toggle; edge-to-edge (Android 15/16 compliance); AMOLED/dark/light themes |
| **v0.4.0** ✅ | Permission Inspector | Review and bulk-revoke dangerous runtime permissions across all installed apps; critical-package denylist guard; in-app recovery action for previously revoked system packages |
| **v0.5.0** | Settings & Discovery | Settings reorganization by task; global in-app search; contextual help tooltips; in-app changelog viewer |
| **v0.6.0** | Rootless Power | Shizuku integration; rootless debloat; wireless ADB auto-pairing |

---

## Tier Definitions

| Tier | Meaning |
|------|---------|
| **Now** | Tied to v0.2.0–v0.3.0 or is a critical bug/regression fix |
| **Next** | Target v0.4.0–v0.6.0 horizon; high-value, well-scoped |
| **Later** | Post-v0.6.0; valuable but requires upstream landing or prior work |
| **Under Consideration** | Not rejected — needs architecture decision or external dependency |
| **Rejected** | Explicitly out of scope; reasoning provided |

---

## Now

### T1 — Identity & Distribution

Required before any APK reaches real users. Every item here is blocking F-Droid listing.

| Item | Description | Effort |
|------|-------------|--------|
| ~~**applicationId Rename**~~ ✅ v0.2.0 | Change `io.github.muntashirakon.AppManager` → `io.github.sysadmindoc.AppManagerNG`; update all `BuildConfig` references | Low |
| ~~**New Release Keystore**~~ ✅ v0.2.0 | Generate NG-specific signing key; document SHA-256 fingerprint in README for AppVerifier compatibility | Low |
| ~~**GitHub Actions Release Pipeline**~~ ✅ v0.2.0 | `release.yml`: tag push → build → sign → upload APK to GitHub Releases; parallel arm64-v8a / universal ABIs | Medium |
| ~~**Reproducible Builds**~~ ✅ 2026-05-14 / hardened 2026-05-16 | Release publishing now runs [`scripts/verify_reproducible_release.sh`](scripts/verify_reproducible_release.sh): two clean signed `:app:assembleRelease` builds from the same tag, SHA-256 comparison, and publish blocked on mismatch. Windows maintainers can run the same contract via [`scripts/verify_reproducible_release.ps1`](scripts/verify_reproducible_release.ps1). Gradle archive tasks use reproducible ordering/timestamps; `server` D8 inputs for `am.jar` / `main.jar` are sorted. The 2026-05-16 hardening pass now compares every generated release APK, writes per-APK checksum sidecars, and uploads the full verified asset set instead of assuming a single universal APK. Details at [docs/distribution/reproducible-builds.md](docs/distribution/reproducible-builds.md). | Medium |
| **IzzyOnDroid Listing** | Submit after rename; IzzyOnDroid is faster than F-Droid proper and the primary privacy-community distribution channel | Low |
| **F-Droid Listing** | Submit to F-Droid proper after IzzyOnDroid pass; requires REUSE compliance (already in place) | Low–Med |
| ~~**Obtainium Config**~~ ✅ 2026-05-01 | Publish pre-built app config at `apps.obtainium.imranr.dev` so users can track NG updates directly. **Shipped at [docs/distribution/obtainium-config.json](docs/distribution/obtainium-config.json) with README "Install via Obtainium" section. Submission to apps.obtainium.imranr.dev is a separate manual PR step (out-of-band).** | Trivial |
| **Accrescent Listing** | Submit to Accrescent store after reproducible builds land; provides key-pinned first-install verification and no-account downloads for privacy-conscious users ([S62]). Requires developer console account + meeting reproducible build requirement (N2 dependency). | Low–Med |
| ~~**NG-Specific CONTRIBUTING.md**~~ ✅ v0.2.0 | Replace upstream's CONTRIBUTING.rst; define AI code policy, commit format, PR expectations, upstream sync protocol | Low |
| ~~**AppVerifier Fingerprint**~~ ✅ v0.2.0 | Add signing certificate SHA-256 to README (model: SAI, Obtainium, Canta all do this) | Trivial |
| ~~**Android TV / Google TV Launcher Support**~~ ✅ pre-existing | `LEANBACK_LAUNCHER` category, `<uses-feature android:name="android.software.leanback" android:required="false"/>`, optional touchscreen, and `android:banner="@mipmap/ic_banner"` are already wired on `SplashActivity` (the user-facing launcher). Audited 2026-05-02; matches SD Maid SE v1.7.x model ([S112]). No work required. | Trivial |
| ~~**Publish Signing Cert Fingerprint at Stable URL**~~ ✅ 2026-05-02 | SHA-256 fingerprint published in machine-parseable form at [`docs/fingerprints.txt`](docs/fingerprints.txt), served via the stable `https://raw.githubusercontent.com/SysAdminDoc/AppManagerNG/main/docs/fingerprints.txt` URL (no GitHub Pages activation required). Comment-tolerant `package:` / `sha256:` record pairs follow the SD Maid SE precedent ([S112]). README "Verifying releases" section links to the URL. | Trivial |

### T2 — Platform Compliance (Android 15/16)

Unaddressed items here will become regressions when targetSdk=36 is enforced on Google Play and will break on Android 16 devices in the field.

| Item | Description | Effort |
|------|-------------|--------|
| ~~**Edge-to-Edge Enforcement**~~ ✅ v0.3.0 | Remove `windowOptOutEdgeToEdgeEnforcement` (removed in Android 16 targetSdk=36); handle all window insets across every screen | Medium |
| ~~**Predictive Back (Android 16)**~~ ✅ v0.3.0 | `enableOnBackInvokedCallback = true` is now the default for targetSdk=36; audit and migrate all `onBackPressed` / `KEYCODE_BACK` consumers | Medium |
| ~~**Themed App Icons**~~ ✅ v0.3.0 | Add monochrome adaptive icon variant (Android 16 auto-applies themed icons; supply the vector to control the output) | Low |
| ~~**16KB Page Size Compliance**~~ ✅ v0.3.0 | Recompile all NDK `.so` libraries with `-DANDROID_SUPPORT_FLEXIBLE_PAGE_SIZES=ON` and verify linker 16KB alignment; required for Android 15+ physical devices | Medium |
| ~~**`announceForAccessibility` Migration**~~ ✅ v0.3.0 | Replace deprecated `announceForAccessibility` calls (deprecated Android 16) with `ViewCompat.performAccessibilityAction` equivalents | Low |
| ~~**Adaptive Layout for Large Screens — Compliance Audit**~~ ✅ 2026-05-02 | Remove any fixed orientation/aspect ratio restrictions; audit for layout breakage on tablets/foldables. Android 16 ignores `screenOrientation` and resizability restrictions for targetSdk=36 apps on displays ≥600dp ([S44]). **Manifest sweep clean: 0 fixed orientations / 0 resizeable=false / 0 aspect-ratio limits across 43 activities. Density bumps shipped via new `values-w600dp/dimens.xml` overrides (app + libcore/ui).** See [docs/audits/2026-05-02-adaptive-layout.md](docs/audits/2026-05-02-adaptive-layout.md). | Medium |
| ~~**Adaptive Layout — Master/Detail UX Iteration**~~ ✅ 2026-05-14 | Follow-up to the compliance audit above. Main → AppDetails now uses AndroidX Activity Embedding on ≥ 900dp displays, with a dedicated wide-screen placeholder before an app is selected and the existing AppDetailsActivity stack preserved. OneClickOpsActivity separates review, backup, and maintenance surfaces on ≥ 900dp displays while preserving the phone layout. Backup/restore review dialogs now keep summary context beside the package/version list on ≥ 900dp displays. | Medium-High |
| ~~**`elegantTextHeight` Audit**~~ ✅ 2026-05-01 | Audit all layouts for reliance on `elegantTextHeight` attribute — it is ignored for apps targeting API 36. Affects text rendering in Arabic, Thai, Indic scripts ([S44]). **Result: zero source matches; no remediation required.** See [docs/audits/2026-05-01-elegant-text-height.md](docs/audits/2026-05-01-elegant-text-height.md). | Low |
| **WorkManager Quota Testing (Android 16)** ⏸ blocked-by Scheduled Auto-Backup | Backup scheduler (T6) must be tested under Android 16's stricter JobScheduler quota model: quota now applies when app is in top-state or running alongside a foreground service. Use `WorkInfo.getStopReason()` / `JobParameters.getStopReason()` for diagnostics ([S45]). **Pre-flight 2026-05-02: zero WorkManager / androidx.work / JobScheduler references in current source — there is no backup scheduler yet to instrument. Item is parked until ROADMAP T6 'Scheduled Auto-Backup' lands; the diagnostics will be wired in as part of that feature, not as a standalone task.** | Low (after dependency lands) |
| ~~**Android 17 MessageQueue Compatibility**~~ ✅ 2026-05-02 | Android 17 ships a lock-free `MessageQueue` for targetSdk=37 apps ([S55]). NG must audit any direct access to `MessageQueue` private fields (used in some root-shell polling paths); `IllegalReflectiveAccess` will escalate to crashes under this impl. **Audit clean — zero source matches; root-shell IPC uses libsu shell processes, not MessageQueue reflection.** See [docs/audits/2026-05-02-android17-messagequeue.md](docs/audits/2026-05-02-android17-messagequeue.md). Other Android 17 behaviour-change gates remain. | Low |
| ~~**Android 17 Keystore Per-App Key Cap**~~ ✅ 2026-05-02 | Audit clean. NG creates at most **2** `AndroidKeyStore` aliases over its lifetime (`aes_local_protection` on M+, `rsa_wrap_local_protection` on pre-M legacy path), both in `CompatUtil.getAesGcmLocalProtectionKey()` and both `containsAlias`-guarded so they're idempotent. All backup-crypto paths route through a **file-backed BKS keystore** (`am_keystore.bks` via `KeyStoreManager`) which is outside the platform-managed Keystore and therefore outside the 50,000-key cap. Zero `AndroidKeyStore` references in `backup/**` or vendored `libs/**`. See [docs/audits/2026-05-02-android17-keystore-key-cap.md](docs/audits/2026-05-02-android17-keystore-key-cap.md). | Low |

### T3 — Critical Bug Fixes & Security Debt

These are `FIXME` / security issues identified in source that should not wait for a feature release.

| Item | File | Description | Effort |
|------|------|-------------|--------|
| ~~**Keystore Password Security**~~ ✅ v0.3.0 | `KeyStoreUtils.java` | Use `char[]` instead of `String` for keystore passwords (String is interned in heap; not clearable) | Low |
| ~~**ABX-to-XML Lossless Fix**~~ ✅ v0.3.0 | `CodeEditorViewModel.java` | Current ABX→XML conversion is lossy; update serializer to round-trip without data loss | Medium |
| ~~**Intent Interceptor OPEN_DOCUMENT**~~ ✅ v0.3.0 | `ActivityInterceptor.java` | Interceptor permanently breaks `android.intent.action.OPEN_DOCUMENT` (Issue #1767, 2 reactions); fix dispatch logic | Medium |
| ~~**Utils.java i18n (×5)**~~ ✅ 2026-05-02 | `Utils.java` | Soft-input / service-flag / activity-flag / input-feature flag strings now pull from `strings.xml`; `getProtectionLevelString` keeps Android's canonical manifest-protection-level tokens (`dangerous`, `signature\|privileged`, etc.) intentionally untranslated — they are technical identifiers, and a `protectionLevel.contains("dangerous")` check exists in `AppDetailsPermissionsFragment`. | Low |
| ~~**Android 18 Implicit URI Grant Removal**~~ ✅ 2026-05-02 | `PackageInstaller` / share intents | All seven outgoing `ACTION_SEND` / `ACTION_SEND_MULTIPLE` paths that carry a content URI now set both `FLAG_GRANT_READ_URI_PERMISSION` and an explicit `ClipData` (multi-item for `SEND_MULTIPLE`) so the chooser target receives read access without relying on Android's implicit auto-grant. Three text-only sender paths (no URI) left untouched. Zero `IMAGE_CAPTURE` callers in source. `PackageInstaller` install path streams via `openWrite()` and is unaffected. See [docs/audits/2026-05-02-android18-implicit-uri-grant.md](docs/audits/2026-05-02-android18-implicit-uri-grant.md). | Low |

### T4 — Observability & Process

| Item | Description | Effort |
|------|-------------|--------|
| ~~**Opt-In Crash Reporting**~~ ✅ v0.3.0 | On uncaught exception: write crash log to app-private storage + show "Share crash report" dialog that deep-links to GitHub Issues. Zero network egress without explicit user action. | Low |
| ~~**In-App Diagnostic Dump**~~ ✅ v0.3.0 | Export logcat (filtered to AM process) + app state snapshot as shareable ZIP for bug reports | Low |
| ~~**CodeQL Alert Triage**~~ ✅ v0.3.0 | Audit all open CodeQL alerts (`.github/workflows/codeql.yml` already present); ensure zero blanket suppressions | Low |
| ~~**ProfilingManager OOM/Anomaly Triggers**~~ ✅ 2026-05-02 (registration) | New `ProfilingTriggerHelper.registerTriggersIfSupported(Context)` (`misc/`) registers `TRIGGER_TYPE_OOM` + `TRIGGER_TYPE_ANOMALY` via reflection on API 37+; silent no-op below or on any reflective-lookup failure (compileSdk is still 36). Wired from `AppManager.onCreate()`. Listener-side wiring to harvest the resulting profile artifacts and attach them to the diagnostic ZIP is deferred until API 37 is on a real device for end-to-end test. | Low |

---

## Next

### T5 — Rootless Users (Shizuku)

Shizuku support is the single most-requested upstream feature with 31 reactions across 5 years ([S02]). It unblocks the majority of AM operations for users without root. Canta ([S19]) and SmartPack Package Manager ([S21]) already ship Shizuku integration; AM/NG without it is at a structural disadvantage for casual users.

| Item | Description | Effort |
|------|-------------|--------|
| ~~**Shizuku Privilege Provider**~~ ✅ 2026-05-14 | Added Shizuku/Sui UserService as a first-class privilege path alongside root and ADB, with automatic runtime detection, permission recovery, mode-selection status, onboarding copy, and Android 7.0+ provider gating. | High |
| ~~**Wireless ADB Auto-Pairing**~~ ✅ 2026-05-14 | Added guided Android 11+ Wireless ADB setup from onboarding, persisted successful pairing metadata, surfaced remembered-device status in onboarding/settings, and kept ADB-over-TCP fallback guidance for older devices. | Medium |
| ~~**Rootless Debloat (Shizuku)**~~ ✅ 2026-05-14 | Added a Shizuku/ADB shell `pm uninstall --user <id>` path for Debloater uninstall batches, bypassing accessibility prompts when the privileged shell is active. Selection confirmation now summarizes android-debloat-list safety ratings, dependency/required-by warnings, and high-risk examples before removal; Debloater rows also surface dependency counts inline. | Medium |
| ~~**Factory-Reset Before System App Uninstall**~~ ✅ 2026-05-14 | Updated uninstall flows to reset updated system apps back to the factory version before user-scope removal, using `pm uninstall-system-updates` when a Shizuku/ADB/root shell is active and surfacing the reset in Debloater confirmation/card copy. | Low |
| ~~**Debloat Presets**~~ ✅ 2026-05-14 | Added Privacy, Gaming, and Minimal OEM Debloater presets that select installed recommendations as curated batches, preview match counts and rationale, then route through the reviewed freeze/remove confirmations before applying the recommended action. | Medium |
| ~~**Install Without Staging APK**~~ ✅ 2026-05-14 | Installer queue items now preserve direct file/content `UriApkSource` inputs instead of forcing a global `CachedApkSource` copy. The final PackageInstaller session write streams from the original source when available, falls back to preview caches only when needed, keeps signed/APKM conversions cache-backed, and no longer risks deleting user-provided source APK files during `ApkFile` cleanup. Issue #1671 [S17]. | Medium |
| ~~**Install Existing Apps via `package:` URI**~~ ✅ 2026-05-14 | Completed the upstream v4.0.5 model: external `package:package-name` view/install intents now resolve to NG's installer, the existing `PackageInstallerCompat.installExisting()` path reinstalls an already-present APK for the selected/current user, and `SEND_MULTIPLE` intake now combines package/file/content URIs from both `EXTRA_STREAM` and `ClipData` without duplicates. | Low (pull) |
| ~~**Onboarding Capability Wizard**~~ ✅ 2026-05-14 | The first-run/replay onboarding sheet detects root, root manager, Shizuku authorization, Wireless ADB pairing/activity, USB debugging, and no-root fallback in a plain-language capability matrix. Shizuku now includes an installed manager version check against v13.6.0+ guidance for Android 16 QPR1 compatibility and surfaces the Android 13+ trusted-WLAN auto-start tip directly in the wizard. | Medium |
| ~~**USB Debugging Prompt in Shizuku Setup**~~ ✅ 2026-05-17 | Wireless ADB setup now checks `Settings.Global.adb_enabled` before pairing and shows an explicit preflight dialog when USB debugging is off, instructing users to enable both USB debugging and Wireless debugging before continuing. The Wireless ADB setup prompt, Shizuku explainer, Wireless ADB explainer, and ADB pairing instruction now all name both Developer Options toggles. Canta v3.2.0 model (fix for issue #284 [S43]). | Low |
| ~~**KernelSU Detection in Capability Wizard**~~ ✅ 2026-05-02 | New `runner/RootManagerInfo` probes `/data/adb/ksu` via the privileged shell (when root is granted) and falls back to the installed `me.weishu.kernelsu` / `com.rifsxd.ksunext` packages otherwise. Surfaced as a " · KernelSU" suffix on the onboarding sheet's Root status line; refresh fires async on bind + on Re-check. Pairs with Magisk + APatch detection in the same helper (one shell round-trip). | Low |
| ~~**Force Stop via Shizuku (Rootless)**~~ ✅ closed 2026-05-16 (already implemented) | [`AppInfoFragment.java:1747`](app/src/main/java/io/github/muntashirakon/AppManager/details/info/AppInfoFragment.java#L1747) registers a "Force Stop" action whenever `SelfPermissions.checkSelfOrRemotePermission(FORCE_STOP_PACKAGES)` succeeds — the privilege probe inherently dispatches through whatever provider is active (Shizuku binder, root, or ADB shell), so when Shizuku grants `android.permission.FORCE_STOP_PACKAGES` the action is enabled without any Shizuku-specific code path. Tap → `PackageManagerCompat.forceStopPackage(packageName, userId)` → `ActivityManagerCompat.getActivityManager().forceStopPackage(...)`. Reference: Hail v1.9.0 / [S65]. | Low |
| **Dhizuku (DeviceOwner) Privilege Path** | Add Dhizuku (GPL-3.0, [S71]) as a fourth rootless privilege tier alongside Root/Shizuku/ADB. Dhizuku shares `DevicePolicyManager` (DPM) ownership with third-party apps via a Binder proxy; activation requires a one-time `adb shell dpm set-device-owner com.rosan.dhizuku/.server.Server`. Unlocks DPM-based hide/suspend/freeze that Shizuku alone cannot perform. Supported Android 8–16. Capability wizard should detect Dhizuku like it detects Shizuku. **2026-05-17 implementation note:** do not add the official `io.github.iamr0s:Dhizuku-API` as a direct implementation dependency until the minSdk strategy is decided: latest API `2.5.4` is current on Maven Central and the upstream API module declares `MIN_SDK = 26`, while NG's floor is API 21 [S328, S329]. Prefer a reflection/optional-provider spike or a minSdk-23/26 decision before code integration. | Medium |
| ~~**APatch Detection in Capability Wizard**~~ ✅ 2026-05-02 | Same `runner/RootManagerInfo` helper probes `/data/adb/ap/` and the `me.bmax.apatch` package fallback. Surfaced as " · APatch" on the onboarding sheet's Root status line. SuperKey availability and per-module count surfacing deferred to the standalone Privilege Health-Check Screen (separate row, T5). | Low |
| ~~**Privilege Health-Check Screen**~~ ✅ 2026-05-17 | New Settings → **Privileges** page (`PrivilegeHealthPreferences`) supplements onboarding with persistent diagnostics: configured/inferred mode, working UID, mode self-test pass/fail, root manager in use (Magisk / KernelSU / APatch / none) with ZygiskNext/Sui markers when visible, Shizuku binding status + API version + minimum-version pass/fail, USB/Wireless ADB status and last pairing port, remote server/service state, and AppManagerNG battery-optimization state. The battery row reuses `SelfBatteryOptimization` for one-tap root/ADB auto-disable before falling back to Android's system exemption prompt. FreezeYou v11.0 self-diagnosis page model ([S75]); SD Maid SE v1.7.2 battery model ([S42]). | Medium |
| ~~**ZygiskNext Detection**~~ ✅ 2026-05-02 | `runner/RootManagerInfo` issues a follow-up `[ -d /data/adb/modules/zygisksu ]` check whenever a non-NONE root manager is detected; result surfaces as " + ZygiskNext" appended to the manager name on the onboarding sheet (e.g. "Detected · KernelSU + ZygiskNext"). Module-error-count via the v1.3.4+ WebUI is deferred to the standalone Privilege Health-Check Screen. | Low |
| ~~**Installer APK SHA256 Toast**~~ ✅ 2026-05-17 | `PackageInstallerCompat` now hashes the exact bytes streamed through each `PackageInstaller.Session.openWrite()` call, stores the session SHA-256 before `commit()`, and passes it through the pending-user-action handoff. `PackageInstallerActivity` shows a dismissible checksum dialog before launching Android's system install confirmation so the user can compare the session payload with a publisher checksum. Pure-JVM helper coverage: [`InstallChecksumDisplayTest`](app/src/test/java/io/github/muntashirakon/AppManager/apk/installer/InstallChecksumDisplayTest.java). Droid-ify v0.7.0 model ([S81]). | Low |
| ~~**Battery Optimization Bypass Toggle**~~ ✅ shipped 2026-05-02 | Added "Battery optimization" entry under Settings → Troubleshooting; surfaces live exemption state via `PowerManager.isIgnoringBatteryOptimizations()` and routes to either `ACTION_REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` (if optimized) or `ACTION_IGNORE_BATTERY_OPTIMIZATION_SETTINGS` (if exempt, so the user can revoke). Refreshes summary on `onResume`. Pre-M devices show a disabled entry with explanatory copy. Manifest declares `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS`. Dhizuku v2.11.2 model ([S84]). | Low |
| ~~**Android 16 Capability Dropping UI**~~ ✅ 2026-05-17 | Settings -> Privileges now has a "Capability dropping (--drop-cap)" row backed by `RootCapabilityDiagnostics`. The probe runs through the same `Runner` path as privileged work, reads `id -u` plus `CapEff` from `/proc/$$/status`, and reports root UID 0, non-root UID with all effective capabilities dropped, non-root UID with capabilities still present, unavailable, or unknown. This closes the roadmap's diagnostic-test route without depending on private Magisk/KernelSU/APatch config formats; deeper Magisk-version/policy parsing remains the separate T9 provider-introspection row. Magisk v30.7 model ([S85]); parser coverage in `RootCapabilityDiagnosticsTest`. | Low |
| **VPN Plugin Flags Control** ⏸ blocked-by Shizuku VPN binding | In Privilege Health-Check Screen, expose an opt-in toggle for system VPN flags (when using Shizuku VPN binding). The VPN-mode flag must be explicitly requested at session time; mirrors shadowsocks-android v5.3.5's opt-in model ([S87]) to surface user choice and avoid silent VPN-mode activation. Relevant for apps using Shizuku-controlled VPN services. **2026-05-17 audit:** repo search found no `VpnService`, `BIND_VPN_SERVICE`, or Shizuku VPN binding/session surface in AppManagerNG, so adding a toggle now would be dead UI. Keep parked until a real VPN integration or plugin binding lands. | Low |
| ~~**Auto-fix Battery Optimization (Root/ADB Path)**~~ ✅ 2026-05-17 | New `SelfBatteryOptimization` helper centralizes AppManagerNG's own Doze-exemption checks and privileged `DEVICE_POWER` auto-fix. Profile routine execution (`ProfileApplierService`) and long-running backup/import/restore batch operations (`BatchOpsService`) call it before work starts, using the same `DeviceIdleManagerCompat.disableBatteryOptimization()` binder path as the manual Troubleshooting entry. If root/ADB/Shizuku lacks `DEVICE_POWER`, the helper returns a no-privilege result and the existing manual Settings fallback remains available from Troubleshooting. SD Maid SE v1.7.x ([S112]) model. | Low |
| ~~**Cross-User Package State Detection**~~ ✅ 2026-05-17 | `ApplicationItem` now preserves enabled/disabled/uninstalled user-id buckets while collapsing DB rows into the main list. `MainRecyclerAdapter` shows compact state text such as `u0 enabled, u10 disabled`, colors mixed/disabled states with the existing warning tint, and appends state to the multi-user selection dialog. Finder also loads every selected user and shows each result's user/state. UAD-ng v1.2.0 ([S113]) model. | Medium |
| ~~**Auto-Update Debloat Definitions**~~ ✅ 2026-05-17 | `DebloatDefinitionsUpdater` auto-fetches the latest AppManagerNG-hosted debloat definition snapshot on app launch only when Settings → Privacy → "Update debloat definitions on launch" is enabled and the existing "Use the Internet" feature gate is on. It fetches [`docs/debloat-definitions/manifest.json`](docs/debloat-definitions/manifest.json) from the pinned SysAdminDoc/AppManagerNG raw-GitHub path, verifies `debloat.json` / `suggestions.json` by byte length + SHA-256, schema-smoke-tests both payloads, writes them atomically to app-private storage, clears the `StaticDataset` cache, and falls back to bundled assets when no valid cache exists. The request uses a generic User-Agent and sends no app package list/device identifier. Pure-JVM guardrail coverage: [`DebloatDefinitionsUpdaterTest`](app/src/test/java/io/github/muntashirakon/AppManager/debloat/DebloatDefinitionsUpdaterTest.java). UAD-ng v1.2.0 ([S113]) model. | Medium |

### T6 — Backup Polish

Neo Backup ([S20]) and Titanium Backup are the benchmark. AM's backup engine is competitive; the UX around scheduling, retention, and verification is not.

| Item | Description | Effort |
|------|-------------|--------|
| **Scheduled Auto-Backup** | WorkManager-based scheduler; triggers: time-of-day, charging state, network availability ([S09]). On API 36+, record and display WorkManager / JobScheduler stop and pending reasons (`WorkInfo.getStopReason()`, `JobParameters.getStopReason()`, `JobScheduler#getPendingJobReasonsHistory()`) when a run is deferred or killed, carrying the parked Android 16 quota-diagnostics row ([S45], [S124], [S125]). On API 37+, use `AlarmManager.setExactAndAllowWhileIdle` with the new `OnAlarmListener` callback instead of `PendingIntent` where exact schedule triggers are required, and surface `JobDebugInfo.getPendingJobReasonStats()` in schedule history when available ([S53]). | Medium |
| **Backup Retention Policy** | Set max backup count per app and age-based pruning (e.g. "keep last 3"); automatic cleanup on schedule ([S41] model) | Low |
| **1-Click Delete Old Backups** | Batch-clean oldest revisions across all apps from Backup list (Issue #387) | Low |
| ~~**Backup Integrity Verification**~~ ✅ closed 2026-05-16 (already implemented) | [`BackupOp`](app/src/main/java/io/github/muntashirakon/AppManager/backup/BackupOp.java) computes a SHA-256 digest per backup file via `DigestUtils.getHexDigest(mMetadata.info.checksumAlgo, file)` and persists the table through [`BackupItems.Checksum`](app/src/main/java/io/github/muntashirakon/AppManager/backup/BackupItems.java#L535). [`RestoreOp`](app/src/main/java/io/github/muntashirakon/AppManager/backup/RestoreOp.java) re-computes the digest for `info.json`, `meta.json`, master key, data tar parts, certificates, and misc files at restore time and raises `BackupException` on mismatch. The checksum algorithm is stored in metadata so backups created with a future stronger digest can still be restored. | Medium |
| ~~**AES-256 Backup Encryption**~~ ✅ closed 2026-05-16 (already implemented, key derivation differs from spec) | NG ships AES-256-GCM backup encryption via [`AESCrypto`](app/src/main/java/io/github/muntashirakon/AppManager/crypto/AESCrypto.java) selected through [`CryptoUtils.MODE_AES`](app/src/main/java/io/github/muntashirakon/AppManager/backup/CryptoUtils.java) in the backup-mode picker. The implementation uses Android Keystore-backed `SecretKey` material (more secure than the original row's PBKDF2 sketch — keys are non-exportable and hardware-isolated where TEE is available) and the recently-shipped metadata-v6 per-file IV derivation. 128-bit GCM MAC since v3.0.3; older 32-bit MAC backups remain restorable. | Medium |
| ~~**PGP Backup Encryption**~~ ✅ closed 2026-05-16 (already implemented) | [`OpenPGPCrypto`](app/src/main/java/io/github/muntashirakon/AppManager/crypto/OpenPGPCrypto.java) integrates the OpenIntents OpenPGP API; encrypted backups go through any OpenPGP provider the user has installed (OpenKeychain is the canonical option). Provider selection lives at `Prefs.Encryption.getOpenPgpProvider()`. Selected via `CryptoUtils.MODE_OPEN_PGP` alongside AES / RSA / ECC in the backup mode picker. Reference: Neo Backup v8.3.12 ([S41]). | Medium |
| ~~**Backup Sharing Button**~~ ✅ 2026-05-16 | The Restore-Single popup menu now exposes a "Share backup" action (enabled when exactly one backup is selected). Reuses the existing [`SharableItems`](app/src/main/java/io/github/muntashirakon/AppManager/fm/SharableItems.java) helper to wrap every file under the backup directory in a single `ACTION_SEND_MULTIPLE` chooser intent through `FmProvider`. Encrypted backups stay encrypted on the way out — the recipient still needs the user's AppManagerNG encryption key to restore. Reference: Neo Backup v8.3.17 ([S41]). | Low |
| **Launcher Shortcuts for Backup Schedules** | Pin a specific backup schedule as a home screen launcher shortcut for one-tap execution. Neo Backup v8.3.17 model ([S41]). | Low |
| **Rich Backup Result Notifications** | Show per-app name, backup count, and total size in the completion notification rather than a generic "done" message. SD Maid SE scheduler model ([S42]). Distinct from the in-progress backup progress notification item. | Low |
| **Backup Progress Notifications** | Rich ongoing notification with per-app name, progress bar, and ETA. On API 36+, use `Notification.ProgressStyle` with milestone `Point` and `Segment` markers to show per-stage progress ("preparing → 3/12 apps → verifying → done") in the notification shade and lock screen; `Build.VERSION.SDK_INT >= 36` guard with graceful `setProgress()` fallback below ([S46]). | Low |
| **Pre-Backup Storage Check** | Verify available storage is sufficient before starting a backup operation; alert user with dismiss-able dialog if likely to fail mid-way. Neo Backup v8.3.15 model ([S41]). | Low |
| **Separated Active/Paused Schedule Lists** | Display enabled and disabled backup schedules in distinct sections rather than a flat unsorted list; mirrors how the system separates active vs. silent alarms. Neo Backup v8.3.15 model ([S41]). | Low |
| ~~**Backup Tag Autocomplete**~~ ✅ 2026-05-08 (closed 2026-05-16 as duplicate) | Already shipped under iter-21 "Neo-Backup-Style Existing-Tag Suggestions": [`BackupFragment.collectExistingBackupNames()`](app/src/main/java/io/github/muntashirakon/AppManager/backup/dialog/BackupFragment.java) walks every `BackupInfo` in scope, harvests distinct non-empty `BackupMetadataV5.metadata.backupName` values, and feeds the de-duplicated `LinkedHashSet` into `TextInputDropdownDialogBuilder.setDropdownItems()`. The multi-backup name dialog now autocompletes from prior labels. When the planned multi-tag DAO lands, the same `setDropdownItems` adapter pattern can be reused for the tag-add dialog with zero changes. Reference: [S41]. | Low |
| **Scheduler Battery Optimization Auto-Fix** | When user enables a backup schedule, auto-detect if battery optimization is active for the NG process and, if root or ADB is available, disable it automatically; otherwise surface a one-tap "Exempt from battery optimization" prompt. Prevents system job-killing from silently skipping scheduled backups. SD Maid SE v1.7.2 model ([S42]). **2026-05-17 note:** the privileged batch-operation auto-fix path is now shipped for backup/import/restore execution via `BatchOpsService`; this row remains open for the future Scheduled Auto-Backup UI's schedule-enable prompt and non-privileged fallback copy. | Low |
| **JobDebugInfo Schedule Diagnostics** | When a scheduled backup fails to fire on Android 17+ (API 37), surface `JobDebugInfo.getPendingJobReasonStats()` output in the operation log to explain the skip (e.g. "quota exhausted", "app in background-restricted bucket"). Conditional on `Build.VERSION.SDK_INT >= 37`; graceful no-op below ([S53]). | Low |
| **Export/Import App List** | Export selected/filtered app list to JSON; import to recreate filter or drive batch ops (Issue #122 [S05], 8 reactions) | Medium |
| **SMB / WebDAV Network Backup Destination** | Allow backup archives to be written directly to an SMB share or WebDAV endpoint in addition to local storage; requires root or `MANAGE_EXTERNAL_STORAGE` for full data backup access; scope limited to backup storage-destination selection — no change to the backup engine itself. Android-DataBackup v2.x model ([S79]). | High |

### T7 — Finder (Cross-App Search)

Code exists (`FinderActivity`, `FinderViewModel`) but is incomplete per `TODO` markers. Issue #321 ([S04]) has 9 reactions and is referenced as an "upcoming feature" in upstream README.

| Item | Description | Effort |
|------|-------------|--------|
| ~~**Finder: Components**~~ ✅ closed 2026-05-16 (already implemented) | [`ComponentsOption`](app/src/main/java/io/github/muntashirakon/AppManager/filters/options/ComponentsOption.java) ships in the Finder filter ladder with `with_type` / `without_type` (activity / service / receiver / provider bit flags) plus `eq` / `contains` / `starts_with` / `ends_with` / `regex` predicates for the component class name. | Medium |
| ~~**Finder: Permissions**~~ ✅ closed 2026-05-16 (already implemented) | [`PermissionsOption`](app/src/main/java/io/github/muntashirakon/AppManager/filters/options/PermissionsOption.java) ships in the Finder filter ladder with `eq` / `contains` / `starts_with` / `ends_with` / `regex` predicates over declared permission names. (Curated flag / grant-state filters are still tracked as an inline TODO inside the file for follow-up; the row's core ask — "find apps holding a permission" — is shipped.) | Low |
| ~~**Finder: AppOps**~~ ✅ 2026-05-16 | New [`AppOpsOption`](app/src/main/java/io/github/muntashirakon/AppManager/filters/options/AppOpsOption.java) registered under the `app_ops` key in [`FilterOptions`](app/src/main/java/io/github/muntashirakon/AppManager/filters/options/FilterOptions.java). Name predicates: `eq` / `contains` / `starts_with` / `ends_with` / `regex` over the op name (`OpEntry.getName()`). Mode-based filtering: new `with_mode` key takes a `TYPE_INT_FLAGS` bitfield (`MODE_FLAG_ALLOWED` / `_IGNORED` / `_ERRORED` / `_DEFAULT` / `_FOREGROUND` with bit position = `AppOpsManager.MODE_*`) so users can pick "only allowed", "only foreground", or any combination. Closes the row's "specific app op in a specific state" ask. | Low |
| ~~**Finder: Regex Support**~~ ✅ 2026-05-16 | `ComponentsOption` already had a `regex` predicate; the matching `name_regex` predicate now lives in [`TrackersOption`](app/src/main/java/io/github/muntashirakon/AppManager/filters/options/TrackersOption.java) so the Finder filter ladder offers regex matching consistently across tracker and component class names. The TrackersOption inline TODO that called this out is now resolved. | Low |
| ~~**Finder: Tracker Name Search**~~ ✅ 2026-05-16 | [`TrackersOption`](app/src/main/java/io/github/muntashirakon/AppManager/filters/options/TrackersOption.java) gains `name_eq` / `name_contains` / `name_starts_with` / `name_ends_with` / `name_regex` predicates over the fully-qualified `ComponentInfo.name` of each tracker component the scanner attributed to the app. Match results are threaded through `TestResult.setMatchedTrackers` so downstream filter chains can compose with the matched subset. The TrackersOption inline TODO is resolved. | Low |
| ~~**Finder: Multi-User Scope**~~ ✅ 2026-05-17 | `FinderViewModel` now loads `FilteringUtils.loadFilterableAppInfo(Users.getUsersIds())` instead of hard-coding `UserHandleHidden.myUserId()`, and `FinderAdapter` shows `User <id>: enabled/disabled/not installed` on each result so duplicate package matches from work/secondary profiles are distinguishable. | Medium |
| ~~**Finder: Uninstalled App Backups**~~ ✅ 2026-05-17 | [`FinderViewModel`](app/src/main/java/io/github/muntashirakon/AppManager/filters/FinderViewModel.java) now opts into backup-only entries through [`FilteringUtils`](app/src/main/java/io/github/muntashirakon/AppManager/filters/FilteringUtils.java), which reads AppManagerNG backup metadata, validates that the archive still exists, selects the latest archive per package/user, and skips package/user pairs already returned by `MATCH_UNINSTALLED_PACKAGES`. [`BackupFilterableAppInfo`](app/src/main/java/io/github/muntashirakon/AppManager/filters/BackupFilterableAppInfo.java) supplies synthetic package/app metadata so existing Finder filters can match archived uninstalled apps, and the old `FinderViewModel` TODO is removed. | Medium |
| ~~**Filter: Permission Flags**~~ ✅ 2026-05-17 | [`PermissionsOption`](app/src/main/java/io/github/muntashirakon/AppManager/filters/options/PermissionsOption.java) now supports `granted`, `denied`, `custom`, `fixed`, `with_flags`, and `without_flags` predicates. [`FilterablePermissionInfo`](app/src/main/java/io/github/muntashirakon/AppManager/filters/FilterablePermissionInfo.java) centralizes requested-permission grant state, source package, declaration/protection metadata, and runtime permission flags for both `FilterableAppInfo` and `ApplicationItem`, so Finder/profile/main-list filter consumers can use the same permission-state model. | Low |
| ~~**Filter: Data Usage Split**~~ ✅ 2026-05-16 | [`IFilterableAppInfo`](app/src/main/java/io/github/muntashirakon/AppManager/filters/IFilterableAppInfo.java) gains `getMobileDataUsage()` / `getWifiDataUsage()` (implemented in both `FilterableAppInfo` and `ApplicationItem` by reading the existing `PackageUsageInfo.mobileData` / `wifiData` fields). [`DataUsageOption`](app/src/main/java/io/github/muntashirakon/AppManager/filters/options/DataUsageOption.java) gains `mobile_le` / `mobile_ge` / `wifi_le` / `wifi_ge` predicates alongside the existing combined ones. The TODO that called this out is now resolved. | Low |
| ~~**Frozen/Disabled Filter in App List**~~ ✅ pre-existing | The main-list quick-filter chip row already includes a first-class **Frozen** chip alongside User / System / Running / Backed Up / Stopped / Trackers (see `chip_frozen` in [activity_main.xml:76-82](app/src/main/res/layout/activity_main.xml#L76-L82), wired to `MainListOptions.FILTER_FROZEN_APPS`). AM's freeze model already covers the disable/freeze cases that Canta exposes separately (component-disable, suspend, cloak, hide), so a distinct "Disabled" chip on top of "Frozen" would be redundant on this stack. Audit row was stale; closing it during the 2026-05-02 batch. | Low |
| ~~**Finder: Relevance-Based Search Scoring**~~ ✅ 2026-05-17 | [`FinderViewModel`](app/src/main/java/io/github/muntashirakon/AppManager/filters/FinderViewModel.java) now sends filtered results through [`FinderRelevanceScorer`](app/src/main/java/io/github/muntashirakon/AppManager/filters/FinderRelevanceScorer.java) before posting them. The scorer extracts literal search terms from package-name, component-name, and tracker-name filter options, then ranks matches by Levenshtein distance against full package names, simple names, tokens, sliding windows, and matched component/tracker names. Regex and negative predicates are excluded from scoring, and rows that only matched unrelated filters keep their original scan order. Inure build106.3.3 model ([S66]). | Low |
| ~~**Finder: Description-Field Search**~~ ✅ 2026-05-17 | Finder's existing `bloatware` filter now includes `description_eq`, `description_contains`, `description_starts_with`, `description_ends_with`, and `description_regex` predicates over the matched `DebloatObject.getDescription()` text loaded from `debloat.json` / cached debloat definitions. Contains/prefix/suffix predicates are case-insensitive for prose search; regex keeps the user's raw pattern. UAD-NG v1.2.0 model ([S67]); metadata bundle already ships via android-debloat-list ([S23]). Parser coverage in `BloatwareOptionTest`. | Low |
| ~~**Bloatware Safety Rating in App Detail**~~ ✅ pre-existing | App Info tag cloud surfaces "Bloatware · Safe / Replace / Caution / Unsafe" via `getBloatwareSafetyLabel(context, removalType)` (`AppInfoFragment.java:1206`) coloured by `ColorCodes.getBloatwareIndicatorColor`; tap opens `BloatwareDetailsDialog` which lists declared `dependencies` from the debloat-list JSON. Rating + dependencies surfacing both shipped; the audit row was stale. | Low |

### T8 — Profiles & Automation

Routine Operations is the #2 requested feature (21 reactions, Issue #61 [S03]). Muntashir himself published the full technical spec in that issue in 2020; implementation was deferred. NG should ship this if upstream continues to stall.

| Item | Description | Effort |
|------|-------------|--------|
| **Routine Operations / Scheduler** | Event-triggered profile execution: boot, custom interval, network available, charging, screen on/off, app foreground/background (full spec: Issue #61 [S03]) | High |
| **Saved Filter Presets** | Persist named filter configurations that auto-include all user apps, allow excluding specific user apps, and manually include specific system apps (Issue #1718 [S38]). Distinct from Finder — these are persistent main-list presets. | Medium |
| ~~**Launcher Shortcuts for AM Features**~~ ✅ 2026-05-09 (static set) | Static launcher shortcuts shipped at [`app/src/main/res/xml/shortcuts.xml`](app/src/main/res/xml/shortcuts.xml) and wired onto `SplashActivity` via `<meta-data android:name="android.app.shortcuts" android:resource="@xml/shortcuts"/>`. Three core power-user entry points exposed on long-press of the AM icon: **1-Click Ops** (batch operations), **Running Apps** (process inspector), and **Finder** (cross-app search). `FinderActivity` and `OneClickOpsActivity` flipped to `exported="true"` so the launcher can dispatch the shortcut intents. Pinned per-app shortcuts (Freeze / Force-Stop / Clear Cache) remain wired through `ShortcutManagerCompat` in `CreateShortcutDialogFragment`; this commit is the static-launcher anchor that the upcoming dynamic top-N pinned-app set (iter-22 [S252]) will extend. Issue #660 [S32]. | Low |
| **Profile State Conditions** | Profiles with conditions (execute only if battery > X%, only between hours Y–Z) | Medium |
| **Schedule Result Notifications with Detail** | When a scheduled operation completes, include in the result notification *what was actually done*: e.g. "Backup complete: 47 apps, 2.3GB, 0 errors". Reference: SD Maid SE v1.7.x ([S112]) "Show what was cleaned in result notifications". Use `Notification.BigTextStyle` or Android 16+ `Notification.ProgressStyle` for finished work summary. | Low |
| **Searchable Schedule Tags** | Allow tagging schedules and routine operations (e.g. "weekly", "pre-update", "test"); search/filter by tag in scheduler view. Reference: Neo Backup v8.3.17 ([S114]) "Existing tags suggestion in the adding dialog". Reduces clutter in heavy-user scheduler dashboards. | Low |
| **KeepAndroidOpen Banner for Long Operations** | Display a "Keep this app open" persistent banner during long-running operations (full backups, batch ops, debloating); explains to users that backgrounding may pause work and shows estimated time remaining. Reference: Neo Backup v8.3.17 ([S114]) KeepAndroidOpen banner. Improves operation-completion rate on devices with aggressive battery management. | Low |
| ~~**Profile Import/Export**~~ ✅ 2026-05-02 | File-roundtrip path was already wired (`ProfilesActivity.mImportProfile.launch("application/json")` + `mExportProfile.launch(profile.name + ".am.json")`). New **Share as JSON** popup action (`action_share`) sends the same JSON via `Intent.ACTION_SEND` for one-tap cross-device transfer (Telegram / KDE Connect / email / Gmail draft) without a SAF round-trip. `BaseProfile.serializeToJson()` already produces the wire format consumed by `BaseProfile.fromPath`, so the receiving NG instance imports through the existing Import action verbatim. | Low |
| **Launcher Backup Restoration** | Support importing backup archives from competing launcher tools (Nova Launcher, Action Launcher, Lawnchair); restores icon grid layout, widgets, folders, and app organization within NG's App Manager view. Lawnchair 15 Beta 3 model ([S86]). Scope: launcher icon grid and widget placement only; does not include app installation. | Medium |
| **Intent Interceptor: Return Results** | Send activity results back to the original calling app after interception (ActivityInterceptor.java TODO) | Medium |
| **Operation Activity Log** | Persistent log of every operation AM has performed (freeze, backup, AppOps change, etc.); filterable by app, type, date (Issue #143 [S13], 3 reactions) | Medium |
| **Multi-Tag per App** | Allow assigning multiple named tags to each app; batch freeze, backup, profile, and filter operations target tag sets instead of hand-picked lists. Hail v1.10.0 model ([S65]). Schema decision required — tags stored in the Room `apps` table alongside existing `tracker_count`/`dangerous_perm_total` columns; many-to-many via a `app_tags` join table. | Medium |
| **URI / Intent Automation API** | Expose a documented `am://` URI scheme for triggering freeze, backup, and profile operations from external apps (Tasker, home screen shortcuts, quick-settings tiles); mirrors Hail v1.10.0's URI automation schema ([S65]). Prerequisite for the Tasker Plugin (Under Consideration). Pairs with T18 deep-link infrastructure — same URL-dispatch layer. | Medium |

### T9 — Privacy & Security

| Item | Description | Effort |
|------|-------------|--------|
| ~~**Biometric App Lock**~~ ✅ closed 2026-05-16 (already implemented) | [`BaseActivity`](app/src/main/java/io/github/muntashirakon/AppManager/BaseActivity.java) gates every authenticated screen behind `BiometricPrompt` (line 122): each activity calls `onAuthenticated()` only after `BiometricPrompt.AuthenticationCallback.onAuthenticationSucceeded()` fires, with the same delegate handling `onAuthenticationError` / `onAuthenticationFailed`. The screen-lock preference and biometric authenticator type live under `compat/BiometricAuthenticatorsCompat`. Inherited at app open via `SplashActivity` extending `BaseActivity`. | Low |
| **App-Ops Per-UID Control** | Allow setting app ops by UID (`--uid`) in addition to per-package; essential for shared-UID system packages where per-package mode is ambiguous (Issue #1863 [S37]). | Medium |
| **AppOps Quick-Toggle in List** | Add an inline allow/deny/default toggle chip directly on each AppOps row in the App Details AppOps tab; avoids opening a full dialog for simple permission mode changes. Reduces interaction depth for bulk AppOps editing sessions. Inure build106.5.1 model ([S66]). | Low |
| **Tracker Blocking (AppOps)** | One-click disable specific tracker components via `AppOps.setUidMode`; show which trackers are currently active. Three graduated blocking intensities: minimal (detect + report only), standard (disable known problematic SDKs), strict (disable all detected trackers); balances privacy vs. app compatibility. TrackerControl 2026040301 model ([S69]). | Medium |
| **Per-App Tracker Statistics Panel** | In App Details, show the count of detected trackers and their associated tracking companies (aggregated by organization, not just SDK name); source data from the εxodus database already embedded in-app. Informational regardless of blocking status; pairs with Tracker Blocking (AppOps). TrackerControl 2026040301 model ([S69]). | Low |
| ~~**Sort by Dangerous Permissions**~~ ✅ pre-existing | `MainListOptions.SORT_BY_DANGEROUS_PERMS = 18` is wired into `MainListOptions.SORT_OPTIONS_MAP` and consumed by `MainViewModel` (sort rule at `MainViewModel.java:658` — primary `dangerousPermGranted` desc, secondary `dangerousPermTotal` desc, mirrors `SORT_BY_TRACKERS` shape). Room v9 columns (`dangerous_perm_total` / `dangerous_perm_granted`) backing the sort are populated. The audit row was stale; closing it during the 2026-05-02 batch. | Low |
| **Privacy Dashboard Integration** | Surface Android 12+ permission usage history (timeline view) inside AM app details | Medium |
| **App Signing Key Change Alert** | Alert when the signing certificate of an installed update differs from the previously stored one | Medium |
| **Permission Policy Flags Display** | Surface `requestedPermissionsFlags` bits (system-fixed, user-set, review-required, preinstalled, installer-exempt) alongside each permission entry in the App Details permissions list. `PackageInfo.requestedPermissionsFlags` already available; pure UI addition. Inure build106.5.0 model ([S66]). | Low |
| **MiUI-Specific AppOps Mapping** | Surface MIUI extended op-codes with human-readable descriptions and full edit support for MIUI/Xiaomi devices. Conditional on `Build.MANUFACTURER.equals("Xiaomi")`; requires embedding a MIUI op-code lookup table. Inure build106.5.0 model ([S66]). | Low–Med |
| **Permission Change Monitor** | Notify when an installed app update gains new dangerous or sensitive permissions vs. the previously installed version; tap to deep-link to App Details permission list for review. Root/Shizuku not required — `PackageManager` broadcast + stored per-app permission snapshot suffices. Permission Manager X Perm Watcher model ([S70]). | Medium |
| **APK Signing Scheme Display** | Show all active APK signing schemes (V1 JAR, V2 APK Scheme, V3 key rotation, V4 incremental) in the App Details Signatures tab. `ApkSignatureSchemeV2Verifier` and `ApkSignatureSchemeV3Verifier` are already part of AOSP; scheme flags are readable without root. LibChecker 2.5.2 model ([S72]). | Low |
| **Permission Provider Attribution** | For each custom permission declared by installed apps, show which installed package provides (declares) that permission in the permissions list. Uses `PackageManager.resolvePermission()` with the declaring package name. Surfaces hidden cross-app permission dependencies that could persist after partial uninstalls. LibChecker 2.5.3 model ([S72]). | Low |
| **Package Visibility Analysis** | In App Details, surface: (a) whether the app holds `QUERY_ALL_PACKAGES` (can enumerate all installed apps), (b) its `<queries>` manifest entries (targeted package-visibility declarations), and (c) other installed apps that explicitly list this package in their own `<queries>`. Uses `PackageInfo.requestedPermissions` + manifest parsing — no root. Highlights apps that enumerate the full package list (privacy/attack-surface signal). Island v6.4 visibility-filter model ([S78]). | Low |
| **Install Source Attribution Detail** | In App Details, distinguish `getInitiatingPackageName()` (the app that triggered the install session) from `getOriginatingPackageName()` (the declared source); surface the installer's signing-cert digest (`getInstallInitiatingPackageCertificateDigest()`, API 34+). Detects cases where an APK's stated origin differs from the app that actually opened the `PackageInstaller` session. `PackageManager.getInstallSourceInfo()` (API 30+) ([S83]). | Low |

### T10 — i18n & Accessibility

| Item | Description | Effort |
|------|-------------|--------|
| **Translation Platform (Weblate)** | Self-host or use Hosted Weblate (model: Neo Backup [S20]); link from CONTRIBUTING.md | Low |
| **RTL Layout Verification** | Full test pass under Arabic/Hebrew locale; fix any mirroring or truncation issues | Medium |
| ~~**Plural String Audit**~~ ✅ 2026-05-02 | Sweep complete. Codebase already shipped 66 `<plurals>` blocks; the remaining `%d`-using format strings split into pluralizable (`main_status_showing_apps`, `main_status_all_apps`, `bar_chart_content_description`) and non-pluralizable (IDs/positions/range bounds: `pid_and_ppid`, `user_with_id`, `external_multiple_data_dir`, `vt_success`, `tag_dangerous_perms` "X of Y" composites, etc.). The three pluralizable strings are now `<plurals>` blocks consumed via `getQuantityString()`; orphan `selected_items_accessibility_description` removed. | Low |
| **Locale-Aware Date/Number Formatting** | Replace raw `SimpleDateFormat` instances with `DateTimeFormatter` + locale-aware formatting | Low |
| **TalkBack Navigation Audit** | Full TalkBack pass on every major screen; fix traversal order, group headings, action labels | Medium |
| **Content Descriptions on Icon Buttons** | Add `contentDescription` to all icon-only action buttons across the app | Low |
| **Font Scale Stress Test** | Test all screens at 200% font scale; fix overflow and truncation | Medium |

### T11 — Developer Experience

| Item | Description | Effort |
|------|-------------|--------|
| **Upstream Diff Tracking** | CI check that weekly reports divergence between NG main and upstream main; summarize cherry-pick candidates | Low |
| **Changelog Automation** | GitHub Action: extract PR titles tagged `[changelog]` into CHANGELOG.md draft on each merge | Low |
| **Architecture Documentation** | Document AIDL service architecture, privilege escalation paths, backup format spec in `/docs/architecture/` | Medium |
| **Unit Test Coverage Expansion** | Extend Robolectric test suite to cover backup/restore engine, permissions resolution, and Finder query logic (currently only basic unit tests exist per `tests.yml`) | High |

---

## Later

Items below depend on upstream shipping features, on prior work in the Now/Next tier landing, or on architectural decisions not yet made.

### T12 — APK Editing & Analysis

Upstream has committed to "basic APK editing" in its Upcoming Features list. NG tracks this closely. Engineering debt in `DexClasses.java` and `CodeEditorViewModel.java` must be resolved (see T3) before this tier is viable.

| Item | Description | Effort | Dependency |
|------|-------------|--------|------------|
| **Basic APK Editing (upstream pull)** | Pull upstream's APK editing when it ships | Low (pull) | Upstream |
| **Smali/Baksmali Roundtrip for API<26** | Complete DexClasses.java FIXME — baksmali 3.0.9 already dep'd | High | T3 ABX fix |
| **APK Signing with Custom Key** | Sign modified APKs with user-provided keystore file | Medium | — |
| **ARSC Resource Table Editing** | Visual editor for string/resource tables via ARSCLib (already dep'd) | High | — |
| **AndroidManifest Visual Editor** | Point-and-click editor for common manifest attributes | High | — |
| **APK Merge (splits → universal)** | Merge split APK set into a single universal APK (model: APKEditor V1.4.8 [S48]) | Medium | — |
| **Structured APK Editing Architecture** | When implementing T12 full APK editing, reference APKEditor v1.4.8's three-layer structure: manifest editing (XML parsing + validation), resource table editing (ARSC parsing), and dex/smali editing (baksmali roundtrip). Allows incremental rollout: resource table first, then manifest, then code. APKEditor v1.4.8 ([S89]) is the reference implementation in the OSS space. | High | — |
| **JADX Decompiler GUI Integration (Read-Only)** | Embed or link to JADX v1.5.5+ decompiler GUI for java source code viewing within App Details "Code" tab (read-only; no editing). JADX v1.5.5 offers cross-platform GUI, split-view mode for parallel viewing, and `.apks` support. External app launch or in-process WebView fallback ([S94]). Provides power users with one-click decompile without leaving NG. | High | JADX binary distribution |
| **Split APK (.apks) Installer Integration** | When opening a `.apks` or `.xapk` file in File Manager, auto-detect and parse the archive; surface split variants (arch, density, language) in a selection dialog; install selected splits alongside the base APK in a single `PackageInstaller` session. Mirrors SAI v4.3 model ([S25]); JADX v1.5.5 and Bundletool v1.18.3 both added `.apks` support signal ([S94], [S100]). | Medium | — |
| **Native Library Sizes in App Details** | In the App Details library tab, display the file size of each loaded `.so` alongside the library name; enumerate `ApplicationInfo.nativeLibraryDir` entries. Inure build107.0.1 model ([S66]). | Low | — |
| **Batch APK Installer from File Manager** | Allow selecting multiple APK/APKS files in the built-in File Manager and installing them in a single batch `PackageInstaller` session. Inure build107.0.0 model ([S66]). | Medium | — |
| **Force DEX Compile Optimization** | Add "Force Optimize" action in App Details triggering `cmd package compile -m speed <pkg>` (or `-m speed-profile` for profile-guided AOT); surfaces three profiles: `speed` (full AOT), `speed-profile`, and `quicken`; requires Root or Shizuku. Useful for apps that ended up in interpreter mode after sideloading or OTA. AppBooster (awesome-shizuku Miscellaneous section [S68]) model. | Low |
| **Large APK Pre-Install Warning** | When user attempts to install an APK > 150MB via File Manager or Intent Interceptor, show a warning dialog with file size, free space remaining, and an "Are you sure?" confirmation. Reference: SAI v4.5 ([S116]) "Added warning when installing huge (>150MB) apps". Prevents accidental installs of large packages on low-storage devices. | Low | — |
| **APKM/XAPK Format Support** | Extend File Manager and split APK installer to recognize `.apkm` (encrypted/unencrypted) and `.xapk` archive formats in addition to standard `.apks`. Reference: SAI v4.3 ([S116]) APKM support, partial XAPK with `.obb` known to need manual placement. Decoupling format-detection from install path keeps the Universal Installer clean. | Medium | — |
| **Per-Lib 16 KB Page-Alignment Indicator** | In the App Details native library tab, badge each `.so` entry with its 16 KB page alignment status. Non-aligned libs cause `dlopen()` failures on Android 15+ 64-bit kernels (including Pixel 9 and any device shipping with a 16 KB page-size kernel). Flags impacted apps in the main list for user action. LibChecker 2.5.2 model ([S72]); alignment check uses ELF `p_align` field, no root required. | Low | — |
| **App State Snapshot & Version-Diff Timeline** | On each install/update, record a snapshot: version code/name, signing certificate fingerprint, min/target SDK, ABI split list, declared permissions, and Build ID. Show a diff between any two snapshots in App Details under a new "History" tab. Detects silent cert rotations, ABI drops, and permission creep across update chains. LibChecker 2.5.3 model ([S72]); requires Room schema extension + per-app history table. | High | — |
| **APK URL Metadata Preview** | Analyze an APK from a direct download URL without fully downloading it: HTTP Range request fetches the ZIP central directory + compressed manifest; parse `AndroidManifest.xml`, signing cert, and ABI list; present summary in a bottom sheet before committing to a full download. LibChecker 2.5.3 model ([S72]). Useful for vetting sideloaded APKs from Obtainium-managed sources. | Medium | — |
| **Native Library Extraction** | Extract a selected `.so` file from an installed or locally-opened APK to the device's Downloads folder. Targeted at reverse-engineering workflows: extract the specific library, inspect ELF headers, compare ABI builds. LibChecker 2.5.3 model ([S72]); requires read access to the APK path (no root for installed apps; root for data partitions). | Low | — |

### T13 — Database & File System

Upstream lists "database viewer and editor" in Upcoming Features (Issue #14 [S11], 3 reactions).

| Item | Description | Effort | Dependency |
|------|-------------|--------|------------|
| **Database Viewer (upstream pull)** | Pull when upstream ships; read-only first | Low (pull) | Upstream |
| **Database Editor** | Write capability after viewer is stable | High | DB Viewer |
| **DocumentsProvider** | Implement proper `DocumentsProvider` for third-party file manager access (FmProvider.java TODO, Issue #516 [S06], 7 reactions) | High | — |
| **File Manager Compression** | ZIP/tar archive creation and extraction in built-in file manager | Medium | — |
| **File Hash Display** | SHA-256/MD5 display for files viewed in file manager | Low | — |
| **File MIME Type Recognition** | Detect and display MIME types for files in the File Manager (e.g. `application/vnd.android.package-archive` for APK). Termux v0.118.3 added basic MIME type recognition in its ContentProvider ([S91]); NG can apply similar logic in file browsing contexts. | Low | — |

### T14 — Terminal & Code Editor Polish

Upstream's "more advanced terminal emulator" is an upcoming feature. NG will pull rather than reimplement.

| Item | Description | Effort | Dependency |
|------|-------------|--------|------------|
| **Advanced Terminal (upstream pull)** | Pull upstream's terminal improvements when they ship | Low (pull) | Upstream |
| ~~**Code Editor i18n**~~ ✅ 2026-05-02 | Hardcoded "tabs"/"spaces" suffix on the toolbar now reads from `R.plurals.editor_tab_size_option_{tabs,spaces}` so locales pluralise correctly. Picker titles + go-to-line labels are also `R.string` resources. | Medium | — |
| ~~**Code Editor Tab Size Setting**~~ ✅ 2026-05-02 | Tap the indent-size toolbar button → popup with **2 / 4 / 8** options (configurable per indent-mode). Wires to `mEditor.setTabWidth(n)` and refreshes the label live. | Low | — |
| ~~**Code Editor Language Display**~~ ✅ 2026-05-02 | Tap the language toolbar button → popup listing all seven tmLanguage-backed languages bundled in `assets/languages/` (java / json / kotlin / properties / sh / smali / xml). Picking switches the highlighter and persists via new `CodeEditorViewModel.setLanguage()`; the indent label re-renders against the new language's `useTab/useSpace` default. | Low | — |

### T15 — Systemless Operations

Issue #150 ([S10], 4 reactions). Magisk/overlayfs-based reversible system app management without modifying `/system`.

| Item | Description | Effort | Dependency |
|------|-------------|--------|------------|
| **Systemless Freeze/Disable** | Disable system apps without removing them; uses overlayfs or Magisk module approach | High | Root |
| **Systemless Uninstall** | Remove system apps without modifying the system partition; reversible via module removal | High | Root + Magisk API |
| **Boot Loop Guard** | Automatically re-enable apps if boot does not complete within N seconds after systemless op (model: Issue #1161) | Medium | Systemless freeze |

### T16 — Clone & Isolation

Issue #1029 ([S14], 2 reactions).

| Item | Description | Effort | Dependency |
|------|-------------|--------|------------|
| **Clone Application** | Install a duplicate app instance under a separate Android user ID with data isolation | High | Multi-user API + Root |

### T17 — Advanced Accessibility & High-Contrast

| Item | Description | Effort |
|------|-------------|--------|
| **High-Contrast Theme** | High-contrast color variant alongside dark/light/AMOLED | Low |
| **Dynamic Font Size Compliance** | Full compliance with Android's font scaling up to 200%. Audit dashboard progress cards, app cards, and detail pages so they handle larger fonts without truncation or clipping. Pattern: SD Maid SE v1.7.x ([S112]) "make dashboard progress cards handle larger font sizes" PR series. | Medium |
| **TalkBack/Screen Reader Audit** | Comprehensive Compose Semantics audit: every clickable, toggleable, and image element has `contentDescription` or `semantics { role = ... }`. Validate with Google's TalkBack 6.1.1 ([S119]) and Compose UI tests via `assertIsDisplayed()` + `onNodeWithContentDescription()`. Wire announcement events for batch operation progress and result. Required for IAAP / WCAG 2.1 AA compliance. | Medium |
| **Reduced Motion Support** | Respect system "Remove animations" toggle (`Settings.Global.TRANSITION_ANIMATION_SCALE`); skip page transitions, ripple animations, predictive back when set to 0. Compose: detect via `LocalDensity` + `Settings.System` query at composition root. Reference: PodAura v3.2 ([S118]) Material 3 Expressive accessibility patterns. | Low |
| **Dyslexia-Friendly Font Option** | Add OpenDyslexic or Atkinson Hyperlegible as optional Settings > Appearance > Font. Reference: Markor v2.15.0 dyslexia-friendly font ([S117]). Bundles ~250KB; gate behind opt-in to avoid bloating non-users. | Low |

### T18 — Overlay Management & Deep Links

Low-effort, high-usability items gated on platform API availability or upstream pull.

| Item | Description | Effort | Dependency |
|------|-------------|--------|------------|
| **Per-App Overlay Management** | Pull upstream v4.0.1's "Overlays" tab in App Details: enable/disable per-app system overlays (root/ADB, Android 8+). Upstream landed it — pull rather than reimplement ([S39]). | Low (pull) | Upstream v4.0.1+ |
| **app-manager:// Deep Link** | Support `app-manager://details?id=<pkg>&user=<uid>` deep links; enables Quick Settings tiles, widgets, and Tasker actions to open specific app's detail page. Upstream ships this in v4.0.x. | Low (pull) | Upstream |
| **Unfreeze on Shortcut Launch** | If user taps a home-screen shortcut for a frozen app, offer to unfreeze temporarily; auto-re-freeze when screen is locked. Pull upstream v4.0.1 model ([S39]). Pairs with T8 "Saved Filter Presets" for freeze-group workflows. | Low (pull) | Upstream v4.0.1+ |
| ~~**Certificate Hash in App Detail**~~ ✅ 2026-05-02 | New "Sign · SHA-256 21:5F…38:6C" chip in the App Info tag cloud surfaces the colon-separated upper-case fingerprint with first/last-4 truncation. Tap opens a `MaterialAlertDialog` showing the full digest plus a one-tap **Copy** button so users can paste it straight into AppVerifier or compare against `apksigner verify --print-certs` output. Computed worker-side in `AppInfoViewModel.computeSigningCertSha256()` and exposed as `TagCloud.signingCertSha256`. Obtainium v1.3.0 / [S51] precedent. | Low | — |

### T19 — Package-Aware Storage Analysis

App-centric storage views informed by SD Maid SE's CorpseFinder ([S24]) and UAD-NG's description-search model ([S67]). NG will not replicate SD Maid SE's full storage engine — scope is limited to per-app storage panels and post-uninstall cleanup that naturally extends the existing App Details view. Items in this tier gate on storage access permissions or root.

| Item | Description | Effort | Dependency |
|------|-------------|--------|------------|
| **App Details Storage Panel** | In App Details, show APK size, split sizes, installed data, cache, OBB, external data, backup archive sizes, and orphan leftovers in a single collapsible panel. SD Maid SE CorpseFinder model ([S24]). | Medium | — |
| **Leftover Detection After Uninstall** | After uninstalling an app, scan for orphan directories (`Android/data/<pkg>`, `Android/obb/<pkg>`, root-accessible `/data/data/<pkg>` stubs) and offer one-tap cleanup. SD Maid SE CorpseFinder model ([S24]). | Medium | Root or `MANAGE_EXTERNAL_STORAGE` |
| **APK Duplicate Finder** | Find multiple copies of the same package/version APK across Downloads, backup dirs, and SD card by matching package name + signing cert + version code; flag in File Manager and backup list. SD Maid SE deduplicator model ([S42]). | Medium | File access |
| **Backup Duplicate Cleaner** | Detect the same package + version + variant backed up more than once across backup roots; offer to delete redundant copies. Extension of Backup Retention Policy (T6). | Low | T6 Retention Policy |

### T20 — Performance & Profiling (System-Level)

Advanced users and developers need visibility into app runtime behavior and system performance impact. Perfetto v54.0 and LeakCanary v3.0+ signal emerging OSS maturity in this space.

| Item | Description | Effort | Dependency |
|------|-------------|--------|------------|
| **Perfetto System Trace Export** | Integrate Perfetto v54.0 tracing infrastructure on Android 11+ to capture system-wide traces (CPU scheduling, I/O, frame pacing, memory allocations) filtered to a target app and its dependencies. Export trace as `.pbtxt` or Perfetto UI deep-link (https://ui.perfetto.dev). Expose as "Export trace" action in App Details (requires ADB or Shizuku); save to Downloads. Target power users and developers; non-expert users directed to device Settings > Developer options builtin Perfetto UI. Perfetto v54.0 ([S97]). | High | ADB/Shizuku or direct tracefs access |
| **LeakCanary Leak Detection Wrapper** | Optional dependency: when user enables "Memory profiling" in developer settings, attach LeakCanary v3.0+ as an agent to the target app's process to detect heap leaks during interactive testing. Surface detected leaks in a new "Leaks" tab in App Details with heap dump access. Non-intrusive; only activates if explicitly enabled. Requires debuggable app or root. LeakCanary v3.0-alpha series ([S96]); high-risk for stability; gate on opt-in developer mode. | High | LeakCanary binary, ADB/Shizuku, debuggable app |
| **CPU Profiler via simpleperf** | Expose simpleperf (LLVM-based CPU profiler, built into Android SDK) as a "Record CPU profile" action in App Details. Capture call stacks during a time window; export as flame graph SVG or callgrind format for analysis in desktop tools (Speedscope, kcachegrind). Gate on ADB/root/Shizuku availability. | High | simpleperf binary, ADB/Shizuku |
| **Memory Allocations Inspector** | Capture heap allocations for a target app (dumpsys meminfo, hwui memory, native memory stats) and surface in a new panel in App Details. Overlay native lib leaks (detected via ELF symbols and linker audit) onto the allocation breakdown. Signals memory pressure and leaks without requiring full Perfetto traces. | Medium | — |

### T21 — UI/Design Polish & Premium Feel

NG is feature-rich but visually dated compared to premium OSS exemplars (Auxio, Now in Android, ProtonMail, Mullvad). Modernizing the visual language and interaction patterns without bloating the codebase is a focus. Material 3 Adaptive is table-stakes for 2026 Android apps.

| Item | Description | Effort | Dependency |
|------|-------------|--------|------------|
| **Material 3 Adaptive Layouts for Tablets** | On Android 12+ (API 31+), use `foldable` and `posture` APIs + Material 3 Adaptive to automatically switch to two-column layouts on tablets, foldables, and landscape mode. Apply to: App List (detail sidebar), Settings (detail pane), Backup/Restore (list + log), Finder results. Reference: Mullvad VPN v2026.4 ([S110]) and Now in Android v0.1.1 ([S108]). Upstream-first pull when available; NG implements if not. | High | Material 3 Adaptive library (androidx.compose.material3.adaptive) |
| **Fast List Rendering for 10k+ Apps** | Audit and optimize app list rendering for devices with large installed app counts (corporate, developer devices can exceed 5k–10k apps). Implement virtualization + incremental search filtering. Reference Auxio v4.0.10's "Musikr" library scan speed ([S109]): native tag parsing + on-device caching reduces initial load from minutes to seconds. Apply same principles to APK metadata parsing. | High | LazyColumn optimization + caching strategy |
| **Landscape-Aware Density Overrides** | On landscape orientation or narrow screens, dynamically adjust list item density (compact vs. normal) and action bar layout. App card can hide less-critical metadata (version code, minSdk) in ultra-compact mode; surface as expandable chip on tap. Maintains usability on small screens and tablets. | Medium | Compose state management |
| **Unread/Attention Badges** | In the app list, add small circular badge counters to apps with pending operations: e.g. "3" badge if app has 3 disabled services, or "!" badge if app needs permission grant. Reference: Now in Android v0.1.1 ([S108]) unread badge pattern. Provides at-a-glance actionability. | Low | Compose Badge component |
| **Undo/SnackBar for Destructive Ops** | When user disables, uninstalls, or clears data for an app, show a SnackBar with "Undo" action for 2–5 seconds before commit. Reduces accidental data loss anxiety. Reference: Now in Android ([S108]) undo bookmark removal. Requires transactional batching of operations. | Medium | Transactional operation model |
| **Discreet/Generic App Icon Mode** | In Settings > Appearance, add option to use a generic system-colored icon (rounded square, NG logo minimal) instead of the distinct AppManagerNG icon. Useful for privacy-conscious users who want the app to blend in on home screen. Reference: ProtonMail v7.9.5 discreet icon mode ([S111]). Low-effort: ship 2–3 icon variants + selection spinner. | Low | Asset variants |
| **In-App Language Selector** | On Android 13+, add Settings > Localization > "App Language" selector to override system locale *per-app* only. Allows user to keep system in English but use NG in their preferred language without system-wide impact. Reference: Mullvad VPN v2026.4 ([S110]). Requires `LocaleCompat.setLocales()` (API 33+) + graceful fallback. | Medium | androidx.appcompat LocaleCompat |
| **Custom Actions & Batch Operations** | Allow power users to define custom shell commands/Tasker actions that run on a selected set of apps. E.g. "Uninstall + Clear Cache for all Google apps" one tap. Reference: Droid-ify v0.7.1 custom actions ([S104]). Requires a safe script-sandboxing model + audit logging. Non-trivial security review needed. | High | Script sandbox + Tasker IPC |
| **Improved Dark Mode + Material You Integration** | Ensure dark mode respects device's Material You color palette (accent, primary, tertiary colors from wallpaper). NG already targets dark by default; audit Compose theme setup to ensure `dynamicColor = true` and color harmonization. Reference: Auxio v4.0.10 Material Design refresh ([S109]). | Low | Audit + theme sync |
| **Smooth Page Transitions & Animations** | Add predictable, spring-eased transitions between app detail, settings, and search results pages. Reduce motion for accessibility. Polish gesture responses (long-press, swipe-to-dismiss) with haptic feedback. Reference: Now in Android v0.1.1 ([S108]) predictive back animation support. Compose animation APIs already handle this elegantly. | Medium | Compose Animation, haptic feedback |
| **Material 3 Expressive Migration** | Adopt Material 3 Expressive (the next iteration of Material You) for shape system, motion choreography, and component variants (FilledTonal buttons, ExtraLarge corner radii, expressive typography). Reference: PodAura v3.2 ([S118]) shipping "More Material 3 Expressive" as a tracked feature. Migrate incrementally to avoid disruptive UI churn. Pin Compose BOM to a version that ships expressive variants. | Medium | Compose Material3 expressive components |
| **Compose Performance: Stable VM States** | Audit ViewModel state holders to ensure all exposed types are `compose-compiler-stable` (annotated `@Stable` / `@Immutable`); avoid `MutableList` / non-data classes in state. Reduces unnecessary recompositions in app list and detail screens. Reference: Neo Backup v8.3.17 ([S114]) "Make VM-states compose-compiler-stable" PR series. Measurable perf win for 5k+ app devices. | Medium | Compose compiler metrics, baseline profiles |
| **Launcher Shortcuts (Pinned Schedules)** | Allow users to pin a frequent NG schedule (e.g. "Backup all user apps", "Force-stop top 10 most-used") to home screen as a shortcut. Tap = run schedule directly. Reference: Neo Backup v8.3.17 ([S114]) "Launcher shortcuts support for running schedules". Uses `ShortcutManager` API; supports Android 7.1+. | Low | ShortcutManager, schedule export model |
| ~~**Backup Sharing Button**~~ ✅ 2026-05-16 | Restore-Single popup menu now exposes "Share backup" via `SharableItems` → `ACTION_SEND_MULTIPLE` + `FmProvider`. Closed in lockstep with the iter-21 "Neo-Backup-Style Backup Sharing Button" row. Reference: Neo Backup v8.3.17 ([S114]). | Low | StorageAccessFramework |
| **Material 3 Result Notifications** | When a long-running operation (backup, batch op, install) completes, show what was done in the result notification using `Notification.ProgressStyle` (Android 16+) or expanded MessagingStyle. Reference: SD Maid SE v1.7.x ([S112]) "Show what was cleaned in result notifications". Bridges work-completion to user awareness without opening the app. | Low | Notification.ProgressStyle (covered T5 row) |



Items that need architectural decisions or external dependencies resolved before scheduling.

| Item | Blocker / Open Question |
|------|------------------------|
| **Shizuku as *default* privilege path** | AppManagerNG currently uses libadb+libsu. Adding Shizuku is a third path that must be maintained across Shizuku API changes (currently v13.6.0, Android 16 QPR1 supported [S22]). The community demand is unambiguous (31 reactions [S02]). Decision: integrate in T5 (Next) as an optional path, not mandatory. |
| **CVE Cross-Reference for Installed Apps** | Requires a reliable, privacy-respecting data source (NVD API, OSV.dev). Any network call needs explicit user consent. Limited MVP scoped to querying OSV.dev for known `packageName`+`versionCode` pairs. Track OSV Android ecosystem feed. |
| **Tasker / MacroDroid Plugin API** | Requires a stable plugin contract. Upstream does not expose a Tasker plugin. Scope: expose AM profile execution and batch ops as Tasker/Locale conditions/actions via `com.twofortyfouram:android-plugin-api`. Revisit after Routine Operations (T8) is stable. |
| **Backup Extras (SMS/Wi-Fi/Calls)** | ContentProvider approach works for standard Android. Fragile across OEM ROMs. Issue #568 asks for this ([S34]). Scoped to AOSP standard ContentProviders only; custom ROM extensions explicitly out. |
| **App "Health Score" Aggregate** | Opinionated composite risk score. Risk: obscures the raw data that AM's power users rely on. Alternative: surfacing existing scores (tracker count, dangerous permissions) more prominently in app list card without collapsing them into a single number. |
| **APK Transparency Log Verification** | Google's APK key attestation transparency requires network + Android 14+ APIs. Low priority for minSdk 21. Revisit when minSdk raises to 26 or higher. |
| **F-Droid / Aurora Store In-App Update** | Issue #464 ([S08], 12 reactions). Deep-link to app's store listing rather than replicate a store. Obtainium ([S26]) solves the full update tracking problem better. Decision: implement deep-link badges (store button on app detail page); reject full in-app store. |
| **Storage Trend Tracking** | Track per-app storage usage over time and display a trend graph. SD Maid SE v1.6.5 model ([S42]). Requires persistent historical data (~1 row/day/app). Non-trivial storage overhead for large app counts; may fit a future "Storage Analyzer" view that's opt-in. |
| ~~**android-debloat-list Safety Ratings in App Detail**~~ | ~~Surface UAD-NG removal rating ("safe", "unsafe", "expert", "untested") directly in the App Details page for system apps.~~ **Promoted to T7 (Next).** AM v4.0.5 shipped "display unsafe bloatware info" in its debloater ([S01]), establishing upstream precedent. Low-effort bundle path confirmed viable. |
| **KernelSU as Third Root Provider** | ~~Promoted to T5 (Next).~~ KernelSU v3.2.3+ `adb root` pathway has sufficient signal (~10 M users, `libsu`-compatible `su` binary) to warrant explicit detection in the capability wizard. See T5 "KernelSU Detection in Capability Wizard" ([S56]). |
| **Android 17 Advanced Protection Mode (AAPM) Integration** | `AdvancedProtectionManager` API (Android 17, API 37 [S53]) lets apps detect if the user has opted into AAPM. NG could surface "Device in Advanced Protection Mode" in the Privacy & Security section and automatically restrict high-risk operations (e.g. USB-debugging auto-pair, unknown-source installs) when active. Gated on minSdk raise to 37. |
| **JADX Plugin API for Analysis Extensions** | JADX 1.5.0+ ships an external plugin ecosystem ([S60]); scripts are now loadable as ZIP artifacts. Could expose custom class detectors (e.g. tracker pattern matching, hardcoded-secret detection) as user-installable NG analysis plugins. Architecture decisions: plugin trust model, sandboxing, GPL-compatible licensing chain. |
| **DDG Tracker Radar as Secondary Tracker Source** | TrackerControl 2026040301 ([S69]) integrates DuckDuckGo Tracker Radar (mobile-specific tracker database) alongside Exodus definitions for broader coverage. DDG Tracker Radar data is CC-BY-NC-SA — the Non-Commercial clause is incompatible with GPL-3.0 redistribution without explicit licensing. **Do not bundle DDG Tracker Radar data until legal clearance is obtained.** Alternative: contribute new SDK signatures upstream to the Exodus database (MIT-licensed), which NG already ships. |
| **Hardware Key Attestation Panel** | Surface KeyMint version, attestation level (software / TEE / StrongBox), and full certificate chain in an App Details or Device Security panel. Requires Shizuku for device-unique-ID attestation; falls back to software attestation without elevated access. KeyAttestation v1.8.4 model ([S73]). Gated on Shizuku T5 integration; architectural decision on placement (per-app vs. device-level) unresolved. |
| **Non-Root APK Module Injection Framework** | Architectural exploration: model a non-root Xposed-like framework (e.g. LSPatch v0.6 or Magisk modules) to selectively inject code into managed apps *without root* — e.g. to hook `PackageManager.getInstalledPackages()` for advanced filtering, or to intercept `Activity.onCreate()` for custom UI overlays. Would require NG to ship a companion Zygisk module or implement an embedded LSPosed core. Significant risk: module crashes can cascade; SELinux denies are non-trivial to debug. **Blocked on architecture consensus and team capacity.** LSPatch v0.6 (archived) was a reference; ZygiskNext v1.3.4 shows current non-root innovation ([S92], [S77]). |
| **Android 15 App Archiving Support** | `PackageInfo.isArchived` (API 35) flags apps in the platform's "archived" state — APK removed, icon + user data preserved, auto-restores on launch. `ArchivedPackageInfo` holds minimal metadata for archived packages ([S82]). NG could surface "Archived" as a distinct app state in the main list (badge alongside installed/disabled/frozen), add Archive and Unarchive actions in App Details, and include archived apps in Finder results. No root required — any `REQUEST_INSTALL_PACKAGES` holder can archive. Blocked on API 35+ conditional activation; NG's minSdk 21 means the feature targets only Android 15+ devices. |
| **Frida-Based Runtime Hook Tracing** | Architectural exploration: integrate Frida v17.9.4+ as an optional advanced-user feature to dynamically hook and trace app runtime behavior (method calls, return values, SELinux denials, system service interactions) without static analysis. Frida Server can run on rooted or ADB-accessible devices; NG could expose a "Record trace" action in App Details that spawns a Frida script targeting the selected package and streams the hook output to an in-app log viewer. High-risk: Frida gadgets and scripts require careful sandboxing and versioning; incompatible Frida client/server versions can crash. **Blocked on architecture consensus, security review, and team capacity.** Frida v17.9.4 ([S93]) and JADX v1.5.5 decompiler integration would unlock post-trace source-to-trace mapping workflows. |

---

## Iter-18 Research Additions (2026-05-03)

Cross-cutting research deltas mined from KernelSU v3.2, Magisk v30.7, Shizuku v13.6.0, Termux v0.118.3, Apktool v3.0.2, JADX v1.5.5, APKEditor v1.4.7-8, Hail v1.10.0, Inure build107, Material Files v1.7.4, SD Maid SE v1.7.2-rc0, UAD-NG v1.2.0, Neo Backup 8.3.17, and Android 16/17 platform docs. Each row carries the home-theme tag in brackets and tier verdict; Now/Next/Later items will be migrated into their themes during normal merging — this section is the working ledger that the next code-bearing iteration draws from.

| Item | Description | Theme | Tier |
|------|-------------|-------|------|
| ~~**Shizuku Trusted-WLAN Auto-Start Awareness**~~ ✅ shipped 2026-05-17 | `ShizukuBridge` now gates the trusted-WLAN auto-start affordance on Android 13+ plus Shizuku Manager `>=13.6.0` and only offers it when Shizuku is installed but the binder is stopped. Settings -> Operating Mode and the onboarding Shizuku card both show a "Configure auto-start in Shizuku" button in that state, so rootless users can recover after reboot without hunting through Shizuku manually. The intent tries the roadmap-tracked `moe.shizuku.privileged.api/.AUTO_START` component first and gracefully falls back to Shizuku's launcher or Android app-info screen because the v13.6.0 manifest does not expose a stable exported auto-start activity in every build. References: [S121], [S331]. | T5 | **Now** |
| **Magisk `--drop-cap` Opt-In Surface** | Magisk v30.7 inverted `--drop-cap` semantics — capability dropping is now opt-in per-app, not opt-out ([S122]). NG's Privilege Health-Check screen should detect Magisk version, parse `magiskpolicy --live` output, and report whether the AppManagerNG process itself is running with the full or dropped capability set. Effort 2/5; ties into existing `Os.prctl(PR_CAPBSET_READ)` plumbing. | T9 | **Next** |
| **KernelSU Sulog & Seccomp Status** | KernelSU v3.2 ships `sulog` (kernel-side su event log) and a per-app seccomp filter status surface ([S123]). When NG detects KernelSU as the root provider, Privilege Health-Check should pull sulog tail (`/data/adb/ksu/log/sulog`) and the seccomp filter mode (`prctl PR_GET_SECCOMP`), display recent denials, and link out to KernelSU Manager. Effort 3/5. | T9 | **Next** |
| **Android 16 `SDK_INT_FULL` Plumbing Audit** | Android 16 introduced `Build.VERSION.SDK_INT_FULL` and `Build.getMinorSdkVersion()` so apps can branch on Q/Q-prime minor releases ([S124]). NG has ~120 `Build.VERSION.SDK_INT >=` call-sites; a single utility (`AndroidUtils.sdkAtLeast(int major, int minor)`) should centralize comparisons and let new code target Android 16.1/16.2 quirks without bloating call-sites. Effort 2/5; pure refactor, zero behavior change at v0 minor. | T2 | **Next** |
| ~~**JobScheduler Quota Stop-Reason Surfacing**~~ ⏸ parked 2026-05-17 | Android 16 tightened JobScheduler quotas; `WorkInfo.getStopReason()` and `JobScheduler#getPendingJobReasonsHistory()` expose why a scheduled run was deferred or killed ([S124], [S125]). **2026-05-17 audit:** repo/Gradle sweep still found zero `androidx.work`, `WorkManager`, `JobScheduler`, `JobService`, or Schedules-screen implementation in current NG source; the only local hit is the manual backup "keep device awake" warning. Implementing this now would add dead code/UI. The requirement is folded into T6 **Scheduled Auto-Backup**: when that scheduler lands, schedule history/result notifications must surface quota/device-state/preempt/constraint reasons instead of silently retrying. | T6 | **Blocked by Scheduled Auto-Backup** |
| **Termux SELinux Context Display** | Termux v0.118.3 surfaces `SE_PROCESS_CONTEXT`, `SE_FILE_CONTEXT`, and `SE_INFO` in its environment ([S126]). NG's App Info "Security" section should display the same trio per-app — sourced via `am.getRunningAppProcesses()` cross-checked with `/proc/<pid>/attr/current` — so power users can audit SELinux domain assignments alongside permissions and AppOps. Effort 3/5. | T9 | **Later** |
| ~~**Termux 16k Page-Size Device Indicator**~~ ✅ shipped 2026-05-02 | Termux v0.118.3 detects 16k page-size devices via `getconf PAGESIZE` and adjusts mmap/exec strategy ([S126]). NG already has a per-lib 16KB-alignment indicator (iter-11) but lacks a *device-side* indicator. Add a one-line "Device page size: 4 KB / 16 KB" row in App Info → Native libs section so 16k-incompatible libs visibly explain the warning instead of looking spurious on 4k devices. Shipped: new "Device page size" row in App Info under Primary ABI, populated via `Os.sysconf(_SC_PAGESIZE)` with explicit 4 KB / 16 KB / Other / Unknown labels. | T19 | **Now** |
| **APKEditor `--smali-comment-level basic`** | APKEditor v1.4.7 added a smali comment-level option (`none`, `basic`, `verbose`) and a top-level `-remove-annotation` flag for stripping `@Nullable`/`@NotNull`/`@RequiresApi` annotations during decode ([S127]). NG's APK Editor flow should expose both as user-toggleable options; "basic" is the new sane default for casual users (smaller diffs, faster recompile) and `--remove-annotation` materially reduces noise when comparing decoded smali across builds. Effort 2/5. | T12 | **Next** |
| ~~**Apktool 3.0.2 "Remastered" Migration**~~ ⏸ parked 2026-05-17 | Apktool 3.0.2 shipped on 2026-04-19 with CVE-2026-39973 path-name hardening, split-library decode support, and ARSC/disassembly performance improvements ([S332]). **2026-05-17 audit:** NG has no `org.apktool`, `apktool-lib`, or `brut.apktool` dependency/call site to migrate, and `versions.gradle` pins only ARSCLib, apksig, Google smali/baksmali `3.0.9`, and JADX for APK editing. Maven Central confirms `org.apktool:apktool-lib:3.0.2` pulls the iBotPeaches smali fork plus Guava/commons runtime deps ([S333]); adding it without an Apktool-backed decode/rebuild backend would add unused weight and risk a duplicate smali classpath. Keep Apktool 3.0.2 as the target for the future T12 backend, not as a standalone Gradle pin. | Eng-Debt | **Blocked by T12 Apktool backend** |
| **JADX 1.5.5 `.apks` Ingestion + UI Zoom** | JADX v1.5.5 added `.apks` (split APK) ingestion via `--input-apks` and a UI zoom factor option ([S129]). NG's planned JADX integration (T12 iter-15) should pass split APK paths through the new `.apks` entry-point instead of stitching APK Editor's merged-APK output. Effort 1/5 once T12 JADX work begins. | T12 | **Later** |
| **JADX 1.5.5 FlatLaf CJK Composite Font** | JADX v1.5.5 switched to FlatLaf composite-font fallback so CJK glyphs render correctly without the Noto-CJK system pack ([S129]). When NG ships an embedded JADX viewer, set the same FlatLaf composite-font UIManager defaults so Chinese/Japanese/Korean class names render in the decompile pane. Effort 1/5. | T10 | **Later** |
| ~~**Hail-Style Auto-Freeze QuickSettings Tile**~~ ✅ 2026-05-17 | Shipped `QuickFreezeTileService` as an exported platform `TileService` with `android.permission.BIND_QUICK_SETTINGS_TILE`, `QS_TILE` intent-filter, and the existing snowflake icon. Profiles that have the `freeze` misc action enabled now show "Use for Quick Settings freeze tile" / clear actions in the profile-list popup; the selected profile ID is stored in app prefs and the tile shows unavailable until one is configured. On tap, the tile uses `unlockAndRun()` when invoked from the lock screen, loads the selected profile, and starts `ProfileApplierService` with `BaseProfile.STATE_ON` so the existing profile/freeze/progress/history path does the work. JVM coverage: `QuickFreezeTileControllerTest`. References: Hail v1.10.0 auto-freeze tile inspiration ([S130]) and Android TileService manifest / lock-screen guidance ([S334], [S335]). | T8 | **Shipped** |
| **Hail-Style Digital-Assistant Launch** | Hail v1.10.0 registered as a Digital Assistant target so long-press home / power gesture launches Hail's freeze flow ([S130]). NG could ship a dedicated `android.intent.action.ASSIST` activity that surfaces a fast-action sheet (Force Stop, Freeze, Open App Info) for whatever app is currently in the foreground when the assistant is invoked. Effort 3/5; novel. | T8 | **Later** |
| **KernelSU App Profile Awareness** | Hail v1.10.0 reads KernelSU's per-app "App Profile" (uid, gid, capabilities, SELinux domain) and respects it ([S130]). NG's Privilege Health-Check should also display the active App Profile when launched under KernelSU and warn when capabilities have been reduced below NG's expected set. Effort 2/5; pairs with [S123]. | T9 | **Next** |
| ~~**Inure-Style Native-Lib Size Surface**~~ ✅ shipped 2026-05-03 | Inure build107 displays per-`.so` sizes in App Info → Native libs ([S131]). Verified already shipped in NG: `NativeLibraries.NativeLib.toLocalizedString()` (both ELF + InvalidLib variants) prepends `Formatter.formatFileSize(context, getSize())` to the description rendered in `AppDetailsOtherFragment` line 330. No code change required — auditing closed the row. | T19 | **Now** |
| ~~**Inure-Style AppOps IGNORE Flag**~~ ✅ 2026-05-08 | Row-tap on an AppOps entry in App Details now cycles **ALLOWED → IGNORED → ERRORED → ALLOWED** instead of binary toggling, with a Toast naming the new mode after each tap. The IGNORE state silently no-ops the op without throwing `SecurityException`, matching platform behavior and avoiding the crashes that DENY (`MODE_ERRORED`) triggers on apps that don't expect a hard rejection. New `nextAppOpModeInCycle()` helper in [`AppDetailsPermissionsFragment.java`](app/src/main/java/io/github/muntashirakon/AppManager/details/AppDetailsPermissionsFragment.java); long-press still opens the full single-choice mode picker (FOREGROUND/DEFAULT/etc.) for advanced users. Reference: [S131]. | T9 | **Now** |
| **Inure-Style Batch APK Installer** | Inure build107.0.0 added a Batch Installer that consumes multiple APK paths in one operation ([S131]). NG already has a Batch Operations menu but split-APK / multi-APK install requires per-package interaction; a single "Install all selected" action that streams `Session.commit()` calls would close this gap. Effort 2/5. | T11 | **Next** |
| ~~**Inure-Style Removed Persistent Search History**~~ ✅ already-compliant 2026-05-02 | Inure build107.0.1 removed persistent search-term storage as a privacy win ([S131]). NG's main app list and Finder both persist recent searches indefinitely; either move to a session-only in-memory cache or expose a "Remember search history" preference (default off) to match Inure's posture. Audit result: NG's `SearchView` usage is session-only in memory — `recent_search`/`searchHistory`/`SearchRecentSuggestionsProvider` grep all return zero hits. No persistent storage exists; nothing to remove. | T9 | **Now** |
| **Material Files Checksum Properties Tab** | Material Files v1.7.2 added a Checksum properties tab (MD5/SHA-1/SHA-256/SHA-512) inside the per-file properties dialog ([S132]). NG's APK Info already shows SHA-256 of the APK; broaden the same checksum-row display to NG's File Manager Properties dialog for arbitrary files (not just APKs). Effort 1/5. | T13 | **Next** |
| **Material Files User-Trust for Self-Signed WebDAV Certs** | Material Files v1.7.2 marks user-installed certs in the Android system credential store as trusted for WebDAV connections with self-signed certificates ([S132]). NG's planned WebDAV/SMB backup destination (T6 iter-12) should consume `KeyChain.getCertificateChain()` for user-installed certs by default rather than rolling its own trust store, matching Material Files' UX. Effort 2/5. | T6 | **Next** |
| **SD-Maid-Style Auto-Fix Battery Optimization** | SD Maid SE v1.7.2-rc0 auto-fixes battery optimization for the scheduler via root or ADB ([S133]). NG now silently whitelists the app via `DeviceIdleManagerCompat.disableBatteryOptimization()` when user has `DEVICE_POWER` permission instead of opening system dialog; UX matches SD-Maid. | T20 | ~~**Now**~~ ✅ **shipped 2026-05-02** |
| **SD-Maid-Style Warn-Before-Volume-Scan** | SD Maid SE v1.7.2-rc0 added a warning before scanning an entire storage volume ([S133]). NG's Storage Analysis (T19) and File Manager search both happily walk the entire `/storage/emulated/0` tree without warning; a "This will take ~N minutes" pre-flight using `StorageStatsManager.getTotalBytes()` over device speed would prevent surprise UI freezes. Effort 1/5. | T19 | **Next** |
| ~~**UAD-Style Auto-Fetch Debloat Definitions**~~ ✅ 2026-05-17 | Closed by iter-32 with the main T5 debloat-definition updater row: default-off Settings → Privacy opt-in, existing Internet gate, pinned raw-GitHub manifest, SHA-256 + byte-length verification, schema-smoke-test, app-private cache, and bundled-snapshot fallback. | T7 | ~~**Next**~~ Shipped |
| ~~**UAD-Style Cross-User Package State Detection**~~ ✅ 2026-05-17 | Closed by iter-31 with the main T5 cross-user state row: `ApplicationItem` now preserves enabled/disabled/uninstalled user-id buckets, the main list/picker surfaces split state, and Finder loads all selected users with visible user/state metadata. | T7 | ~~**Next**~~ Shipped |
| ~~**UAD-Style Selectable & Copyable Descriptions**~~ ✅ shipped 2026-05-03 | UAD-NG v1.2.0 made every package description in its UI selectable and copyable ([S134]). Shipped: `item_app_details_appop.xml` and `item_app_details_perm.xml` mark `perm_description`, `perm_protection_level`, `op_mode_running_duration`, `op_accept_reject_time` as `textIsSelectable="true"`. Long-press now reveals the system Copy affordance directly on the list rows. | T10 | **Now** |
| ~~**UAD-Style Copy-Error Button on Failure Toasts**~~ ✅ shipped 2026-05-03 (foundation) | UAD-NG v1.2.0 added a one-tap "Copy error" button on every error toast ([S134]). Shipped: new `UIUtils.displayCopyableErrorDialog(context, title, message)` helper backed by `ClipboardUtils` (already handles >1MB error blobs via FileProvider URI fallback). Foundation only; high-traffic toast failure sites migrate in a follow-up commit. | T4 | **Now** |
| ~~**Neo-Backup-Style Backup Sharing Button**~~ ✅ 2026-05-16 | Implemented in [`RestoreSingleFragment`](app/src/main/java/io/github/muntashirakon/AppManager/backup/dialog/RestoreSingleFragment.java) — "Share backup" entry in the popup menu (enabled when one backup is selected) walks the backup directory, filters for files, and hands the list to `SharableItems`. The resulting chooser intent carries `ACTION_SEND_MULTIPLE` + ClipData + `FLAG_GRANT_READ_URI_PERMISSION` so Android 18+ targets receive the FmProvider URIs correctly. Encrypted backups remain encrypted on the way out — the recipient still needs the user's AppManagerNG encryption key to restore. Closes the iter-18 "Backup Sharing Button" row in the same pass. Reference: [S135]. | T6 | **Shipped** |
| ~~**Neo-Backup-Style Existing-Tag Suggestions**~~ ✅ 2026-05-08 | Re-scoped: NG's multi-tag system (iter-19 plan) hasn't shipped yet, so the *applicable* user-facing surface today is the **backup-name** dialog in the multiple-backup flow. Replaced [`BackupFragment.handleBackup()`](app/src/main/java/io/github/muntashirakon/AppManager/backup/dialog/BackupFragment.java)'s `TextInputDialogBuilder` with `TextInputDropdownDialogBuilder`; new `collectExistingBackupNames()` walks `viewModel.getBackupInfoList()` → per-backup `BackupMetadataV5.metadata.backupName` and feeds the de-duplicated `LinkedHashSet` into `setDropdownItems()`. Users now get an autocomplete dropdown of every prior backup label across the apps in scope, so labelling a fresh backup with the same tag as last time is one tap instead of full retype. When the multi-tag dao lands, the same `setDropdownItems` adapter pattern can be reused for the tag-add dialog with zero code change. Reference: [S135]. | T8 | **Now** |
| ~~**Neo-Backup-Style KeepAndroidOpen Banner**~~ ✅ shipped 2026-05-02 | Neo Backup 8.3.17 surfaces a "Keep Android open during long backups" banner when a backup operation begins on Android 14+ to combat aggressive Doze/standby kills ([S135]). NG's backup operation should display the same banner under Android 14+ given the new JobScheduler quota tightening. Shipped: long Toast warning fired from `BackupRestoreDialogFragment.startOperation()` whenever `MODE_BACKUP` runs on `SDK_INT >= UPSIDE_DOWN_CAKE` (API 34+). | T6 | **Now** |
| **Glance Widget Parity Audit** | androidx.glance enables Compose-style home-screen widgets ([S136]). NG ships zero home-screen widgets today; an exploration pass to evaluate a 1×1 "Last backup status", 2×1 "Foreground app freeze toggle", and 4×1 "Schedule next-run" widget set would unlock a use-case category competitors (Hail, Neo Backup) already cover. Effort 3/5; UC. | T8 | **Under Consideration** |
| ~~**Repo-Rename Detection for Upstream Pin**~~ ✅ 2026-05-09 | New CI workflow at [`.github/workflows/upstream-rename-watch.yml`](.github/workflows/upstream-rename-watch.yml) hits the GitHub API on a weekly cadence (Wednesday 09:27 UTC, staggered off CodeQL + dependency-scan) plus `workflow_dispatch`, asserts that `MuntashirAkon/AppManager` still resolves to the same `full_name`, and on drift auto-opens an `upstream-sync`/`eng-debt`-labelled issue with a 7-step rename audit checklist (workflow EXPECTED_SLUG, README, ROADMAP, CLAUDE.md, CHANGELOG historical refs, submodule URLs, Obtainium config, Sphinx docs). Idempotent — never opens a duplicate issue for the same drift. Reference: [S121]. | Eng-Debt | **Next** |

---

## Iter-19 Research Additions (2026-05-06)

Three-day delta from iter-18: GitHub-issue / Reddit pain-point mining of MuntashirAkon/AppManager, samolego/Canta, aistra0528/Hail, NeoApplications/Neo-Backup, d4rken-org/sdmaid-se, Universal-Debloater-Alliance/UAD-NG; Shizuku-era competitor sweep across `timschneeb/awesome-shizuku` (Blocker, ShizuWall, de1984, OwnDroid, InstallerX-Revived, PI, Accrescent, Amarok-Hider, UpgradeAll, Language-Selector, debuggable-app-data-backup, KernelSU v3.2.3/4); Android 17 Beta 4 + Android 16 QPR2 platform behavior changes; F-Droid 2.0 index v2 + ROM JSON pre-seeding format; Google Play Contacts/Location-button policy enforcement (~2026-05-15). Most upstream issues surface user-impact gaps that the iter-18 themes name in principle but don't yet scope concretely; iter-19 turns them into actionable rows.

| Item | Description | Theme | Tier |
|------|-------------|-------|------|
| ~~**Hidden-API Compatibility Harness**~~ ✅ 2026-05-17 | Shipped the first structural harness for NG's hidden-API migration cliff. `scripts/generate-hidden-api-baseline.ps1` parses the `hiddenapi/src/main/java` stub tree and writes `app/src/androidTest/assets/api/api-versions-appmanagerng-hiddenapi.json` (128 class descriptors / 1,120 runtime members in the current baseline). `HiddenApiDescriptorBaselineTest` keeps the checked-in baseline aligned with every hiddenapi source file, and `HiddenApiCompatibilityInstrumentedTest` loads that asset on-device, applies HiddenApiBypass exemptions, reflectively probes active-SDK hidden classes/methods/fields, writes `hidden-api-compat-sdk<api>.json`, and fails on required missing APIs while downgrading deprecated removals to report warnings. This replaces the stale roadmap assumption that api-version JSON assets already existed under main assets. Reference: [S137]. | Eng-Debt | **Shipped** |
| ~~**GCM Cipher Reuse on Large OBB Backup**~~ ✅ 2026-05-16 | New metadata-v6 encrypted backups keep the existing archive-level IV in `info_v5.am.json` but derive a deterministic per-file 96-bit GCM IV from the backup IV + canonical backup filename, then initialize a fresh `GCMBlockCipher` for each file. Legacy v5-and-older backups retain their historical behavior for restore compatibility. Regression coverage now checks IV stability, per-file separation, and filename canonicalization in [`AESCryptoTest`](app/src/test/java/io/github/muntashirakon/AppManager/crypto/AESCryptoTest.java). Reference: AM #1958 / [S138]. | T6 | **Shipped** |
| ~~**Shizuku Permission Auto-Revoke Warning**~~ ✅ 2026-05-17 | Direct App Info clear-data confirmations now append Shizuku-specific warnings for AppManagerNG, Shizuku Manager, and packages that declare `rikka.shizuku.ShizukuProvider`; the main-list batch clear-data dialog warns when any selected package matches the same risk set. Privileged direct clears capture AppManagerNG's Shizuku permission state before the operation, re-probe via `Shizuku.checkSelfPermission()` after success, and show a Settings -> Mode of operation re-auth deep-link if the grant vanished. Batch/profile clear-data records the same revoked-permission signal in the operation log because there is no foreground dialog surface. Reference: Canta #359 / [S139]. | T5 | **Shipped** |
| **Hidden-Shizuku Fork Detection** | The `thedjchi/Shizuku` fork ("Hidden-Shizuku") obfuscates the package name to bypass Play Integrity / SafetyNet checks, breaking NG's `Shizuku.isPreV11()` / `isShizukuAvailable()` probes which assume the canonical `moe.shizuku.privileged.api` package ([S139], [S152]). Switch the probe to enumerate every package that exports `moe.shizuku.api.SHIZUKU_SERVICE` permission via `PackageManager.getPermissionInfo()` rather than hard-coding the package name. Effort 2/5; broadens compatibility without compromising trust. | T5 | **Next** |
| **OEM Debloat-Blocker Bypass** | OPlus (OnePlus/Realme/Oppo) ships `OplusForbidUninstallAppManager` that re-installs flagged packages within minutes of removal; Samsung One UI 8.5 SmartSuggestions has hidden functional dependencies that bootloop Mobile Networks; MIUI's `com.miui.core` throws `SecurityException` on `pm uninstall --user 0` for protected packages ([S151], [S154]). Debloat Presets must add per-OEM allow/block matrices keyed off `Build.MANUFACTURER` + `Build.VERSION.INCREMENTAL` and a "Disable instead of uninstall" fallback when OEM enforcement is detected. Effort 4/5; resolves the #1 debloat-failure class on Chinese-market devices. | T7 | **Next** |
| **Per-App Rollback / "Revert All Changes"** | Upstream #1959 asked for an "undo" surface that reverts every NG-applied change to an app (permissions, AppOps, components, freeze state, network rules) without restoring user data ([S141]). NG's iter-18 Operation Activity Log captures the audit trail; building a per-app inverse-replay against that log delivers rollback for free. Effort 3/5; pairs naturally with the freeze audit-log work in [S144]. | T8 | **Next** |
| **Backup Path Exclusion Patterns** | Neo-Backup #991 surfaced repeated requests for glob-style exclusion patterns inside backup archives — `**/cache/**`, `**/.thumbnails/**`, custom user globs — to skip ephemeral data and shrink backup size by 40-80% on media-heavy apps ([S142]). NG's `BackupFlags` has the binary toggles but no path filters. Add a per-app and per-profile exclusion list using `PathMatcher`; default-on globs for known-throwaway dirs. Effort 2/5. | T6 | **Next** |
| **Tasker Parameterized Intent API** | Upstream #1968 requested Tasker-friendly parameterized intents — currently NG's URI handler accepts `appmanager://run-profile/<id>` but no per-action parameters (target package, override flags, dry-run) ([S143]). Extend the existing URI/Intent surface with documented `EXTRA_PACKAGE`, `EXTRA_DRY_RUN`, `EXTRA_PROFILE_OVERRIDES` so Tasker, MacroDroid, and shortcut launchers can drive NG without bespoke plugins. Cross-refs iter-18 Hail Uri-API row. Effort 2/5. | T8 | **Next** |
| ~~**Freeze / Operation Audit Log**~~ ✅ 2026-05-17 | Closed as an existing `OpHistoryActivity` completion plus the missing Settings/row-action links. NG's current operation history is Room-backed, searchable/filterable/sortable by status/risk/type/mode/reversible/date, exports JSON/CSV/text, opens single targets, re-runs replayable batch/profile/installer entries through preflight, and records rollback guidance with reversible metadata for batch freeze/unfreeze, tracker, and component operations. This pass added Settings -> Privacy -> History and a per-row "Recovery guidance" action for reversible entries. Automatic inverse replay remains in the separate Per-App Rollback row. References: [S144], [S145], [S231], [S232]. | T8 | **Shipped** |
| **Settings Import/Export Portability** | Hail #390 + Canta + multiple SD-Maid threads all want a single-file export of every app preference / freeze list / debloat ruleset / schedule for use after factory reset or ROM flash ([S146]). NG's Routine Operations exports profiles but not the global app config. Add a one-tap "Export all settings" producing an encrypted JSON bundle (same crypto as backup), and an "Import" picker that diff-merges into existing prefs. Effort 3/5. | T8 | **Next** |
| **Install-Date Filter + Filter-Applied Indicator** | Upstream #1855 + #1964 detailed a power-user filter UX gap: no "recently installed" sort, no install-date range picker, and no visible indicator that filters are active (200+-app devices regularly hide things accidentally) ([S149]). NG's main list and Finder both fall short here. Add an install-date column to the sort menu, a date-range bottom-sheet filter, and a persistent "N filters active — clear" chip when any filter is non-default. Effort 2/5. | T10 | **Next** |
| ~~**Android 17 16 KB Page-Size Compatibility**~~ ✅ 2026-05-17 | Remediated for NG's source/release path after Pixel 9a Android 17 testers reported zero-app-list failures from non-16 KB-aligned native libraries ([S148]). [`CMakeLists.txt`](app/src/main/cpp/CMakeLists.txt) now links `libam.so` with `-Wl,-z,max-page-size=16384` and `-Wl,-z,common-page-size=16384` per Android's 16 KB page-size guidance ([S336]); [`NativeLibraries`](app/src/main/java/io/github/muntashirakon/AppManager/scanner/NativeLibraries.java) parses ELF32/ELF64 program headers so App Details / scanner rows show `PT_LOAD.p_align` status as 16 KB aligned, not aligned, or unknown; and [`scripts/verify-native-page-alignment.py`](scripts/verify-native-page-alignment.py) is wired into both reproducible-release wrappers to fail publish APKs with misaligned ELF load segments or uncompressed `.so` ZIP offsets following the `zipalign -P 16` rule ([S337]). Audit: [docs/audits/2026-05-17-android17-16kb-native-page-size.md](docs/audits/2026-05-17-android17-16kb-native-page-size.md). Device-side install testing on a 16 KB Android image remains required once a local/CI JDK is available. | Eng-Debt | **Shipped** |
| ~~**Google Play Contacts/Location-Button Policy**~~ ✅ 2026-05-08 | Audit clean — policy does not apply. NG declares **only** `READ_PHONE_STATE` (used for the telephony-side mobile/Wi-Fi data-usage split via subscription-ID lookup), not any contact or location permission. The roadmap row's claim that the manifest declares `READ_CONTACTS` and `ACCESS_FINE_LOCATION` was incorrect; the permission strings appear in source only as label constants in [`PermissionGroupCatalog.java`](app/src/main/java/io/github/muntashirakon/AppManager/permissions/PermissionGroupCatalog.java) for the Permission Inspector to render groups when inspecting *other* apps. No NG UI button reveals contact info or precise location. Audit at [docs/audits/2026-05-08-google-play-contacts-location-policy.md](docs/audits/2026-05-08-google-play-contacts-location-policy.md) — closes the row before the 2026-05-15 enforcement window with no code change required. | Eng-Debt | **Now** |
| ~~**KernelSU ADB-Root Privilege Detection**~~ ✅ 2026-05-08 (detection foundation) | New static helper [`Ops.isAdbShellRoot()`](app/src/main/java/io/github/muntashirakon/AppManager/settings/Ops.java) returns true when the configured mode is ADB but the working shell's uid is 0 — the "ADB Root" surface KernelSU v3.2.3+ added (also reachable via APatch's adb-root toggle and Magisk's kang mode). Cheap, all-thread-safe, no shell round-trip. The detection layer doesn't gate on KernelSU specifically because APatch / Magisk-kang reach the same privilege state; callers wanting a stricter "root manager confirmed" gate pair this with `RootManagerInfo.detect()`. The roadmap row's "explicit user confirmation before NG trusts it" UX work is deferred to the still-pending Privilege Health-Check screen and onboarding wizard — the helper documents the trust caveat in its javadoc so consumers pick up the rule when they wire it. Reference: [S166]. | T9 | **Now** |
| **Blocker-Style IFW Rule Editor UI** | `lihenggui/blocker` v2.0.6339 ships a visual IFW (Intent Firewall) rule editor that renders/imports/exports `/data/system/ifw/*.xml`, plus a combined IFW + `pm disable` controller and a community-curated debloat ruleset ([S155]). NG's component manager applies IFW rules via code paths but exposes no editor and no rule-set import. Add a "Component Rules" Settings screen that surfaces the active IFW XML, allows visual toggle/import/export, and registers a community-rule-set fetcher analogous to UAD definitions ([S134]). Effort 4/5; eliminates the largest competitor delta. | T9 | **Next** |
| **Amarok-Hider-Style `pm hide` Toggle** | `deltazefiro/Amarok-Hider` exposes `pm hide` as a per-app one-tap toggle so apps disappear from `pm list packages` and the launcher without being uninstalled ([S162]). NG distinguishes Disable (`pm disable-user`) from Freeze (Shizuku) but does not expose `pm hide`, which is a separate state with different semantics (cannot be re-enabled by the app itself). Add a "Hide" quick-action and a "Hidden" status badge in App Info. Effort 2/5. | T7 | **Next** |
| **Language-Selector Per-App Locale** | `VegaBobo/Language-Selector` calls `LocaleManagerService.setApplicationLocales()` (Android 13+ hidden API) via Shizuku to set per-app display language without root ([S164]). NG's App Info already reads the per-app locale; let it write it too via the same Shizuku path, surfacing a language picker inline. Effort 2/5; a frequent forum request that closes the read/write asymmetry. | T9 | **Next** |
| **InstallerX-Style Biometric Install Gate** | `wxxsfxyzm/InstallerX-Revived` v2.3.2 requires biometric authentication on install and uninstall via `BiometricPrompt`, even for Shizuku-approved sessions, mitigating shoulder-surfing and shared-device install drift ([S159]). NG already has biometric session-lock for the app itself; extend it as an *optional* per-action gate for install / uninstall / clear-data. Effort 2/5. | T9 | **Next** |
| **Debuggable-App Rootless Backup** | `timschneeb/debuggable-app-data-backup` demonstrates rootless `/data/data/<pkg>` backup for apps with `android:debuggable="true"` via `adb backup` + Shizuku shell ([S165]). NG's backup currently requires root; add a "Debuggable-only" rootless backup mode that activates when target app has the debuggable flag and Shizuku is available but root isn't — narrow but unblocks F-Droid sideloads and dev builds. Effort 3/5. | T6 | **Later** |
| **F-Droid 2.0 ROM JSON Pre-Seeding Format** | F-Droid 2.0 (alpha8 in test) drops the legacy XML pre-loaded-repo format (`/system/etc/org.fdroid.fdroid/additional_repos.xml`) for a JSON file at `/system_ext/etc/fdroid/additional_repos.json` ([S167]). NG's docs/ROM-integration notes (and any "Add NG repo to F-Droid" path) must dual-publish both formats during the migration window. Effort 1/5; documentation + sample file only. | Docs | **Next** |
| **F-Droid 2.0 Index v2 Protobuf** | F-Droid 2.0 alpha8 enters broad testing with a new protobuf-based index v2 incompatible with index v1 ([S168]). If NG ships any F-Droid update-checker (planned T11), it must parse index v2 (protobuf, signed per repo) rather than the XML index. Effort 3/5; deferred behind T11 anyway but worth a row to prevent rework. | T11 | **Later** |
| **Android 17 ACCESS_LOCAL_NETWORK + Static-Final Reflection Ban** | Android 17 (API 37) introduces `ACCESS_LOCAL_NETWORK` runtime permission for any LAN/UDP/mDNS traffic, blocks `Field.set()` on static-final fields even with `setAccessible(true)`, caps Keystore at 50K entries per UID with new `KeyStoreException`, surfaces `MemoryLimiter:AnonSwap`/`SwapFull` exit reasons, extends DCL restrictions to native `.so` files, and breaks background `IntentSender.sendIntent()` BAL inheritance ([S169], [S170]). Six independent compatibility audits required before NG bumps `targetSdk` to 37; the static-final reflection ban alone affects multiple hidden-API workarounds NG carries from upstream. Effort 4/5 cumulative; sequence after iter-18 SDK_INT_FULL plumbing audit lands. | Eng-Debt | **Next** |
| **Android 17 ML-DSA Certificate OID Recognition** | Android 17 adds ML-DSA-65 (OID `1.3.6.1.4.1.2.267.12.6.5`) and ML-DSA-87 (`1.3.6.1.4.1.2.267.12.8.7`) post-quantum signing algorithms to Android Keystore; APKs signed with these on API 37+ devices will surface unknown public-key types in NG's certificate viewer ([S169]). Add OID-to-display-name mapping in `CertUtils` and ensure the cert parser doesn't crash on the new key shape. Effort 1/5. | T9 | **Next** |
| **Android 17 cleartext Deprecation Warning** | Android 17 announces `android:usesCleartextTraffic="true"` as deprecated for Android 18 ([S170]). NG's App Info Network Security panel shows the flag; add a "Cleartext deprecation — Android 18" badge for apps that set the flag without a `network_security_config.xml`. Effort 1/5; pre-emptive UX win. | T9 | **Next** |
| **OwnDroid-Style Dhizuku Device-Owner Mode** | `BinTianqi/OwnDroid` v8.0 embeds the Dhizuku rootless Device-Owner delegation framework directly and exposes `DevicePolicyManager.setPermissionGrantState()` for enterprise-style permission management without root ([S158]). Heavy lift but a leapfrog feature: NG could add a "Device Admin" mode that activates Dhizuku internally and exposes DPM operations (cross-profile intent filters, managed permission grants) without the user installing Dhizuku separately. Effort 5/5. | T9 | **Later** |
| **FireOS SYSTEM USER Privilege Backend** | Upstream #1835 (15 comments, active through March 2026) requested support for Amazon Fire's "SYSTEM USER" privilege escalation path used by LM Toolbox; ~1M Fire device users have no NG-class power tool available ([S147]). Adds a fourth privilege backend (Root / Shizuku / ADB / FireOS SYSTEM USER) gated to FireOS detection. Effort 4/5; meaningful new addressable segment but adds a maintenance lane. | T11 | **Under Consideration** |
| **PI-Style Install-Interception Surface** | `SanmerApps/PI` registers as the system default installer (requester override + executor override), giving it a system-wide hook on every install — automatic backup-before-install, permission diffing, audit logging ([S160]). NG could optionally register itself the same way to enable "Backup before any install" and "Diff permissions on install" features cross-app. Privacy and trust trade-offs are non-trivial; default off. Effort 4/5. | T11 | **Under Consideration** |
| **UpgradeAll-Style Update-Source Plugin API** | `DUpdateSystem/UpgradeAll` v0.13-beta.4 ships a getter-plugin API where F-Droid, IzzyOnDroid, GitHub Releases, APKPure, CoolApk, and custom RSS are loadable update sources ([S163]). NG's planned per-app update-checker should adopt the same pluggable architecture from the start; F-Droid + GitHub built-in, others as community plugins. Effort 3/5 to wire the plugin contract. | T11 | **Later** |
| **Material You / Monet Widget Theming** | Upstream #1944 reports that AppManager's home-screen widgets ignore Android 12+ Monet dynamic colors and look out of place on themed launchers ([S150]). Cross-refs iter-18 Glance widget-parity row [S136]; when Glance widgets land they must consume `MaterialColors.getColor(context, R.attr.colorPrimary, 0)` from `Theme.Material3.DynamicColors` rather than hard-coded brand tokens. | Premium-Polish | **Next** |
| **Shizuku-iptables Per-App Firewall (Reject)** | Three independent non-VPN firewalls (`AhmetCanArslan/ShizuWall`, `dorumrr/de1984`, `shynoiddev/FireWall-Blocks`) all use the same Shizuku-`iptables` per-UID DROP pattern to block WiFi/data per app ([S156], [S157]). Powerful but conflicts with NG's stated "audit and govern apps, not proxy their traffic" philosophy and adds an entire networking-policy maintenance lane. Reject as core feature; a documented "Shizuku command snippets" recipe page is the right scope instead. | n/a | **Rejected** |
| **Thanox-Style Accessibility-Service Auto-Freeze (Reject)** | Hail #385 referenced Thanox's "freeze on swipe-from-recents" using an Accessibility Service ([S130]). Requires NG to register as a system AccessibilityService — a security/privacy posture incompatible with NG's "least-privilege" principle and a Play-policy minefield. Reject. | n/a | **Rejected** |

---

## Iter-20 Research Additions (2026-05-08)

Two-day delta from iter-19 (closing 2026-05-06): GitHub-issue mining of MuntashirAkon/AppManager (#1956–#1968), Canta, Hail (#387–#391), Neo-Backup (#1029–#1034), sdmaid-se (#2410–#2413), UAD-NG (#1386–#1394), Hamza417/Inure (#480), Obtainium (#2908–#2911 + discussion #2846), RikkaApps/Shizuku (#2036–#2052); Android 17 Beta 4 / QPR1 Beta 2 / Android Security Bulletin May-2026; Google Play Developer Verification rollout (BR/ID/SG/TH, enforcement 2026-09-30); 7-day GitHub-releases sweep (Neo-Backup 8.3.18, sdmaid-se v1.7.2-rc0 cert publish, Material Components 1.14.0-rc01, Gson 2.14.0, BouncyCastle 1.84, hddq/restoid v0.5.0, wxxsfxyzm/InstallerX-Revived 26.05); new-competitor harvest the iter-19 list missed (Sui, sameerasw/essentials, yume-chan/VolumeManager, pass-with-high-score/universal-installer, Hjsosn/FireWall-Blocks); GrapheneOS forum + XDA Shizuku/QPR1 threads. Most rows here either *promote* an iter-19 row (severity revision after new evidence) or *concretize* an iter-19 theme with a ground-truth bug from a 2026-05 issue.

| Item | Description | Theme | Tier |
|------|-------------|-------|------|
| ~~**Android Developer Verification — BR/ID/SG/TH Enforcement**~~ ✅ 2026-05-17 | New [`DeveloperVerificationCompat`](app/src/main/java/io/github/muntashirakon/AppManager/compat/DeveloperVerificationCompat.java) centralizes the `developer_verifier` service probe and Android 36.1 `PackageInstaller` failure-reason extras without compile-time SDK-symbol drift. App Details now shows a "Verifier unknown" chip when the device exposes the verifier service; the installer confirmation body warns before commit, and split-APK chooser flows get a blocking developer-verification warning before the install service starts. Final platform failures append `Developer verification: ...` to install dialogs/transcripts when Android returns `EXTRA_DEVELOPER_VERIFICATION_FAILURE_REASON`. Docs updated to state the current public API limit: Android exposes final failure reasons, not a stable installer-side verified/unverified preflight verdict. References: [S172], [S173], [S309], [S310]. | T1 | **Shipped** |
| ~~**Sideloading-Verification Position Document**~~ ✅ 2026-05-08 | Companion to the Verification row above. Published [`docs/sideload-verification.md`](docs/sideload-verification.md) — covers TL;DR, what the verifier does, what AppManagerNG does and does not do, per-situation outcome matrix (4 enforcement regions × certified/non-certified × verified/unverified developer), downstream-packager guidance (F-Droid / IzzyOnDroid / Accrescent / Obtainium), parallel-verifier discussion (GrapheneOS / Accrescent / F-Droid 2.0 protobuf index v2), 6-question FAQ. README "Install" section now carries an `IMPORTANT` callout linking the document for BR/ID/SG/TH users. Reference: [S174]. | Docs | **Now** |
| ~~**CVE-2026-0073 Min-Patch-Level Disclosure for ADB Mode**~~ ✅ 2026-05-08 / in-app banner 2026-05-16 | May 2026 Android Security Bulletin patched CVE-2026-0073, a Critical zero-click proximal RCE in `adbd` running as the shell user ([S175]). NG's ADB-mode and Shizuku-via-wireless-debug paths talk to the same daemon. Advisory shipped at [`docs/security-advisories/2026-05-08-cve-2026-0073-adb-mode.md`](docs/security-advisories/2026-05-08-cve-2026-0073-adb-mode.md). The onboarding capability wizard now checks `Build.VERSION.SECURITY_PATCH` locally and displays an ADB-mode warning when the device patch level is older than 2026-05-01; no network call or telemetry is involved. Effort 1/5. | T5 | **Shipped** |
| ~~**BouncyCastle 1.83 → 1.84 (CVE-2026-3505 / 5588 / 5598)**~~ ✅ 2026-05-08 | BouncyCastle 1.84 closes CVE-2026-3505 (PGP AEAD chunk-size DoS — relevant since `libopenpgp` powers OpenPGP-encrypted backups), CVE-2026-5588, and CVE-2026-5598 (FrodoKEM non-constant-time compare → pre-emptive against future PQ private-key leak when NG adopts ML-DSA). `bouncycastle_version` bumped 1.83 → 1.84 in [`versions.gradle:21`](versions.gradle#L21); CVE list inlined as a trailing comment so the rationale lives at the dependency line. Both `bcprov-jdk15to18` and `bcpkix-jdk15to18` artifacts re-resolve from the same template. No other module in the project declares a direct BouncyCastle dependency (audited; Robolectric's transient 1.81 stays JVM-host-only). Audit at [docs/audits/2026-05-08-bouncycastle-1-84-cve-bump.md](docs/audits/2026-05-08-bouncycastle-1-84-cve-bump.md). Engineering Debt Register row supersedes the long-standing low-urgency 1.83 entry. Effort 1/5. | Eng-Debt | **Now** |
| ~~**Gson 2.13.2 → 2.14.0**~~ ✅ 2026-05-08 | Gson 2.14.0 ships built-in `java.time` adapters (drops `--add-opens` requirement on JDK17) and strict duplicate-JSON-key handling. `gson_version` bumped 2.13.2 → 2.14.0 in [`versions.gradle:26`](versions.gradle#L26). Audited every Gson call-site (7 files: `StaticDataset`, `FilterItem`, `FilterOption`, `OperationHistoryExporter`, `OperationJournalMetadata`, `OpHistoryActivity`, `OpHistoryItem`); zero `setLenient(true)` / `setStrictness(LENIENT)` opt-outs in the codebase, so all parse paths benefit from the strictness improvement. Audit at [docs/audits/2026-05-08-gson-2-14-0-bump.md](docs/audits/2026-05-08-gson-2-14-0-bump.md). Engineering Debt Register row supersedes the long-standing 2.13.2 entry. Effort 1/5. | Eng-Debt | **Now** |
| ~~**Sui (Magisk-Module Shizuku) Detection**~~ ✅ 2026-05-08 | New `checkSuiViaShell()` probe in [`runner/RootManagerInfo`](app/src/main/java/io/github/muntashirakon/AppManager/runner/RootManagerInfo.java) reads `/data/adb/modules/sui/` whenever the privileged shell already returned a non-NONE root manager (Magisk / KernelSU / APatch). Result surfaces as a " + Sui" suffix on the onboarding-sheet Root status line via `OnboardingFragment.buildRootManagerSuffix()`, alongside the existing ZygiskNext suffix (e.g. "Detected · Magisk + Sui" or "Detected · KernelSU + Sui + ZygiskNext"). Sui ships with no `moe.shizuku.privileged.api` package, so the Magisk-module marker is the only authoritative signal. The "prefer Sui over Shizuku" routing is deferred to the Privilege Health-Check screen (still pending) — the helper exposes `info.suiPresent` for future consumers but does not yet rewire the Shizuku binder selection. Reference: [S178]. | T5 | **Now** |
| **Shizuku 13.6.0 OEM Allowlist** | Three independent reports in 7 days surfaced 13.6.0 regressions: (a) `RikkaApps/Shizuku#2048` — UserService silently killed via reflective `LoadedApk.makeApplication` NPE on Transsion (Infinix/Tecno/Itel) Android 15 ROMs ([S179]); (b) Mediatek-known-bad — per `zacharee/InstallWithOptions` README, downgrade to 13.5.4 required; (c) XDA — Pixel 9 Pro on stock Android 16 QPR1 sees Shizuku turn off after a few minutes regardless of battery-restriction state ([S180]). NG's Shizuku probe should (a) read `Build.MANUFACTURER` + `ro.transsion.version` + `ro.product.platform` (Mediatek SoC tag) + `Build.VERSION_CODES`, (b) compare against Shizuku binding API version, (c) surface a "Your OEM/SoC is breaking Shizuku 13.6.0 — pin Shizuku 13.5.4" banner with deep-link to F-Droid 13.5.4 archive when in the known-bad matrix. Effort 2/5. | T5 | **Now** |
| **Shizuku Root-Backed Avoidance for Banking Apps** | `RikkaApps/Shizuku#2052` ([S181]) — when Shizuku is started in Root mode (KernelSU + Shizuku service backed by `su`), apps that hate root crash on launch even though they were never granted Shizuku permission (banking, Play Integrity-strict). NG's Shizuku integration must never default to *root-backed* Shizuku when ADB-backed is available; surface a one-tap "Switch Shizuku to ADB mode" entry in **Settings → Mode of Operation** with a tooltip explaining the banking-app side-effect. Effort 2/5. | T5 | **Now** |
| **OS-Revert Detection Banner** | Cross-issue theme: AM `#1956` (battery-optimization "ignore" stuck — Doze re-flags within 60s) ([S182]) + Hail `#387` (frozen apps' state lost on unfreeze — launcher icon doesn't return) ([S183]). Pattern: NG writes a state, the OS silently undoes it, the user sees no signal. Build a generic reverted-by-OS detector: after every NG-applied state mutation (battery-optimization grant, freeze, AppOps mode, component-disable), schedule a 30s re-poll; if the post-poll state ≠ NG's intended state, surface a Material banner ("OS reverted your change — see why") with a one-tap diff against the current Doze allowlist / component setting / AppOps mode. Novel — no current competitor (Hail, Canta, SD Maid) surfaces this. Pairs with the iter-19 Operation Activity Log row. Effort 3/5. | T9 | **Now** |
| ~~**A16 QPR2 `clearApplicationUserData` Fallback Path**~~ ✅ 2026-05-08 | [`PackageManagerCompat.clearApplicationUserData()`](app/src/main/java/io/github/muntashirakon/AppManager/compat/PackageManagerCompat.java) now (a) snapshots `IStorageStatsManager.queryStatsForPackage()` `dataBytes + cacheBytes` pre-clear, (b) calls the hidden-API IPC path, (c) re-snapshots post-clear, (d) when the post-clear size hasn't dropped below the pre-clear baseline (with a 64 KiB tolerance for the user-data dir's skeleton state), runs `pm clear --user N <pkg>` as a shell fallback. The 64 KiB tolerance avoids false positives on the small placeholder state the OS retains even after a clean wipe; full-MB-or-GB silent-failure cases (Poco F3 / Infinity-X 3.9 / Root mode on QPR2) are caught and recovered. The IPC path is also fallen back-to-shell when it throws, so true IPC failures plus QPR2 silent-success both route to the same shell remediation. Reference: AM #1965 / [S184]. | T2 | **Now** |
| ~~**LineageOS 23.2 / Android 16 Root Binder Regression Probe**~~ ✅ 2026-05-08 | New `logBootstrapFailureSignature()` helper in [`LocalServer.checkConnect()`](app/src/main/java/io/github/muntashirakon/AppManager/servermanager/LocalServer.java) emits a single-line failure signature whenever the privileged-shell handshake throws (`IOException` / `AdbPairingRequiredException`). Captures `Build.MANUFACTURER/PRODUCT/DEVICE`, `SDK_INT`, `Build.ID`, `ro.lineage.version` (when present), the exception class + message, and the cause chain. Bug reporters can copy this single line from `Log.e("IPC", …)` into an issue instead of a full audit log. The actual SELinux denial line still has to be pulled from `dmesg` / `logcat` separately, but the device + exception fingerprint is now structured and trivially diff-able across reports. Reference: AM #1962 / [S185]. | T2 | **Now** |
| ~~**Debloater Activity-Alias for Pre-A13 / Unisoc Devices**~~ ✅ 2026-05-08 | AM `#1963` ([S186]) — Moto g22 / Unisoc T606 / Android 12 crashed on Debloater shortcut tap with `ActivityNotFoundException`. Added `.debloat.DebloaterActivityAlias` (`android:exported="true"`, `targetActivity=".debloat.DebloaterActivity"`) in [`AndroidManifest.xml`](app/src/main/AndroidManifest.xml) so external launcher pins / Tasker shortcuts / third-party app shortcuts resolve to a stable component name even on platforms where pinning the underlying activity directly fails. The alias has no `CATEGORY_LAUNCHER` filter — it does not appear as a separate launcher icon. Hidden-API runtime coverage now exists separately via the iter-52 harness; launcher-target resolution remains a future UI/intent lint if needed. | T2 | **Now** |
| ~~**App Info Popup Density Refactor — Cert Subject + Issuer**~~ ✅ 2026-05-08 (partial) | The "Sign · SHA-256" cert dialog now exposes the X.509 **Subject** and **Issuer** distinguished names alongside the fingerprint. Computed in `AppInfoViewModel.populateSigningCertInfo()` (replaces `computeSigningCertSha256`) via `cert.getSubjectX500Principal().getName()` / `getIssuerX500Principal().getName()`; rendered as labelled sections in [`AppInfoFragment.showCertFingerprintDialog()`](app/src/main/java/io/github/muntashirakon/AppManager/details/info/AppInfoFragment.java) under three new string resources (`cert_fingerprint_dialog_{sha256,subject,issuer}_header`). Copy button still copies fingerprint-only to keep AppVerifier / `apksigner verify --print-certs` paste-compatibility. The other parts of the row (SDK-row reorder, two-column layout, popup max-height) target an upstream `app_info_card.xml` popup that doesn't exist in NG — App Info is a full pager fragment (`pager_app_info.xml`), not a bottom sheet, so the layout-density bullets don't map. Reference: [S187]. | T21 | **Now** |
| ~~**Per-OEM Debloat Risk Ribbon — One UI 8.5 SmartSuggestions**~~ ✅ 2026-05-08 | New [`OemBloatRiskTable`](app/src/main/java/io/github/muntashirakon/AppManager/debloat/OemBloatRiskTable.java) helper resolves vendor-aware known-bad warnings from a `(package, Build.MANUFACTURER, vendor-OS-version)` triple. First entry: `com.samsung.android.smartsuggestions` on Samsung One UI 8.5 (`ro.build.version.oneui == 80500`) renders a Settings → Mobile-Networks crash-loop warning. Wired into [`BloatwareDetailsDialog.bind()`](app/src/main/java/io/github/muntashirakon/AppManager/debloat/BloatwareDetailsDialog.java) — vendor-known-bad ribbons stack on top of any upstream debloat-list warning and always escalate the alert chip to `ALERT_TYPE_WARN` regardless of the upstream Removal rating. Wildcard fallback (`*`) handles devices where the vendor-OS-version property is unreadable. Reference: UAD-NG #1394 / [S188]. | T7 | **Now** |
| **Default-App Role Re-Binding After Restore** | UAD-NG `#1390` ([S189]) — restore path for `com.android.dialer` doesn't re-bind the dialer Role, leaving the user without a phone app. NG's restore engine must call `RoleManager.createRequestRoleIntent(ROLE_DIALER / ROLE_SMS / ROLE_HOME / ROLE_BROWSER)` after `pm install-existing` for any system app whose previous default-app status the backup manifest captured; auto-prompt the user to re-bind. Effort 2/5. | T6 | **Next** |
| **Backup Scheduler Newest-Age Gate** | Neo-Backup `#1034` ([S190]) — scheduled "user apps" run skips any package that already has *any* backup, even stale ones. NG's backup scheduler (T6 Scheduled Auto-Backup) must use age-of-newest-backup as the gate (`if newestBackup.ageMillis < schedule.minAgeMillis`), not "exists / doesn't exist". Folds into the un-implemented T6 row; flag here so the implementation matches Neo-Backup pain at design time. Effort 1/5 (post-T6). | T6 | **Next** |
| **CIFS / SMB Backup Streaming Hardening** | Neo-Backup `#1029` ([S191]) — streaming write to SMB/CIFS providers corrupts archives with `\0` byte sequences. Pre-empts iter-18 row "SMB / WebDAV Network Backup Destination" with a known failure mode: AM's backup writer must avoid `WriteableByteChannel.write` short-write races on SAF-mediated SMB providers; flush-then-fsync per chunk and verify byte count post-write. Add a regression test that writes through a `SmbjFileSystem` mock and re-reads. Effort 2/5. | T6 | **Next** |
| **Profile Blocklist Editor Enumerates Backup Roots** | Neo-Backup `#1032` ([S192]) — you can't blocklist a package whose only on-disk trace is its backup folder. NG Profiles → blocklist editor must enumerate `<backup-root>/*` directories in addition to the live `PackageManager` list, so a user can re-add a previously-uninstalled package to the blocklist for restore-time decisions. Effort 1/5. | T6 | **Next** |
| **Wi-Fi Configurations Backup (Root)** | Neo-Backup `#1033` ([S193]) — saved Wi-Fi networks + passwords aren't preserved across factory resets / ROM flashes. Adds a "system data" backup category (Wi-Fi, Bluetooth pairings, accounts) to NG's backup engine — root-only path via `/data/misc/wifi/` + `/data/misc/bluetooth/`. Promotes from iter-19 "Backup Extras" Under Consideration row to a concrete root-only sub-feature. Effort 3/5. | T6 | **Next** |
| **Squashfs Writer Header Validation** | AM `#1957` ([S194]) — AM-generated squashfs backup triggers a Dolphin warning on open; magic-byte / header off vs `mksquashfs 4.6` reference. Validate NG's squashfs writer header against the mksquashfs 4.6 binary in CI (round-trip test) and ship a fixed magic-byte writer ahead of T6 polish. Effort 2/5. | T6 | **Next** |
| **File Manager Recursive In-Folder Search** | AM `#1964` ([S195]) — built-in File Manager has no in-folder search/filter; large `/data/data/<pkg>/` trees are unnavigable. Add a `SearchView` to `FmActivity` toolbar with debounced (250 ms) recursive filter on `FmAdapter`; reuse the existing main-list filter chip pattern. Concretizes a long-standing T13 gap. Effort 2/5. | T13 | **Next** |
| **Per-App Volume via AppOps `OP_AUDIO_VOLUME`** | Reference impl: `yume-chan/VolumeManager` ([S196]) writes `OP_AUDIO_VOLUME` per-UID via Shizuku. Closes upstream AM `#1863` (App-Ops Per-UID Control) with a known-working pattern — VolumeManager's `AppOpsService` binder usage is the answer to the "AppOps per-UID precision control" iter-19 row. Adopt the binder pattern in NG's existing AppOps editor for any per-UID-only op. Effort 2/5. | T9 | **Next** |
| **InstallerX-Revived Privilege-Elevation Cascade** | `wxxsfxyzm/InstallerX-Revived` v2.3.2 / 26.05 preview ([S197]) — install flow walks Shizuku → root → Dhizuku → MIUI optimization-disable nudge in a single cascade; AM's installer is a flat session-options chooser without privilege fallback. Replace AM's `InstallerActivity` privilege-pick UX with a cascading-fallback pattern: prefer the lowest-privilege path that succeeds; surface each fallback step with a chip ("Tried ADB, falling back to Shizuku..."). Pairs with the GrapheneOS A16 background-install fix below. Effort 3/5. | T11 | **Next** |
| ~~**GrapheneOS A16 Background-Install-Confirmation Fix**~~ ✅ 2026-05-08 (patch reference shipped) | Documented patch reference at [`docs/patch-references/2026-05-08-grapheneos-a16-background-install.md`](docs/patch-references/2026-05-08-grapheneos-a16-background-install.md). Captures both fixes from GrapheneOS AppStore Release 36 ([S198]): (a) wrap user-confirmation `startActivity()` in an `isResumed` check + defer to `onPostResume()` when paused; (b) audit `getCallingPackage()` + `getReferrer()` and drop queued `PendingActions` when an external untrusted caller re-targets the activity. Port deferred until an Android 16 test device is available — the same calibre of patch as the iter-19 16 KB page-size fix where wrong-state-machine fixes have historically caused silent install-cancellation regressions on the still-current path. The doc lists the exact NG site to patch ([`PackageInstallerActivity.java`](app/src/main/java/io/github/muntashirakon/AppManager/apk/installer/PackageInstallerActivity.java)) and the validation steps so a future maintainer can resume the work without re-reading the GrapheneOS Release 36 changelog from scratch. | T11 | **Now** |
| **Split-APK Cert-Mismatch Dialog** | `pass-with-high-score/universal-installer` ([S199]) surfaces per-split signing-cert mismatches in a per-split table dialog before commit; AM's installer silently rejects mismatched-signature splits. Replace AM's silent rejection with a Material dialog listing each split's `(name, version, cert SHA-256, mismatch reason)` so users can selectively dismiss bad splits. Pairs with the iter-19 SAI / `.apks` ingestion row. Effort 2/5. | T12 | **Next** |
| ~~**Predictive-Back WebView Freeze Fix**~~ ✅ 2026-05-09 (audit clean — row premise stale) | Audit at [`docs/audits/2026-05-09-predictive-back-webview.md`](docs/audits/2026-05-09-predictive-back-webview.md). The roadmap row's premise that NG ships WebView surfaces in `RulesActivity` and an APK-info preview pane is **stale** — neither activity exists in NG (Component Rules viewers are `RulesFragment` RecyclerView surfaces, not WebView). The single WebView in NG is `HelpActivity`, which already uses the correct dispatcher-based pattern: `android:enableOnBackInvokedCallback="true"` declared in the manifest, `OnBackPressedCallback` registered via `getOnBackPressedDispatcher().addCallback(...)`, and the WebView's `canGoBack()` state tracked on `doUpdateVisitedHistory()` so predictive-back animation only previews when there's a back-stack entry. The Obtainium #2911 regression class only affects activities that bypass the dispatcher or register a raw `OnBackInvokedCallback` without integrating with the WebView's back-stack — neither pattern is present in NG. Audit doc establishes the canonical shape for any future WebView-hosting activity (in-app changelog viewer in v0.5.0, JADX decompile pane in T12). | T2 | **Next** |
| **Material Components 1.13 → 1.14 (FocusRingDrawable + SplitButton)** | Material Components 1.14.0-rc01 ([S201]) ships `FocusRingDrawable` (auto-construct from child shape, hide on no-window-focus) and a `SplitButton` RTL fix. Closes a known a11y gap (iter-19 T17 row "TalkBack Navigation Audit") with a free baseline. Bump `materialVersion = 1.14.0` in `gradle/libs.versions.toml` once 1.14.0 stable lands; minSdk 23 still matches NG. Gated on the Material 1.14 stable release ≠ rc01. Effort 1/5 (dep bump). | T17 | **Next** |
| **Compose Material 3 1.5.0-alpha19 — M3 Expressive Toggle/FAB Promotion** | `androidx.compose.material3` 1.5.0-alpha19 ([S202]) promoted ToggleButtons / FAB / menu APIs to stable in M3 Expressive. NG's Compose surfaces (planned Pro-mode dashboard) should pin to this BOM when starting Compose work — pre-empts API churn. Document in `versions.gradle` comment; not yet actionable. Effort 0/5 (note only). | Premium-Polish | **Later** |
| **AGP 8.13.2 → 9.2.0 Migration** | AGP 9.0 (Jan 2026), 9.1.1 (Apr 2026), 9.2.0 (Apr 2026) shipped during the iter-19 → iter-20 window ([S203]). AGP 10 (mid-2026) will be the cliff — old `BaseExtension`-flavored DSL goes away. Promotes the iter-18 AGP-9.2 Eng-Debt row from "track for annual cycle" to **must-do before AGP 10**. R8 in 9.1+ defaults to repackaging classes into the unnamed/default package for DEX — audit `proguard-rules.pro` `-keep` rules that NG holds on `io.github.muntashirakon.**` reflection. Effort 3/5. | Eng-Debt | **Next** |
| ~~**AOSP Source-Pull Retarget to `android-latest-release`**~~ ✅ 2026-05-08 | Trunk-stable model live in 2026: AOSP source publishing now happens on Q2 + Q4 cadence only ([S204]). Pinned the **`android-latest-release`** branch as the only safe target for `hiddenapi/` stub harvesting in two places: a new "Pulling AOSP source for `hiddenapi/`" section in [`CONTRIBUTING.md`](CONTRIBUTING.md) (developer-facing) and a new [`hiddenapi/README.md`](hiddenapi/README.md) (in-module visibility for anyone landing in that directory). Both explicitly forbid `master` / `main` / `android-mainline` / date-stamped tags and point version-specific backports at version-tagged branches. Iter-52 added `scripts/generate-hidden-api-baseline.ps1`, so future stub refreshes can update the instrumented-test baseline after pulling from the pinned AOSP source target. Effort 1/5. | Eng-Debt | **Now** |
| **Android 17 ML-DSA Keystore `KeyPairGenerator` Recognition** | Iter-19 `[S169]` covered ML-DSA cert OIDs; iter-20 confirms Android 17 Keystore now exposes `KeyPairGenerator` algorithms `ML-DSA-65` / `ML-DSA-87` via `KeyProperties.KEY_ALGORITHM_ML_DSA_*` ([S205]). NG's signature-verification utility (`apk/signing/*`) must enumerate these algorithms when displaying signer info — otherwise Android-17-signed APKs render "Unknown algorithm". Pairs with the iter-19 cert-OID-to-name mapping row in `CertUtils`. Effort 1/5. | T9 | **Next** |
| **Keystore 50K Cap — HKDF-from-Master Backup Key Derivation** | Concretizes iter-19's audit-clean Android-17 50K Keystore key cap row: NG's per-archive AES key minting could plausibly cross 50K over a long-lived install once Scheduled Auto-Backup (T6) lands — switch to deterministic HKDF derivation from a single `am_keystore.bks` master so total Keystore-resident keys stay bounded at the existing 2 aliases. Audit `crypto/AESCrypto` and the planned T6 backup writer. Pre-emptive — closes a future-correctness gap before it materializes. Effort 2/5. | T9 | **Next** |
| ~~**System.load() Read-Only Native Audit (Android 17)**~~ ✅ 2026-05-08 | Android 17 requires extracted native libs to be `chmod 444` post-extraction ([S206]). **Audit clean — zero matches.** Every `System.loadLibrary("am")` (only two call sites — `AhoCorasick.java:7`, `CpuUtils.java:13`) uses the canonical AOSP path; zero `System.load(absolutePath)` call sites exist. The 40+ `IoUtils.copy` call sites never write `.so` files (they handle APK extraction, backup data, image cache, logs, keystore export — the only `.so` references are *read-only* ELF parsing paths in `scanner/NativeLibraries.java`, `apk/signing/ZipAlign.java`, `details/AppDetailsViewModel.java`). Packaging declares `useLegacyPackaging true` but the platform installer extracts native libs and handles the read-only flag itself. Audit at [docs/audits/2026-05-08-android17-system-load-readonly.md](docs/audits/2026-05-08-android17-system-load-readonly.md). | Eng-Debt | **Now** |
| ~~**Android 17 Static-Final Reflection Audit (Severity Promotion)**~~ ✅ 2026-05-08 (audit shipped) | Audited 20 `setAccessible(true)` call sites across `app/`, `libcore/`, `server/`. 17 are safe (10 `Method`/`Constructor`, 7 read-only `Field.get`). Of the 3 `Field.set()` writes, 1 targets an instance field (safe), 1 was a redundant write-back to `Typeface.sSystemFontMap` (static-final) — **fixed**: the map's contents are mutated in place via `remove()`/`put()` so the `field.set(null, sameRef)` call was a no-op; removed it from [`TypefaceUtil.java`](app/src/main/java/io/github/muntashirakon/AppManager/utils/appearance/TypefaceUtil.java). The remaining site at [`RootServiceMain.java:206`](server/src/main/java/io/github/muntashirakon/AppManager/server/RootServiceMain.java) writes to `Resources.mSystem` (static-final); flagged for the targetSdk=37 bump task with three remediation options documented (VarHandle lookup, decorator at consume sites, or hidden-API exemption). Currently `targetSdk=36` so it's not yet broken — fixing it without a real Android 17 device risks regressing the still-current path. Audit at [docs/audits/2026-05-08-android17-static-final-reflection.md](docs/audits/2026-05-08-android17-static-final-reflection.md). | Eng-Debt | **Now** |
| **Restic-Style Backup Engine** | `hddq/restoid` v0.5.0 ([S208]) ships a real restic-backed Android backup (industry-standard chunked AEAD, dedup, snapshot metadata) — answers in one move both the iter-19 GCM-cipher-reuse-on-large-OBB pain (`#1958`) and the CIFS-null-bytes streaming bug (Neo-Backup `#1029` above). Architectural decision: add a "restic mode" toggle in NG `BackupOptions` alongside the existing tar+GCM engine, or vendor restic as a sidecar binary launched from libsu shell. GPL-3.0 license alignment requires confirming restic's BSD-2-Clause is GPL-compatible (it is). Significant lift; defer to v0.7+ but architect for it now. Effort 4/5. | T6 | **Under Consideration** |
| **Persistent ADB tcpip 5555 Detection in Shizuku Setup** | `RikkaApps/Shizuku#2044` ([S209]) — wireless-debugging is unstable on Android 11+ and users want to bootstrap Shizuku against an existing `adb tcpip 5555` session. NG's first-run Shizuku setup wizard should `Socket("127.0.0.1", 5555)` connect-test on entry and surface a third path ("Use existing tcpip session") next to the QR-pair / USB-ADB options. Effort 1/5. | T5 | **Next** |
| **Backup-Aware Doze Allowlist Diff Banner** | Concretizes the OS-Revert Detection Banner row above for the specific Doze case: when NG grants `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` and the OS reverts within 60s (AM `#1956` failure mode), the banner must include a one-line diff against `device_idle_constants` so the user sees *why* (OEM hard-coded denylist, system-app-only allowlist, Knox policy override, etc.). Pairs with the iter-19 SD-Maid-style Auto-Fix Battery Optimization (already shipped) — the diagnostic complement to the auto-fix. Effort 1/5. | T5 | **Now** |
| **Watch-List: kerneldroid/Shizuku-modern + Buge-App-Manager** | `kerneldroid/Shizuku-modern` ([S210]) is a Compose Material 3 Expressive Shizuku fork targeting SDK 37; `BugeStudioTeam/Buge-App-Manager` ([S211]) is a direct name-collision competitor (both very early). Watch only — no row to commit yet. If `Shizuku-modern` survives 90 days, NG's Shizuku probe should detect both upstream Shizuku and Shizuku-modern via the iter-19 [S139] Hidden-Shizuku-fork-detection pattern (enumerate every package exporting `moe.shizuku.api.SHIZUKU_SERVICE`). Effort 0/5 (track only). | n/a | **Watch** |

---

## Iter-21 Research Additions (2026-05-09)

Capability-extension delta. Two-track focus: (a) **dataset ingestion** — auxiliary debloat / privacy datasets to make Finder, Debloater, and the Tracker tab smarter without re-deriving them per-device, and (b) **modern Android 14–16 platform APIs** that expose privacy / security signals we currently can't show. Companion research at [`docs/research/2026-05-09-capability-extension.md`](docs/research/2026-05-09-capability-extension.md). Each row is net-new versus iter-18/19/20 and the existing T1–T21 themes. Rows are deliberately scoped to *new data sources* and *unexploited APIs* — capability rows already named in T9 / T16 / T19 (tracker AppOps, Insular cloning, corpse-finder) are not duplicated here.

| Item | Description | Theme | Tier |
|------|-------------|-------|------|
| **Ingest UAD-NG `uad_lists.json` Dependency Graph** | UAD-NG's `resources/assets/uad_lists.json` ([S212]) populates `dependencies` and `neededBy` densely (~80% of system entries) where our upstream MuntashirAkon dataset populates them sparsely. Add `tools/import_uad.py` that emits a sidecar `dependencies.json` keyed by package id with edges only — don't merge entry bodies (overlap is messy and our descriptions are higher-quality). Refresh weekly via CI. Powers honest "removing X will break Y, Z, W" warnings in the Debloater that today require manual graph authoring. License: GPL-3.0 → AGPL-3.0 one-way OK per FSF. Effort 2/5. | T7 | **Next** |
| **Ingest UAD-NG Preinstalled-Lists for OEM Provenance** | Sister repo `Universal-Debloater-Alliance/universal-android-preinstalled-lists` ([S213]) is a per-OEM ground-truth corpus of "package X is known preinstalled on OEM Y, model Z, region R" claims. Bundle as sidecar `preinstalled.json`, expose via `IFilterableAppInfo.getKnownPreinstallOems()` and surface as Finder filter chips ("preinstalled on Samsung / Pixel / OnePlus / Xiaomi / Motorola") plus an App Info "Provenance" row. Avoids re-deriving the OEM presence map per-device audit. License: GPL-3.0. Effort 2/5. | T7 | **Next** |
| **Ingest IzzyOnDroid `apkscanner-data` SDK Signatures** | `codeberg.org/Katastima/apkscanner-data` ([S214]) ships SDK / NLP / anti-feature signatures that complement the εxodus tracker DB; powers F-Droid IzzyOnDroid antifeature flags. Catches signatures Exodus doesn't (~30–40% additional library coverage on a typical Play Store APK). The existing tracker-scan engine already does regex-match against class-signature lists — adding apkscanner-data is one new asset file + one merged scan pass + dual-source attribution in the Trackers tab. License: GPL-2.0 → GPL-3.0+ compatible. Effort 2/5. | T9 | **Next** |
| **Achno Samsung Debloat List Cross-Check** | `Achno/debloat-samsung-ADB-shizuku` ([S215]) is a Samsung-specific list with CSC (Country Sales Code) / region annotations in prose. One-time validation pass against `oem.json`'s Samsung block to fill gaps in our 2026-05-08 S25 Ultra additions ([SysAdminDoc/android-debloat-list@b53089f]). Not ongoing ingest — corpus is small and prose-only. Effort 1/5. | T7 | **Now** |
| **SDK Sandbox Runtime Enumeration** | Android 14+ `SdkSandboxManager.getSandboxedSdks()` ([S216]) returns the list of advertising / analytics SDKs an app currently has loaded into its sandbox process. Today we detect SDKs at static-analysis time via Exodus signatures; the SDK Runtime surface tells us which sandboxed SDKs are *actually loaded right now*. Add a new "SDK Sandbox" row in App Info → Trackers tab; falls back to "Not supported on this device" below API 34. Adds a runtime-truth column to the static tracker panel. Effort 2/5. | T9 | **Next** |
| **Domain Verification Audit + Deep-Link Conflict Finder** | `DomainVerificationManager.getDomainVerificationUserState(packageName)` ([S217]) lets us enumerate every domain an app claims via `<intent-filter android:autoVerify="true">`, the verification state per domain (verified / declined / unverified), and — by cross-referencing across all installed apps — which other installed apps claim the same domain. Net effect: a deep-link hijack inspector. v1 = per-app "Domain Verification" tab in App Info (effort 2/5); v2 = system-wide "Deep Link Conflicts" Finder filter (effort 3/5). API 31+ baseline; behaviour expanded in 34. | T9 | **Next** |
| **App Archiving (Android 15+)** | `PackageInstaller.requestArchive()` / `requestUnarchive()` ([S218]) removes the APK while preserving user data — Hail-adjacent freeze with zero privilege requirement (the platform handles it). Pairs with our existing freeze infrastructure as a third state — Active / Frozen / Archived — and unlocks storage reclaim that freeze can't deliver. New "Archive" action in App Info next to Freeze and Force Stop; new "Archive selected (preserves data)" Batch Operations entry. Effort 2/5. | T19 | **Next** |
| **Memory Tagging Extension (MTE) Status Chip** | Per-app manifest property `android:allowNativeHeapPointerTagging` (effective Android 11+) plus runtime MTE state (Android 14+ Pixel 8+) ([S219]) determine whether an app gets MTE-hardened heap protection. Today the user has no way to see who opted in. Add a new "MTE: enabled / disabled / not supported" chip to the App Info tag cloud — sibling to the iter-18 signing-cert SHA-256 chip. Below API 30 displays "MTE: not supported on this device". Effort 1/5. | T9 | **Next** |
| **Health Connect Privacy Dashboard** | `HealthConnectClient.getGrantedPermissions()` ([S220]) enumerates which apps read or write fitness / sleep / clinical data via the Android 14 mainline Health Connect store. We currently audit standard permissions and AppOps but treat Health Connect as opaque. New "Privacy Dashboard" entry under Settings (or as a sibling tab under T19 Storage Analysis) listing Health Connect permission grants per app. Differentiator vs. every other Android app manager today. Effort 2/5. | T9 | **Next** |
| **Credential Manager Audit** | `CredentialManager` + `Settings.ACTION_CREDENTIAL_PROVIDER` ([S221]) lets us enumerate which apps registered passkeys / passwords / federated identity providers. Today the user can see *that* providers exist in system Settings but not which credentials are stored, when, or by whom. Pairs with the Health Connect dashboard as a second "Privacy Dashboard" tab. Effort 2/5. | T9 | **Next** |
| **Restricted Settings Unlock Walkthrough** | Restricted Settings (Android 13+) ([S222]) blocks sideloaded apps from being granted Accessibility / NotificationListener / DevicePolicy without a manual long-press unlock in Settings. This is the #1 support question for sideloaded power-user apps. Detect the restriction in the existing T5 Privilege Health-Check, explain why it exists, and deep-link to the right Settings screen with a "long-press the disabled toggle" tooltip. Eliminates a recurring user pain. Effort 2/5. | T5 | **Now** |
| **MMRL + LSPosed Module Browser Tab** | Read-only enumeration of `/data/adb/modules/` (Magisk + KernelSU + APatch) and `/data/adb/lspd/` (LSPosed) ([S223]) — parse `module.prop` metadata for each. We already detect the active root provider (iter-7); a "Modules" entry under Privilege Health-Check is one tab away. v1 read-only (effort 3/5); v2 toggle modules (effort 3/5); v3 install/upload (effort 4/5). Surfaces module management without requiring users to leave NG for MMRL or LSPosed Manager. | T5 | **Later** |
| **Decision-Logged: Local-VPN Per-App Firewall** | NetGuard / TrackerControl-style local VpnService + DNS proxy + per-app rules ([S224]). Effort 5/5. The biggest single capability gap vs. dedicated tracker-blocking apps. Decision: **park under Under Consideration until T9 AppOps-based tracker blocking has shipped.** Once we have user feedback on whether AppOp-layer enforcement is sufficient, revisit. AppOp blocking is fraction-of-the-cost and addresses the Day-1 user need; a VPN service is a substantially larger surface (battery, MTU, DNS pinning, per-network rules, single-VPN-slot conflict with WireGuard / Mullvad / corporate VPN). Effort 5/5. | T9 | **Under Consideration** |

---

## Iter-22 Research Additions (2026-05-09)

Same-day phase-2 extension after iter-21. Where iter-21 covered datasets and modern Android APIs, iter-22 covers the **operational layer** AppManagerNG hasn't yet built out: privileged-action accountability, recoverability when shells die, automation surfaces (Tasker / QS / Wear), non-phone form factors, localization tooling, migration paths in/out, and offline resilience. Companion research at [`docs/research/2026-05-09-roadmap-extension-phase-2.md`](docs/research/2026-05-09-roadmap-extension-phase-2.md) and [`docs/research/2026-05-09-observability-testing-audit.md`](docs/research/2026-05-09-observability-testing-audit.md). Mandatory category sweep: every prompt-stipulated category (security, accessibility, i18n/l10n, observability, testing, docs, distribution, plugin ecosystem, mobile/non-phone, offline/resilience, multi-user/collab, migration, upgrade) is represented below or already covered in T1–T21 / iter-18–21.

### Reliability & Recoverability

| Item | Description | Theme | Tier |
|------|-------------|-------|------|
| ~~**Privileged-Shell Journal + DeathRecipient Replay**~~ ✅ shipped 2026-05-17 | `BatchOpsJournal` now writes persistent intent/executing state before each `BatchOpsService` run and clears only after `BatchOpsManager.performOp(...)` completes. If the service dies, the process is killed, or Shizuku/Sui's binder-dead callback fires while a batch is active, the journal remains/marks interrupted. `MainActivity` checks the journal on reattach when no batch service is currently working and shows an "Interrupted batch operation" recovery dialog with retry, not-now, and clear actions. The retry path restarts the saved `BatchQueueItem`; normal per-package failures still use the existing failed-app retry flow. Root/ADB LocalServer failures are covered by the persisted executing journal and exception path rather than a fragile socket poll. References: [S225], [S330]. | T5 | **Now** |
| ~~**Mode Self-Test "Doctor"**~~ ✅ shipped 2026-05-17 | Settings -> Privileges now has a distinct "Mode doctor" active probe beside the passive health rows. It generates a copyable report with ordered PASS/WARN/FAIL/SKIP probes for configured/inferred mode, root binary/grant, root manager and Sui marker, Shizuku binder/UserService/permission/UID, ADB USB/wireless/pairing reachability, LocalServer `id -u`, SELinux domain, and ABI state. Each row includes concrete fix guidance such as granting root, starting/authorizing Shizuku, enabling USB/Wireless debugging, or sharing the support-info bundle after LocalServer failure. References: [S226], [S227]. | T4 | **Now** |
| ~~**LocalServer Bootstrap Smoke Test**~~ ✅ shipped 2026-05-17 | Settings -> Privileges -> "LocalServer bootstrap smoke test" now runs the LocalServer privileged-shell handshake end-to-end against the current mode, probes `id -u`, and displays a copyable single-line signature on success or failure. The existing bootstrap-failure log path now uses the same `LocalServer.buildBootstrapSignature(...)` formatter, so success and failure reports share device/build/mode/UID/LineageOS/exception/probe fields. Extends the bootstrap-failure-signature feature already shipped in v0.4.1. Reference: [S228]. | T4 | **Now** |
| ~~**Support Info Bundle Composer**~~ ✅ shipped 2026-05-17 | Settings -> Troubleshooting -> "Share support info" now snapshots Android/ROM build, root/Shizuku/Sui/provider state, AppManagerNG mode, feature flags, the remembered LocalServer bootstrap signature, and the last 120 lines of logcat into a zero-network `support-info-<device>-<ts>.txt` file shared through the system chooser. UIDs are masked, package-like tokens / URIs / paths / emails are scrubbed from the log tail, and no installed-package list is included. References: [S229], [S230]. | T4 | **Now** |
| ~~**Privileged Op Audit Log**~~ ✅ shipped 2026-05-17 | The roadmap gap was stale after the earlier op-history implementation: `op_history` is already a Room-backed append-only table with `time`, `type`, `data`, `status`, and metadata `extra`; `OpHistoryActivity` already provides viewer/search/filter/sort/delete/rerun/share, JSON/CSV/text export, and Settings -> Privacy retention controls. Iter-45 completed the explicit `(exit_code, signature)` fields by adding normalized `exit_code` metadata for batch/profile success/failure and platform installer status codes, plus the remembered LocalServer bootstrap signature in details and JSON/CSV export. Current journaled surfaces cover batch ops, installer sessions, and profile runs; per-command shell replay remains the separate Privileged-Shell Journal row. References: [S231], [S232]. | T9 | **Now** |
| **"Was It Me?" Reverse Audit** | Given a package, show every privileged op AppManagerNG performed against it from the audit log. Useful when something broke and the user wonders if AM did it. Power-user accountability; nothing comparable in upstream AM or peers. Composes on top of Privileged Op Audit Log. Effort 2/5. Reference: composes [S231]. | T9 | **Next** |
| **Bundled-Data Freshness Banner + Air-Gap Toggle** | Two-part: (a) write `defs_updated_at` to prefs, banner at >30d, blocking warn at >180d so users don't trust a stale debloat verdict; (b) `prefs.networkDisabled` short-circuits every `OkHttpClient.newCall()` with `IOException("airgap")`. Fails closed; pairs with bundled-only debloat defs for users who refuse network calls. Effort 2/5. Reference: [S233]. | T7 | **Next** |
| **Multi-Mirror Debloat-Defs Fetcher** | When iter-20's "Auto-Update Debloat Definitions" flips on, fetch from a priority list (GitHub raw → Codeberg raw → optional IPFS gateway) with signed manifest verification. F-Droid's mirror pattern; tolerates GitHub blackouts and avoids single-CDN failure. Effort 3/5. Reference: [S234], [S235]. | T7 | **Next** |
| **Battery-Aware JobScheduler for Batch Ops** | `setRequiresBatteryNotLow(true)` + `setRequiresDeviceIdle(true)` for non-urgent batch freezes / disables / debloat-passes. Defers to charging-idle windows; respects Doze + App Standby Buckets + Android 16 JobScheduler quotas (already surfaced in iter-18). Effort 2/5. Reference: [S236]. | T6 | **Next** |
| **Atomic-Write Profile Dir + Syncthing Conflict Picker** | Atomic JSON writes with mtime preservation in `/sdcard/AppManagerNG/profiles/` so Syncthing handles peer sync without conflicts; AM-NG ships a conflict-file picker UI for `<name>.sync-conflict-<date>-<peer>.json` files Syncthing emits. Zero-code P2P profile sync. Effort 2/5 (UI only). Reference: [S237]. | T8 | **Next** |

### Migration & Distribution

| Item | Description | Theme | Tier |
|------|-------------|-------|------|
| **Snapshot Bundle Export/Import** | One-button "Export everything" / "Import everything": ZIP of `prefs/` + `profiles/` + `tags/` + `history.db` + audit-log + bundled-data manifest, with schema-version header. AnkiDroid `.colpkg` precedent. **Critical pre-rename ship**: must land *before* v0.2.0 applicationId rename (`io.github.muntashirakon.AppManager` → `io.github.sysadmindoc.AppManagerNG`) so users don't lose data on the flip. Effort 2/5. Reference: [S238]. | T11 | **Now** |
| **Importers: Canta + UAD-NG + Hail** | Three trivial importers covering the AM-adjacent power-user ecosystem: Canta JSON presets ([S239], `{packages: [...]}` schema, ~30 LoC), UAD-NG `uad_settings.json` per-device-serial selection state ([S240]), Hail tag-list newline-separated package lists ([S241]). Lets users migrating *to* NG arrive with their existing curation intact. Effort 1/5 each (3 total). | T8 | **Next** |
| **Export-to-Upstream-AM Compat Profile** | Emit an upstream-AM-compatible subset of NG profile JSON on export so users who try NG can switch back if they want. Anti-lock-in stance — important for adoption from the upstream-AM user base. Effort 2/5. Reference: [S01]. | T11 | **Next** |
| **WebDAV Snapshot Target** | DAV4JVM-backed WebDAV destination for the snapshot bundle (#export-import). Hits the Nextcloud / ownCloud / Hetzner Storage Box use case the way Aegis does for vault sync. Effort 3/5. Reference: [S242]. | T6 | **Later** |
| ~~**APK Share-Target Receiver**~~ ✅ 2026-05-17 (stale gap closed by audit) | Already implemented in the installer surface: [`PackageInstallerActivity`](app/src/main/AndroidManifest.xml) is exported for `ACTION_SEND` with `application/vnd.android.package-archive`, `application/vnd.apkm`, `application/xapk-package-archive`, and `application/octet-stream`; it also accepts `ACTION_SEND_MULTIPLE` and `ACTION_VIEW` / `ACTION_INSTALL_PACKAGE` for `file:`, `content:`, and `package:` URIs. [`ApkQueueItem.fromIntent()`](app/src/main/java/io/github/muntashirakon/AppManager/apk/installer/ApkQueueItem.java) consumes the shared URIs and persists granted URI permission when available. [`PackageInstallerActivity`](app/src/main/java/io/github/muntashirakon/AppManager/apk/installer/PackageInstallerActivity.java) then shows tracker counts, dependency warnings, checksum confirmation, and signature-mismatch handling before or during the install flow. Reference: [S243]. | T11 | **Now** |
| ~~**`floss` vs `full` Build Flavors (F-Droid Clean Path)**~~ ✅ 2026-05-17 | `app/build.gradle` now defines the `distribution` flavor dimension with default `floss` (`BuildConfig.ALLOW_OPTIONAL_NETWORK_FEATURES=false`) and `full` (`true`). `floss` preserves local ADB/localhost networking but disables the optional user-facing Internet feature gate, VirusTotal/Pithus scan-report lookups, and debloat-definition auto-update path through `FeatureController`; `full` keeps those surfaces available behind the existing opt-in settings. Release reproducibility scripts collect recursive flavored APK outputs, CI artifact globs cover flavored unit/lint reports, Obtainium now filters for `full` assets, and [`docs/distribution/build-flavors.md`](docs/distribution/build-flavors.md) pins F-Droid metadata to `flossRelease` with F-Droid anti-feature rationale. Reference: [S244], [S245]. | T1 | **Now** |

### Automation Surface (T8 deepening)

| Item | Description | Theme | Tier |
|------|-------------|-------|------|
| ~~**Broadcast Intent API (`io.github.sysadmindoc.AppManagerNG.action.*`)**~~ ✅ 2026-05-17 | Concrete schema for the existing T8 placeholder is shipped in [`AutomationIntents`](app/src/main/java/io/github/muntashirakon/AppManager/automation/AutomationIntents.java) and [`AutomationReceiver`](app/src/main/java/io/github/muntashirakon/AppManager/automation/AutomationReceiver.java). The manifest declares `io.github.sysadmindoc.AppManagerNG.permission.AUTOMATION` as `signature` and requires it on the exported receiver. Supported actions cover `FREEZE` / `UNFREEZE` / `FORCE_STOP` / `CLEAR_CACHE` / `CLEAR_DATA` / `UNINSTALL` / `BACKUP` / `RESTORE` / `DISABLE_COMPONENT` / `ENABLE_COMPONENT` / `RUN_PROFILE` / `INSTALL_FROM_URI` / `SCAN_TRACKERS`, routing service work through existing batch/profile/installer/app-details paths. Full contract documented in [`docs/intent-api.md`](docs/intent-api.md). Reference: [S246], [S247]. | T8 | **Now** |
| ~~**`am://` URI Scheme — Concrete Schema**~~ ✅ 2026-05-09 (App Info alias shipped, freeze/profile/install deferred) | First concrete shape of the schema documented at [`docs/intent-api.md`](docs/intent-api.md). **`am://app/<pkg>?user=<uid>` shipped** as a short alias for the canonical `app-manager://details?id=<pkg>&user=<uid>`: new `AM_SCHEME` + `APP_HOST` constants in [`SelfUriManager`](app/src/main/java/io/github/muntashirakon/AppManager/self/SelfUriManager.java), parsing routes through the existing `getUserPackagePairFromUri()` so consumers don't change; new intent-filter `<data android:host="app" android:scheme="am"/>` on the `details.AppInfoActivity` activity-alias in [`AndroidManifest.xml`](app/src/main/AndroidManifest.xml). The remaining shapes (`am://freeze/<pkg>`, `am://profile/<id>/run`, `am://install?source=<url>`) are reserved in the docs but **not yet wired** — they need the iter-22 T8 broadcast-intent automation surface ([S247]) for the authorization model (signature-permission-gated callers vs. user-confirmed dialogs). Pairs with T18 deep-link infrastructure — same URL-dispatch layer. Reference: [S246]. | T8 | **Now** |
| **Tasker Plugin (In-App, No Separate APK)** | Add `com.twofortyfouram.locale.intent.action.EDIT_SETTING` activity + `FIRE_SETTING` receiver inside NG. Pulls in `com.joaomgcd:taskerpluginlibrary` (~120 KB APK growth). Action plugin only (no event/state/condition v2 in v1). Works with Automate (LlamaLab) + MacroDroid for free — both consume Locale-spec plugins. **Novelty 5/5 in the AM/Canta/Hail/SD Maid/Neo Backup space**: SD Maid has had open Tasker tickets for ~7 years; AM has [#1219](https://github.com/MuntashirAkon/AppManager/issues/1219) closed-as-duplicate. Effort 2/5. Reference: [S248], [S249], [S250]. | T8 | **Next** |
| **Per-App Pinned QS TileService** | `TileService` bound to a single user-pinned app for repeat force-stop on a misbehaving app. Distinct from the iter-18 "Hail-Style Auto-Freeze QuickSettings Tile" (which runs a profile); this tile is per-app force-stop. Use Android 13's `TileService.requestAddTileService()` for one-tap install from onboarding. Effort 1/5. Reference: [S251]. | T8 | **Next** |
| ~~**App Shortcut: Freeze / Force-Stop / Clear Cache Per-App**~~ ✅ 2026-05-17 | [`AppActionShortcutPublisher`](app/src/main/java/io/github/muntashirakon/AppManager/shortcut/AppActionShortcutPublisher.java) publishes dynamic launcher shortcuts from the loaded app list, choosing recent installed packages and only exposing actions supported by the current privilege path: freeze, force-stop, and clear-cache. [`AppActionShortcutActivity`](app/src/main/java/io/github/muntashirakon/AppManager/shortcut/AppActionShortcutActivity.java) is non-exported, runs after normal `BaseActivity` authentication, and dispatches through the same `FreezeUtils` / `PackageManagerCompat` helpers as App Details. [`AppInfoFragment`](app/src/main/java/io/github/muntashirakon/AppManager/details/info/AppInfoFragment.java) now lets users pin force-stop and clear-cache action shortcuts by long-pressing those action chips, and freeze shortcuts default to "Freeze <app>" / "Unfreeze <app>" labels. Unit coverage: [`AppActionShortcutPublisherTest`](app/src/test/java/io/github/muntashirakon/AppManager/shortcut/AppActionShortcutPublisherTest.java). Reference: [S252]. | T8 | **Now** |
| **Sharing Shortcuts API for Direct-Share** | Publish top-N apps as direct-share targets so a user can send a backup / profile JSON straight to a contact-shaped target. Replaces deprecated `ChooserTargetService` (gone in SDK 31). Effort 2/5. Reference: [S253]. | T11 | **Later** |
| **DocumentsProvider for `am://backups` + `am://profiles`** | Backups + profile JSONs surface in any SAF picker (Files by Google, Material Files, Solid Explorer). `MANAGE_DOCUMENTS` not required since paths are user-scoped. Backup managers don't ship this — high differentiation. Effort 3/5. Reference: [S254]. | T6 | **Later** |
| **Assist App Handler / Quick Pick** | Register `android.intent.action.ASSIST` so long-press home / power gesture launches NG's Quick Pick — App Switcher with Force Stop / Freeze / Open App Info affordances for whichever app is currently in the foreground. Hail v1.10.0 just shipped this (already noted in iter-18 [S130]); concretize the implementation. Effort 1/5. Reference: [S130]. | T8 | **Later** |

### Form-Factor Expansion (T17 + new themes)

| Item | Description | Theme | Tier |
|------|-------------|-------|------|
| **`ListDetailPaneScaffold` + 5-Breakpoint WindowSizeClass** | Concretize T17 Adaptive Layout. Use Jetpack WindowManager 1.4 + `androidx.compose.material3.adaptive.ListDetailPaneScaffold` driven off `currentWindowAdaptiveInfo().windowSizeClass`. Five M3 breakpoints: Compact / Medium / Expanded / Large / XL (TriFold / Tab S11 Ultra / external display). Without this, NG looks dated on a Tab S11 vs. Inure / Material Files. Note: NG ships Views, not Compose — the equivalent is `SlidingPaneLayout` + `WindowMetricsCalculator` + `WindowInfoTracker`; same UX shape, different API. Effort 2/5. Reference: [S255], [S256], [S257]. | T17 | **Next** |
| **Foldable Posture Awareness** | `WindowInfoTracker.windowLayoutInfo` → `FoldingFeature` (state FLAT / HALF_OPENED, orientation, isSeparating) plus WindowManager 1.4's `supportedPostures` API for tabletop detection. Z Fold 7 / Pixel Fold 2 use case: package list on outer panel, detail on inner panel when unfolded. Zero net-new effort once master/detail row above lands — Activity Embedding handles fold/unfold transitions for free. Effort 2/5. Reference: [S258]. | T17 | **Next** |
| **DeX Multi-Instance + Pop-Up Resizeable** | `Intent.FLAG_ACTIVITY_LAUNCH_ADJACENT \| NEW_TASK \| MULTIPLE_TASK` to allow two NG instances side-by-side (compare two app's manifests, diff permissions). One UI 8 / Android 16 introduced new pop-up swipe handle on Z TriFold and tablets — declare `android:resizeableActivity="true"` and support `android.allow_multiple_resumed_activities`. Free win. Effort 2/5. Reference: [S259]. | T8 | **Later** |
| **ChromeOS ARCVM Detection + UX Polish** | `hasSystemFeature("org.chromium.arc.device_management")` → hide phone-only ops (SIM, telephony bloat), surface ChromeOS-broker constraints (system installs go through ChromeOS, not `pm`). Keyboard / mouse polish piggybacks on master/detail. Effort 2/5. Reference: [S260]. | T17 | **Later** |
| **Android XR Manifest + Spatial Track** | `<uses-feature android:name="android.software.xr.api.spatial" android:required="false"/>` plus optional XR-track variant on Play (`android.software.xr.api.openxr`) sharing the same package name. Galaxy XR launches Q4 2026 with **the most open sideload story of any XR platform** — direct APK install from browser, no PC, no developer mode. Lands NG as plausibly the first FOSS app manager on Android XR. Effort 2/5. Reference: [S261], [S262]. | T1 | **Later** |
| **Wear OS Phone-Side Companion** | The killer addition. **No FOSS app does this.** Phone-side ADB-over-WiFi to paired Wear OS watch, `WearableListenerService` + `MessageClient` for live state, ~200 KB companion APK on the watch with same package ID + signing cert. Operations: list packages, pull APK, disable / enable, force-stop, clear data, `pm uninstall --user 0`, set DPI, debloat with red/orange/green safety scoring. Galaxy Watch 7+ uses One UI Watch 6 + Wear OS 5; bootloader locked but `pm` works fine. Effort 4/5. Defer until [`Wear OS Tiles + Material3TileService`](https://developer.android.com/training/wearables/tiles/versioning) infrastructure stabilizes; under-consideration not because of risk but because of effort + bandwidth. Reference: [S263], [S264], [S265]. | T16 | **Under Consideration** |

### Localization & i18n

| Item | Description | Theme | Tier |
|------|-------------|-------|------|
| **Hosted Weblate Bridge (NewPipe / Tachiyomi Pattern)** | Bridge to upstream's `hosted.weblate.org/projects/app-manager/` as a separate `app-manager-ng` component sharing upstream's glossary + TM. Cost ~1 day setup, free hosting. Win: every locale upstream gains flows downstream, NG-specific strings (Pro mode, M3 dashboard labels) translate independently. Self-hosted Weblate is overkill at this scale; Crowdin OSS reintroduces a proprietary dep. Effort 2/5. Reference: [S266], [S267]. | T10 | **Now** |
| ~~**Pseudolocale Build Variants + RTL CI Pass**~~ ✅ 2026-05-09 (build-side) | `pseudoLocalesEnabled true` set on the `debug` build type in [`app/build.gradle`](app/build.gradle); release stays clean. Debug builds now bake `en-XA` (accented + bracketed text — catches truncation, missing-translatable strings) and `en-XB` (RTL mirror of English — catches RTL layout breaks). Activate via `adb shell setprop persist.sys.locale en-XA` (or via Settings → Developer options → Pseudolocale on Android 13+). The CI screenshot-diff side of this row (`connectedCheck` matrix + PR comment) stays open as a follow-up — gates on the **Espresso + UI Automator Smoke Pack** row landing first (it provides the headless instrumentation pipe the screenshot capture needs). Reference: [S268]. | T10 | **Next** |
| ~~**Per-App Locale Picker (`AppCompatDelegate.setApplicationLocales`)**~~ ✅ 2026-05-09 | Wired the in-app language preference into the OS-side per-app locale: [`Prefs.Appearance.setLanguage()`](app/src/main/java/io/github/muntashirakon/AppManager/settings/Prefs.java) now calls `AppCompatDelegate.setApplicationLocales(LocaleListCompat)` after persisting the preference, and registers `androidx.appcompat.app.AppLocalesMetadataHolderService` (`autoStoreLocales=true`, `enabled=false`) in [`AndroidManifest.xml`](app/src/main/AndroidManifest.xml) for API 26-32 SharedPreferences-backed back-port. `android:localeConfig="@xml/locales_config"` was already declared at the application level, so on API 33+ the OS surfaces AM-NG in **Settings → Apps → AppManagerNG → Language** out of the box; the new wiring keeps that surface in sync with the in-app picker bidirectionally. `LANG_AUTO` maps to `LocaleListCompat.getEmptyLocaleList()` so "Auto" tracks the system locale via the platform mechanism rather than NG's own override pipeline. The existing `AppearanceUtils.applyConfigurationChangesToActivities()` recreate path remains untouched and continues to drive immediate in-app re-render. Reference: [S269]. | T10 | **Now** |

### Observability + Testing + CI

| Item | Description | Theme | Tier |
|------|-------------|-------|------|
| **In-App Log Viewer with Severity Filter + Redaction** | Replace raw logcat surface with structured viewer: filter by tag / severity, one-tap "redact UIDs / paths / package names" for screenshot sharing, "copy line" button. Inure ships this. Protects users when pasting into issues. Effort 2/5. Reference: [S270]. | T4 | **Next** |
| **Opt-In Local ACRA-Style Crash Sink** | ACRA configured to write crash reports to `Documents/AppManagerNG/crashes/*.json`, NOT a network endpoint. User shares via Support Info Bundle Composer. Default OFF. F-Droid `Tracking` antifeature avoided because no network and disabled by default. Effort 2/5. Reference: [S271], [S244]. | T4 | **Under Consideration** |
| **Macrobenchmark Module + Baseline Profiles** | New `:benchmark` module measuring cold-start, package-list scroll jank, Backups screen TTI. Run on physical device in CI nightly; results posted as PR comment. Generate Baseline Profile for the app-list path. AM cold-start is dominated by `PackageManager` enumeration — exactly the workload where Baseline Profiles deliver outsized win. Pure local, no privacy cost. Effort 4/5. Reference: [S272]. | T20 | **Later** |
| **Espresso + UI Automator Smoke Pack** | Headless device-side suite that exercises: open app-list, freeze / unfreeze test package, view component blocker, run one-shot rule. Runs in `connectedCheck` on emulator matrix (API 26 / 30 / 34 / 35 / 36). SD Maid SE ships these; AM has zero. Catches regressions in privileged ops without user telemetry. Note: this is a *smoke pack for power-user flows*, distinct from the project's "no unit tests unless requested" policy in `CLAUDE.md`. Effort 4/5. Reference: [S273]. | T11 | **Later** |
| ~~**CI Dependency CVE Scan**~~ ✅ 2026-05-09 | Two-layer pipeline shipped at [`.github/workflows/dependency-scan.yml`](.github/workflows/dependency-scan.yml): (a) **PR Dependency Review** runs `actions/dependency-review-action@v4` on every pull request, fails the PR on HIGH/CRITICAL severity introduced by a dependency change and denies CC-BY-NC* / CC-BY-ND* / AGPL-1.0 licenses up-front (GPL-3.0-or-later compatibility); (b) **Weekly OWASP Dependency Check** (`org.owasp:dependency-check-gradle:10.0.3`, classpath in [`build.gradle`](build.gradle)) on Sunday 04:13 UTC + manual `workflow_dispatch` produces a full HTML/SARIF report uploaded as artifact (catches CVEs disclosed *after* the dependency landed). Configured to consume an `NVD_API_KEY` secret when set for higher rate limits, anonymous fallback otherwise. Local runs use `failBuildOnCVSS = 11.0` (effectively never fail) so the report is purely informational; CI uses `continue-on-error: true` and surfaces the report rather than failing the entire weekly cadence on a single new CVE. F-Droid reproducible-build pipeline already does signature checks; this stacks the CVE check on top. Reference: [S274]. | T4 | **Now** |
| **OEM Quirk Panel** | Detect Samsung Knox / MIUI / EMUI / OxygenOS / ColorOS / OneUI / HyperOS at runtime. Surface known limitations (Knox container packages can't be frozen; MIUI auto-restarts disabled apps; EMUI revokes notification access; HyperOS aggressive App Standby). Link to per-OEM workaround page in app. UAD-NG carries per-OEM lists; AM users hit these constantly. Effort 3/5. Reference: [S275], [S240]. | T9 | **Next** |

### Plugin Ecosystem

| Item | Description | Theme | Tier |
|------|-------------|-------|------|
| **MMRL System-App Distribution Channel** | Ship NG as a `signature \| privileged` system-app via MMRL's Magisk / KernelSU / APatch module repo so it can use platform-signature-gated APIs without manual `/system` install. NG already detects all four root managers (iter-7). Module repo URL pinned, signed, optional install path. Effort 3/5. Reference: [S276]. | T1 | **Later** |
| **LSPosed Companion Module** | Optional `am-xposed-helpers` module — auto-unfreeze on launch (Hail just shipped this), bypass component-blocking on whitelisted apps, log hidden API calls for the Audit Log. NG core stays Xposed-free — ships as a separate module APK. Effort 4/5. Reference: [S277]. | T9 | **Under Consideration** |
| **KDE Connect Plugin (Custom `packetType`)** | New `packetType: kdeconnect.appmanager.action` so desktop KDE Connect can trigger freeze / uninstall / backup-now on phone. Submit upstream PR; ship pre-built fork as fallback. Cross-device power-user surface no AM-class app has. Effort 4/5. Reference: [S278]. | T8 | **Under Consideration** |
| **Termux:API-Style Helper APK** | `com.sysadmindoc.appmanagerng.shell` companion APK exposes `am-shell freeze <pkg>` from Termux. Unix-socket dispatcher pattern from `TermuxApiReceiver`. Optional install. Effort 4/5. Reference: [S279]. | T8 | **Under Consideration** |

### Rejects (with reasoning)

| Item | Reasoning |
|------|-----------|
| **Android Automotive OS package management** | AAOS apps are restricted to media / nav / messaging / parking / charging / IoT / weather templates. Power-user package management is not a sanctioned category. AOSP's `disable_packages` doc is for OEMs building the OS, not third-party tools. Effort 5+, differentiation 0. Reference: [S280]. |
| **Android Auto projection** | No package management surface. Car App Library is template-only. |
| **Wear OS standalone Tile / Complication for managing watch from watch** | Novelty without value — power-user watch management belongs on the phone. The phone-side Wear OS companion (above) is the right shape. |
| **Firebase Analytics / Crashlytics / Performance** | Triggers F-Droid `Tracking` + `NonFreeNet` antifeatures, requires GMS, defeats the AM-class user demographic. |
| **Always-on cloud Sentry (any deployment)** | Even with `sendDefaultPii=false` + `beforeSend` scrubber, AM stack traces leak target package names = user behaviour. F-Droid still flags. Acceptable only as **opt-in + self-hosted + scrubber** — already expressed via the Local ACRA Crash Sink under-consideration row. |
| **Anonymous feature-usage counters** | F-Droid's Tracking rule is bright-line: opt-in default-off only. Any "user opened Permissions tab" counter violates project posture. Reference: [S244]. |
| **Play Console Android Vitals** | Distribution is F-Droid + GitHub Releases + IzzyOnDroid + Accrescent; no Play Console signal exists. |
| **Play Integrity / SafetyNet attestation** | Antithetical to a tool whose users root their devices. |
| **Google Play In-App Update API** | GMS dependency; we ship update checks via GitHub Releases JSON / Obtainium / F-Droid's own update channels. |
| **Crowdin OSS plan** | Plausible alternative to Weblate, but reintroduces a proprietary dependency a GPL fork shouldn't accept when Weblate Libre exists and upstream already uses it. Reference: [S266]. |
| **Self-hosted Weblate** | Overkill until contributor count >50 or upstream relationship breaks. Reference: [S266]. |
| **Tor onion mirror for debloat-defs** | Niche; only matters if AM-NG ever becomes geo-blocked. Premature. |
| **Google Drive sync (REST API)** | OAuth + Play Services; conflicts with FOSS-pure positioning. WebDAV (above) covers the use case without Play Services. |

---

## Iter-23 Repo Reconciliation + 2026 Platform Delta (2026-05-16)

Phase-0 state memo: AppManagerNG is an Android power-user package manager fork, not a generic app store or desktop debloater. The current repo is Java/Kotlin Android Views + Material Components, Gradle/AGP, GPL-3.0-or-later, minSdk 21, targetSdk 36, release 0.4.2 / versionCode 6. Its stated philosophy is "retain upstream App Manager power, remove friction": progressive disclosure, rootless/Shizuku/ADB first, no Compose migration yet, no telemetry by default, and no cloud/account dependency for core workflows. Local reconnaissance found the shipped source largely consistent with that philosophy, but several roadmap/docs claims have drifted from disk: `README.md` and the old roadmap claim an NG-specific `CONTRIBUTING.md` exists when it does not; release docs advertise ABI choices while `release.yml` uploads only the universal APK; lint/tests workflows do not trigger for the actual `main` branch; and the changelog claims dependency-check `13.1.1` while `versions.gradle` pins `10.0.3`.

External signal snapshot (GitHub/API snapshot 2026-05-16): upstream AM remains the capability reference (8,025 stars, 213 open issues, pushed 2026-04-16 [S291]); Canta (4,780 stars, pushed 2026-05-12 [S292]), UAD-NG (6,606 stars, pushed 2026-05-11 [S293]), Hail (5,821 stars, pushed 2026-04-26 [S294]), Inure (1,757 stars, pushed 2026-05-16 [S295]), SD Maid SE (6,740 stars, pushed 2026-05-16 [S296]), Neo Backup (3,614 stars, pushed 2026-05-03 [S297]), Android-DataBackup (6,690 stars, pushed 2026-05-14 [S298]), Shizuku (25,130 stars [S299]), LibChecker (6,866 stars, pushed 2026-05-16 [S300]), AppVerifier (1,046 stars [S301]), and Obtainium (17,115 stars [S302]) define the active OSS feature envelope. Commercial signal says users still pay for polished backup scheduling/cloud destinations (Swift Backup, Titanium Backup [S305], [S306]), app history/tags/insights (AppDash [S304]), and lower-friction desktop debloat flows (ADB AppControl [S303]). NG should harvest the workflow lessons, not the account-bound or desktop-only architecture.

### Now: Contract Repair Before More Feature Work

| Candidate | Evidence + fit | Impact | Effort | Risk / dependencies | Novelty | Tier |
|-----------|----------------|--------|--------|---------------------|---------|------|
| ~~**Restore NG Governance Docs (`CONTRIBUTING.md`, support policy, AI-code policy)**~~ ✅ 2026-05-16 | Added root [`CONTRIBUTING.md`](CONTRIBUTING.md), reconciled stale `CLAUDE.md` applicationId/namespace notes, and made the contributor contract explicit: GPL/SPDX, minSdk 21, no broad namespace churn, no telemetry by default, and no unreviewed machine-generated code. | 4/5 | 1/5 | Low; issue/PR template rebrand remains separate. | Parity / hygiene | **Shipped** — contributor routing no longer points at a missing file. |
| ~~**Repair `main`-Branch CI Coverage**~~ ✅ 2026-05-16 | `tests.yml` and `lint.yml` now include `main` for both push and pull_request filters, matching the active branch. | 5/5 | 1/5 | Low; live GitHub execution still needs the next PR/tag event. | Parity | **Shipped** — the silent branch-filter gap is closed. |
| ~~**Release Artifact Matrix Contract**~~ ✅ 2026-05-16 | The reproducible-release scripts now copy and compare every generated release APK, emit a publish manifest, and the release workflow uploads the full verified asset/checksum set. | 4/5 | 2/5 | Medium; next tag should verify actual signed artifact names. | Parity | **Shipped** — release automation no longer assumes one universal APK. |
| ~~**Dependency-Scan Version Reconciliation**~~ ✅ 2026-05-16 | Changelog now matches the live `dependency_check_version = '10.0.3'` pin in [`versions.gradle`](versions.gradle). | 3/5 | 1/5 | Low; future plugin bumps should update changelog and versions together. | Parity | **Shipped** — docs no longer advertise an unpinned scanner version. |
| ~~**MinSdk-21 Dependency Ceiling Ledger**~~ ✅ 2026-05-16 | New [`docs/policy/minsdk-21-ceiling.md`](docs/policy/minsdk-21-ceiling.md) is the running ledger of every dependency that has already dropped (or imminently drops) API 21-22 support: activity 1.11.0 → 1.12.0, biometric 1.4.0-alpha04 → -alpha05, room 2.7.2 → 2.8.0, webkit 1.14.0 → 1.15.0, material 1.13.0 → 1.14.0, plus the activity-embedding window-1.4.0 ceiling. Doc captures the minSdk-21 contract, the "raise the floor vs. raise a dependency" decision tree, and the discovery flow for new ceilings. [`versions.gradle`](versions.gradle) now carries an inline pointer above `min_sdk` so contributors hit the ledger before bumping. Closes the iter-19 Material 1.14 row in the same pass. References: [S201], [S286], [S307]. | 4/5 | 2/5 | Medium; every dependency bump still needs the "minSdk 21 allowed?" gate, but it's now a one-doc lookup. | Parity / upgrade strategy | **Shipped** — ledger lives at [`docs/policy/minsdk-21-ceiling.md`](docs/policy/minsdk-21-ceiling.md). |
| ~~**ADB CVE Patch-Level Banner Follow-Through**~~ ✅ 2026-05-16 | Onboarding now checks `Build.VERSION.SECURITY_PATCH` locally and shows an ADB workflow warning below the mode-picker intro when the patch level is older than 2026-05-01. | 5/5 | 1/5 | Low; static helper covered by `OnboardingFragmentTest`. | Parity / safety | **Shipped** — the warning is now in the first-run/replay surface. |
| ~~**Package-Visibility Declaration Dossier**~~ ✅ 2026-05-16 | New [`docs/distribution/package-visibility.md`](docs/distribution/package-visibility.md) is the canonical `QUERY_ALL_PACKAGES` justification for F-Droid / IzzyOnDroid / Accrescent / Obtainium review. Covers per-surface impact (main list, App Details, backup/restore, debloater, profiles, batch ops, scanner, Filters/Finder, logcat), the no-telemetry/no-cloud posture, distribution-channel eligibility (Play deliberately not pursued because of co-declared privileged permissions), and a maintainer gate so future `<uses-permission>` / `<queries>` edits run through the document before shipping. Reference: [S308]. | 3/5 | 1/5 | Low; docs-only unless distribution channels change. | Parity | **Shipped** — justification lives at [`docs/distribution/package-visibility.md`](docs/distribution/package-visibility.md). |
| ~~**Issue/PR Template Rebrand + Support Bundle Intake**~~ ✅ 2026-05-16 | All five `.github/ISSUE_TEMPLATE/*` files and `.github/PULL_REQUEST_TEMPLATE/pull_request.md` now point at the NG repo, `CONTRIBUTING.md`, and `ROADMAP.md` instead of upstream `muntashir.dev` / `MuntashirAkon/AppManager`. The bug report adds NG-specific support-bundle prompts (active mode, privilege provider, LocalServer bootstrap signature, install source, security patch, ROM/build, architecture, affected operation) so the intake meshes with the future "Support Info Bundle Composer" feature in row 577. The PR template replaces the upstream blanket AI/LLM ban with the tool-assisted-but-reviewed contract from `CONTRIBUTING.md`. References: [S289], [S290]. | 3/5 | 1/5 | Low; future support-bundle composer can extend the same field set. | Parity | **Shipped** — triage intake now collects what AppManagerNG triage actually needs. |

### Next: Feature Harvest That Fits NG

| Candidate | Evidence + fit | Impact | Effort | Risk / dependencies | Novelty | Tier |
|-----------|----------------|--------|--------|---------------------|---------|------|
| ~~**Installer Dependency Resolver**~~ ✅ 2026-05-16 (v1: minSdk gate + `<uses-library>` check) | The long-standing `// TODO: Resolve dependencies` in [`PackageInstallerActivity`](app/src/main/java/io/github/muntashirakon/AppManager/apk/installer/PackageInstallerActivity.java) is now a real call into the new [`InstallDependencyChecker`](app/src/main/java/io/github/muntashirakon/AppManager/apk/installer/InstallDependencyChecker.java). v1 covers the two most common silent install failures: `INSTALL_FAILED_OLDER_SDK` (when `applicationInfo.minSdkVersion > Build.VERSION.SDK_INT`) and `INSTALL_FAILED_MISSING_SHARED_LIBRARY` (when an APK declares `<uses-library>` entries the device's `PackageManager.getSystemSharedLibraryNames()` does not advertise). Each detected issue is rendered as a localized line below the existing tracker callout, so users see the block before they tap Install. Helper is Android-API-free; JVM tests in [`InstallDependencyCheckerTest`](app/src/test/java/io/github/muntashirakon/AppManager/apk/installer/InstallDependencyCheckerTest.java) cover floor-met / floor-not-met / unknown-version cases plus shared-library dedup / trim / missing-name aggregation. Follow-up rows for ABI compatibility and split-APK base-missing detection can extend the same checker without touching the call site. References: [S314], [S304], [S309]. | 4/5 | 3/5 | Medium; ABI + split-base-missing checks tracked as follow-up extension of the same helper. | Parity | **Shipped** — minSdk + `<uses-library>` checks both surface above the install button; ABI / split follow-ups stay open. |
| ~~**Developer Verification Install Transcript**~~ ✅ 2026-05-16 | Install-failure dialog in [`PackageInstallerActivity`](app/src/main/java/io/github/muntashirakon/AppManager/apk/installer/PackageInstallerActivity.java) now exposes a "Copy diagnostic info" neutral button on any non-success status. The new [`InstallTranscript`](app/src/main/java/io/github/muntashirakon/AppManager/apk/installer/InstallTranscript.java) captures timestamp, AppManagerNG version + code, manufacturer + model + device, Android release + API + security patch, ABI, active Ops mode, package name, status code + name, status message, and the source URI — redacted by default so `file:///storage/...`, `content://provider/document/id`, and `https://host/path` paste safely (scheme + authority kept, path replaced with `<redacted>`). `ApkSource.getUri()` was added (null default, overridden in `UriApkSource` / `CachedApkSource`) so the transcript reads the raw source without leaking it. Pure-JVM tests in [`InstallTranscriptTest`](app/src/test/java/io/github/muntashirakon/AppManager/apk/installer/InstallTranscriptTest.java) cover redaction and stable line ordering. References: [S172], [S173], [S310]. | 4/5 | 2/5 | Medium privacy risk; redaction is on by default, opt-in for verbatim source is a future toggle. | Leapfrog | **Shipped** — install failures now copy a paste-friendly diagnostic block. |
| **App Notes / Tags / Watchlist Layer** | Commercial AppDash monetizes app history, tagging, and install insight workflows ([S304]); NG already has package inventory and profiles. | 3/5 | 2/5 | Low; local SQLite only, no telemetry. | Parity | **Next** — useful once audit-log and Finder work settle. |
| ~~**Backup Destination Capability Matrix**~~ ✅ 2026-05-16 | New [`docs/distribution/backup-destinations.md`](docs/distribution/backup-destinations.md) is the canonical inventory of what backup destinations NG supports today (native primary / adopted / removable / USB-OTG via SAF), which destinations are indirect via third-party DocumentsProvider apps (WebDAV, SMB, FTP/SFTP, cloud via Material Files / DAVx⁵ / FolderSync / RCX), and which ones are deliberately rejected (direct Google Drive / Dropbox / OneDrive OAuth — they fail the no-cloud / no-telemetry contract in `CONTRIBUTING.md`). Documents reliability watch-outs per provider, planned destinations cross-linked to existing roadmap rows (Atomic-Write Profile Dir, Backup Retention, Integrity Verification, Sharing Button, Scheduled Auto-Backup), and a "recommended setup per use case" matrix. References: [S305], [S306]. | 4/5 | 3/5 | Medium; future plug-in/provider work tracked separately under iter-23 "Later". | Parity | **Shipped** — provider posture now lives at [`docs/distribution/backup-destinations.md`](docs/distribution/backup-destinations.md). |
| **F-Droid Index-v2 Metadata Reader (Read-Only)** | F-Droid's modern API exposes package metadata suitable for provenance checks without building a full app store ([S311]). | 3/5 | 3/5 | Low-medium; must stay read-only and avoid duplicating Obtainium/F-Droid clients. | Leapfrog | **Next** — fits as "where did this APK come from?" metadata, not update tracking. |
| **Accessibility + Pseudolocale Screenshot Gate** | Pseudolocales are already enabled for debug builds, but screenshot CI remains deferred; AndroidX/Material minSdk churn makes regression gates more important ([S315], [S268], [S307]). | 3/5 | 3/5 | Medium; depends on the UI Automator smoke pack. | Parity | **Next** — catches layout damage from the premium UI track. |

### Later / Under Consideration

| Candidate | Evidence + fit | Impact | Effort | Risk / dependencies | Novelty | Tier |
|-----------|----------------|--------|--------|---------------------|---------|------|
| **ADB AppControl-Style Desktop Companion** | ADB AppControl proves desktop debloat UX demand ([S303]), but NG's strategy is on-device power tooling. | 2/5 | 5/5 | High maintenance and platform split; depends on a stable remote-command API. | Parity | **Under Consideration** — only revisit after T8 automation/API surfaces are stable. |
| **Commercial Cloud Backup APIs** | Swift/Titanium cloud destinations are a paid differentiator ([S305], [S306]), but OAuth/GMS/account coupling conflicts with FOSS/offline posture. | 3/5 | 4/5 | High privacy/support risk; WebDAV/SAF/Syncthing should land first. | Parity | **Under Consideration** — allow plug-in/provider architecture first, not hard-coded Google Drive. |
| **Play-Store-Style Update Tracking** | Obtainium already owns multi-source update tracking and is active ([S302]). | 2/5 | 4/5 | Medium; duplicates an excellent external app and expands network surface. | Parity | **Rejected for now** — deep-link to Obtainium/F-Droid instead. |
| **Desktop Multi-Device Fleet Debloating** | UAD-NG solves this well as a desktop fleet tool ([S293]). | 2/5 | 5/5 | High product mismatch; contradicts on-device-first UX. | Parity | **Rejected** — keep import/export compatibility, not fleet orchestration. |

Mandatory category self-check for this pass: security (CVE banner, dependency scan, package visibility), accessibility/i18n (pseudolocale gate, Material minSdk), observability/testing (CI repair, issue intake), docs/distribution (governance docs, release artifact matrix), plugin ecosystem (left to existing T8/T9 rows), mobile/offline/resilience (F-Droid metadata read-only, no cloud requirement), multi-user/collab (not expanded; existing Finder multi-user debt remains), migration/upgrade (minSdk ledger, release contract). Thin areas are intentional: multi-user and plugin work already have stronger existing rows than any net-new source found in this pass.

---

## Iter-25 Research Additions (2026-05-17)

Nine-day delta off iter-23 (closing 2026-05-08): autonomous research-session sweep on competitor releases, Android 17 platform progression, in-flight uncommitted-work audit. Companion artifacts at [`.ai/research/2026-05-17/`](.ai/research/2026-05-17/) and [`.ai/research/2026-05-17-pass-2/`](.ai/research/2026-05-17-pass-2/). The session also stood up [`docs/architecture/`](docs/architecture/) closing T11 "Architecture Documentation" — see §"Iter-25 Closures" below.

| Item | Description | Theme | Tier |
|------|-------------|-------|------|
| **Shizuku Android-17 Compatibility Watch** | Shizuku 13.6.0 has confirmed Android 17 Beta 3 regressions, but the evidence is mixed: #1965 reports a blank Application Management page while authorized apps still appear to function; #1967 reports broader "app won't get Shizuku" failure; #1988 repeats the Android 17 managed-apps-empty symptom on Pixel 9 Pro XL [S321, S322, S326]. The thedjchi Shizuku fork has an assigned v14.0.0 Android 17 support issue noting new `ACCESS_LOCAL_NETWORK` pairing pressure [S327]. **NG-side mitigation shipped 2026-05-17**: `ShizukuBridge.hasAndroid17CompatibilityRisk(Context)` warns on API 37 while `MIN_ANDROID_17_COMPATIBLE_VERSION` is unknown, onboarding surfaces a Wireless ADB fallback banner, and `.github/workflows/shizuku-release-watch.yml` opens a maintainer issue when Shizuku release notes mention Android 17 / #1965 / #1967. Remaining action: verify on an Android 17 device/image, then populate the fixed-version floor when upstream ships a confirmed fix. Cross-reference iter-19's "Hidden-Shizuku Fork Detection" [S139, S152] in case the community migrates to forks. | T5 | **Watch / mitigated** |
| **Android 17 Stable June 2026 — targetSdk=37 Audit Batch Sequencing** | Android 17 Beta 3 reached platform stability on 2026-03-26; API surface is locked; stable rollout to Pixel devices targets **June 2026** [S324]. The Engineering Debt Register's "Android 17 targetSdk=37 compliance batch" remains the single largest open Eng-Debt item — five open sub-audits (ACCESS_LOCAL_NETWORK runtime permission, usesCleartextTraffic enforcement, MODE_BACKGROUND_ACTIVITY_START_ALLOWED migration, ECH default-on, ML-DSA Keystore OID recognition). Schedule the audit batch into v0.7.x window so the targetSdk bump can land alongside the next minor. Effort cumulative 4/5. | Eng-Debt | **Next** |
| **Neo-Backup 8.3.18 (2026-05-04)** | Minor bump from 8.3.17 cited at iter-18 [S135] / iter-19 [S114]. No NG-action delta surfaced — the rows already derived from 8.3.17 remain accurate. Source-register version pointer updated at [S323]. | T6 | **(no row delta)** |
| **Material Components 1.14.0 still alpha (alpha06/07/08)** | Iter-18 [S57] / [S201] flagged 1.14.0-rc01's minSdk-23 raise as the dep ceiling. As of 2026-05-17 the 1.14.0 line **has not progressed to stable** [S325] — the minSdk-21 ceiling decision can stay deferred. `versions.gradle` comment on `material_version` updated 2026-05-17 to flag the cascade implication when the bump does land. | Eng-Debt | **(no row delta; defer)** |
| **Iter-24 In-Progress Fix Triple (commit-ready, 2026-05-17)** | Three commit-ready in-progress fixes surfaced by the 2026-05-17 research-run uncommitted-work audit, all landed during the iter-25 follow-through: (1) **`fix(finder)`** [73387cd] — `FilterOption.setKeyValue()` was wrapping every regex predicate in `Pattern.quote()`, silently neutering iter-23's tracker-name regex work into literal-string matches; (2) **`security(installer)`** [bcb2874] — install-transcript URI redactor now strips userinfo / query / fragment per RFC 3986 §3.2 (previously leaked `https://host?token=…` and `https://user:pass@host/…` shapes through the "Copy diagnostic info" surface); (3) **`fix(onboarding)`** [25c629a] — root-manager probe captures `Application` context on main thread before background post (`requireContext()` on a detached fragment from a worker pool was silently killing the worker and leaving onboarding root-suffix unpopulated). New `FilterOptionTest` (Robolectric) lands alongside (1). | T7 / T3 / T4 | **Shipped 2026-05-17** |

### Iter-37 Closures (2026-05-17)

| Item | Result |
|------|--------|
| **Filter: Permission Flags** | Closed the T7 row by adding `FilterablePermissionInfo` and permission-detail providers on `FilterableAppInfo` / `ApplicationItem`, then extending `PermissionsOption` with grant-state, custom-source, fixed-state, and raw runtime-permission-flag predicates. |

### Iter-36 Closures (2026-05-17)

| Item | Result |
|------|--------|
| **Finder: Uninstalled App Backups** | Closed the T7 row by adding a Finder-only opt-in for backup rows whose validated backup metadata package/user pair is absent from the package-manager result set. Synthetic `BackupFilterableAppInfo` rows preserve app label, version, system/has-code, keystore, rule, and uninstalled state signals while existing backup filters continue to load validated backup metadata from the DB. |

### Iter-35 Closures (2026-05-17)

| Item | Result |
|------|--------|
| **VPN Plugin Flags Control** | Audited and parked as blocked: AppManagerNG has no `VpnService`, `BIND_VPN_SERVICE`, or Shizuku VPN binding/session source, so an opt-in flag toggle would not control anything yet. |
| **Finder: Description-Field Search** | Closed the T7 row by adding debloat-description predicates to `BloatwareOption`; Finder can now match debloat prose through the existing filter editor. |

### Iter-34 Closures (2026-05-17)

| Item | Result |
|------|--------|
| **Android 16 Capability Dropping UI** | Closed the T5 row by adding `RootCapabilityDiagnostics` and a Settings -> Privileges row that reads the active privileged shell's UID and `CapEff` kernel capability mask, then reports root, dropped, present, unavailable, or unknown states. |
| **Dependent rows left open** | VPN plugin flag control remains open; it likely needs a real Shizuku VPN binding source before it should become an interactive toggle. |

### Iter-33 Closures (2026-05-17)

| Item | Result |
|------|--------|
| **Privilege Health-Check Screen** | Closed the T5 row by adding Settings → Privileges with active-mode diagnostics, root-manager detection, Shizuku API/min-version status, ADB/Wireless ADB state, remote service state, mode self-test status, and the `SelfBatteryOptimization` one-tap auto-fix path. |
| **Dependent rows left open** | Android 16 capability-dropping UI and VPN plugin flag control remained open as follow-ups that could attach to the new Privileges screen; Iter-34 closed capability dropping, leaving VPN plugin flag control. |

### Iter-32 Closures (2026-05-17)

| Item | Result |
|------|--------|
| **Auto-Update Debloat Definitions** | Closed the T5 row by adding the Settings → Privacy opt-in, launch-time updater, manifest/checksum verifier, app-private cache, `StaticDataset` cached-asset fallback, and pure-JVM updater guardrail tests. |
| **UAD-Style Auto-Fetch Debloat Definitions** | Closed the duplicate iter-addition row against the same implementation. The follow-up **Multi-Mirror Debloat-Defs Fetcher** row remains open for signed multi-origin mirrors. |

### Iter-31 Closures (2026-05-17)

| Item | Result |
|------|--------|
| **Cross-User Package State Detection** | Closed the T5 row by carrying per-user enabled/disabled/uninstalled buckets through `ApplicationItem`, showing them on the main list row, and adding state to the multi-user picker. |
| **Finder: Multi-User Scope** | Closed the T7 row by replacing Finder's current-user-only load with `Users.getUsersIds()` and adding visible user/state metadata to Finder rows. |

### Iter-30 Closures (2026-05-17)

| Item | Result |
|------|--------|
| **Auto-fix Battery Optimization (Root/ADB Path)** | Closed T5's root/ADB auto-fix row by adding `SelfBatteryOptimization`, wiring profile routines and long-running backup/import/restore batch operations to call it before execution, and refactoring the Troubleshooting battery entry onto the same helper. |
| **Scheduler battery follow-up** | Left T6's scheduler-specific row open: the backup execution path now auto-fixes when privileged, but the future Scheduled Auto-Backup UI still needs the schedule-enable prompt and no-privilege fallback copy. |

### Iter-29 Closures (2026-05-17)

| Item | Result |
|------|--------|
| **Installer APK SHA256 Toast** | Closed T5's installer checksum row by hashing streamed session bytes, propagating the digest through the install confirmation handoff, and showing a pre-system-prompt checksum dialog. |
| **Dhizuku integration constraint** | Left the Dhizuku row open after a primary-source check: official Dhizuku-API `2.5.4` is current but its upstream module declares `MIN_SDK = 26`, which conflicts with NG's API-21 floor. The next Dhizuku slice needs an optional/reflection integration design or a minSdk decision before code lands. |

### Iter-28 Closures (2026-05-17)

| Item | Result |
|------|--------|
| **USB Debugging Prompt in Shizuku Setup** | Closed T5's Canta-model setup gap by adding a USB-debugging preflight dialog to onboarding's Wireless ADB setup flow and tightening the Shizuku / Wireless ADB / pairing instructions around the two required Developer Options toggles. |

### Iter-27 Closures (2026-05-17)

Same-day follow-through after iter-26 converted the two safest implementation carryovers into code and added process automation for the remaining external dependency.

- ~~**Shizuku Android-17 runtime warning + Wireless ADB fallback**~~ ✅ shipped — [`ShizukuBridge.hasAndroid17CompatibilityRisk(Context)`](app/src/main/java/io/github/muntashirakon/AppManager/shizuku/ShizukuBridge.java) returns true on API 37 while `MIN_ANDROID_17_COMPATIBLE_VERSION` remains unknown; [`OnboardingFragment`](app/src/main/java/io/github/muntashirakon/AppManager/onboarding/OnboardingFragment.java) surfaces an Android-17-only Shizuku warning and starts the existing Wireless ADB setup flow when tapped. Unit coverage: [`ShizukuBridgeTest`](app/src/test/java/io/github/muntashirakon/AppManager/shizuku/ShizukuBridgeTest.java).
- ~~**Shizuku release-feed watcher**~~ ✅ shipped — [`.github/workflows/shizuku-release-watch.yml`](.github/workflows/shizuku-release-watch.yml) scans recent official Shizuku releases weekly and opens a maintainer issue when a 13.6.x / 13.7.x release note mentions Android 17 or Shizuku #1965 / #1967. The checklist points maintainers to populate `MIN_ANDROID_17_COMPATIBLE_VERSION` only after Android 17 device verification.
- ~~**ML-DSA OID-to-display-name mapping**~~ ✅ shipped — [`Utils.getCertificateSignatureAlgorithmName(X509Certificate)`](app/src/main/java/io/github/muntashirakon/AppManager/utils/Utils.java) maps Android 17 ML-DSA-65 / ML-DSA-87 OIDs to readable Dilithium names. Wired into Package Info, Scanner, and `Utils.getIssuerAndAlg()`. Unit coverage: [`UtilsCertificateAlgorithmTest`](app/src/test/java/io/github/muntashirakon/AppManager/utils/UtilsCertificateAlgorithmTest.java).

### Iter-26 Closures (2026-05-17)

Same-day follow-through after iter-25 — see [`.ai/research/2026-05-17-pass-2/CONTINUE_FROM_HERE.md`](.ai/research/2026-05-17-pass-2/CONTINUE_FROM_HERE.md). All five open Android 17 targetSdk=37 sub-audits closed clean; one new Shizuku Android-17 audit shipped with a `confirmed, needs-design` verdict and a non-destructive runtime-detection sketch. The two CI/docs hygiene items landed: the Markdown link-checker workflow ships now; the JaCoCo coverage plan ships as a policy doc the maintainer can drop into a single-commit PR.

- ~~**Android 17 `usesCleartextTraffic` enforcement audit**~~ ✅ shipped — [`docs/audits/2026-05-17-android17-cleartext-traffic-enforcement.md`](docs/audits/2026-05-17-android17-cleartext-traffic-enforcement.md). **Verdict: clean** — `network_security_config.xml` declares `base-config cleartextTrafficPermitted="false"`; manifest has no `usesCleartextTraffic` attribute; pinned domains are HTTPS-only; `127.0.0.1` + `localhost` are loopback opt-ins. Zero source code changes required.
- ~~**Android 17 `ACCESS_LOCAL_NETWORK` runtime permission audit**~~ ✅ shipped — [`docs/audits/2026-05-17-android17-access-local-network.md`](docs/audits/2026-05-17-android17-access-local-network.md). **Verdict: clean** — zero `NsdManager` / `MulticastSocket` / mDNS / `NetworkInterface.getNetworkInterfaces()` in production code. Wireless ADB pairing is input-driven (user types IP+port from the device's Wireless Debugging panel) — no LAN discovery surface that requires the new permission.
- ~~**Android 17 BAL hardening + `MODE_BACKGROUND_ACTIVITY_START_ALLOWED` migration audit**~~ ✅ shipped — [`docs/audits/2026-05-17-android17-bal-intentsender.md`](docs/audits/2026-05-17-android17-bal-intentsender.md). **Verdict: clean** — zero matches in `app/src/main/java/` for the deprecated BAL mode or `setPendingIntentBackgroundActivityStartMode()`. NG launches activities from foreground UI contexts; the `am://` URI scheme and the planned Tasker-parameterized broadcast API are receiver-driven and unaffected.
- ~~**Android 17 ECH default-on audit**~~ ✅ shipped — [`docs/audits/2026-05-17-android17-ech-default-on.md`](docs/audits/2026-05-17-android17-ech-default-on.md). **Verdict: clean** — NG's three network destinations (VirusTotal public API, Pithus public API, loopback) all handle ECH default-on cleanly. No `<domainEncryption>` opt-out needed.
- ~~**Android 17 ML-DSA Keystore OID recognition audit**~~ ✅ shipped — [`docs/audits/2026-05-17-android17-ml-dsa-keystore-oid.md`](docs/audits/2026-05-17-android17-ml-dsa-keystore-oid.md). **Verdict: clean (audit)** — NG's APK cert display already shows both `getSigAlgName()` AND `getSigAlgOID()`; no string-comparison branches on the algorithm name. Iter-27 shipped the polish map recognizing ML-DSA-65 (`1.3.6.1.4.1.2.267.12.6.5`) and ML-DSA-87 (`.8.7`).
- ⚠ **Shizuku 13.6.0 Android 17 compatibility audit** — [`docs/audits/2026-05-17-shizuku-android17-compat.md`](docs/audits/2026-05-17-shizuku-android17-compat.md). **Verdict: mitigated, needs device verification + fixed-version floor** — NG's probes are `Throwable`-caught and will not crash on Android 17 Beta 3; mixed evidence from #1965 / #1967 means the safe posture is a warning, not disabling Shizuku. Iter-27 landed the non-destructive `hasAndroid17CompatibilityRisk(Context)` probe, onboarding fallback banner, and Shizuku release watcher.
- ~~**F-NEW-09 — minSdk-21 cascade analysis**~~ ✅ shipped 2026-05-17 — [`docs/policy/minsdk-21-ceiling.md`](docs/policy/minsdk-21-ceiling.md) gained a "Cascade analysis: what `minSdk = 23` would unlock" sub-section. Maps the Material 1.14 ceiling pressure to the four other pinned-cluster lines (activity, biometric, room, webkit) and documents the decision pressure: 1.14.0 is still alpha (alpha06/07/08 — [S325]), so the floor decision can stay deferred.
- ~~**F-NEW-13 — Markdown link-checker CI workflow**~~ ✅ shipped 2026-05-17 — new [`.github/workflows/docs-link-check.yml`](.github/workflows/docs-link-check.yml) runs `lycheeverse/lychee-action@v2` on push + PR + weekly Tuesday 11:11 UTC. Scope: `*.md`, `docs/**/*.md`, `.ai/**/*.md`, plus the top-level `ROADMAP.md` / `CHANGELOG.md` / `PROJECT_CONTEXT.md`. Cache-backed (7-day TTL); offline-default to honor the FOSS Anti-Features posture for CI runs. Closes the link-rot insurance ask in the iter-24 backlog.
- ~~**F-NEW-11 — JaCoCo coverage rollout plan**~~ ✅ planned 2026-05-17 — [`docs/policy/jacoco-coverage-rollout.md`](docs/policy/jacoco-coverage-rollout.md) documents the full implementation: `app/build.gradle` plugin block + `jacoco` configuration + a `jacocoTestReport` task with the standard Android exclusion set, and a `tests.yml` step that uploads the HTML artifact (optionally to Codecov). The autonomous session deliberately did **not** modify `app/build.gradle` because the JaCoCo wire-in benefits from local-build verification; the plan is detailed enough that the maintainer can land it as a single copy-paste-and-verify commit.

### Iter-25 Closures

- ~~**T11 Architecture Documentation**~~ ✅ shipped 2026-05-17 — Stood up [`docs/architecture/`](docs/architecture/) with three docs and a navigation README: [01-privilege-providers.md](docs/architecture/01-privilege-providers.md) (Root / Shizuku / Sui / ADB / no-root path selection; `Runner` decision tree; `LocalServices` binder bridge; `SelfPermissions.checkSelfOrRemotePermission()` capability fan-out pattern); [02-backup-format.md](docs/architecture/02-backup-format.md) (on-disk layout; metadata schema v1–v6; five `MODE_*` crypto modes; platform-Keystore vs file-BKS; cross-version restore contract; SAF DocumentsProvider considerations); [03-hidden-api-bypass.md](docs/architecture/03-hidden-api-bypass.md) (4-layer stack: `dev.rikka.tools.refine` + `AndroidHiddenApiBypass` runtime; `hiddenapi/` stub source set ~80 files across 13 subsystem namespaces; `compat/*Compat.java` wrappers; the Android-version migration cliff and how the proposed Compatibility Harness amortises it). Closes the T11 "Document AIDL service architecture, privilege escalation paths, backup format spec in `/docs/architecture/`" row and iter-24 backlog item F-NEW-14. Pairs with the audit-doc doctrine README that landed alongside the 2026-05-17 consolidation pass.
- ~~**Iter-24 Backlog F-NEW-04 → F-NEW-08, F-NEW-12**~~ ✅ shipped 2026-05-17 — All five Now-tier governance items from the 2026-05-17 backlog landed: `AGENTS.md` Canonical Project Context pointer block; `README.md` version-target preview refreshed (v0.4.0 ✅ Permission Inspector + Onboarding; v0.5.0 = Settings & Discovery; v0.6.0 = Rootless Power); `ROADMAP.md` Source Appendix S316 → S320 + header pointer to PROJECT_CONTEXT.md; `versions.gradle` `material_version` minSdk-cascade comment; `docs/audits/README.md` documenting the audit-doc doctrine (filename convention `<YYYY-MM-DD>-<topic>.md`, document-shape template, verdict vocabulary). Plus the new `PROJECT_CONTEXT.md` canonical entry-point at repo root. See [`.ai/research/2026-05-17/CHANGESET_SUMMARY.md`](.ai/research/2026-05-17/CHANGESET_SUMMARY.md) and [`.ai/research/2026-05-17-pass-2/CHANGESET_SUMMARY.md`](.ai/research/2026-05-17-pass-2/CHANGESET_SUMMARY.md).

---

## Premium Polish Track (v0.4.x → v0.7.x)

Parallel work-stream off the Onboarding theme (v0.4.0). The four-step rollout drafted in `design/plan/3-rollout.md` migrates AppManagerNG from the existing M3 token plane to a refined v2 token plane (calmer surfaces, tighter typography, pill-shaped FAB/search, outlined card variants). Each phase is independently revertable.

| Phase | Status | Scope |
|-------|--------|-------|
| ~~**v0.4.x Foundation**~~ ✅ shipped 2026-05-03 | Done | `app/src/main/res/values/{colors,dimens,themes}-v2.xml` copied verbatim from `design/impl/`. New pref `PREF_PREMIUM_PREVIEW_BOOL` (default OFF). `Prefs.Appearance.getAppTheme()` routes to `AppTheme.V2` / `AppTheme.V2.Amoled` when enabled, classic `AppTheme` / `AppTheme.Black` otherwise. Toggle exposed at Settings → Appearance → "Preview new design (BETA)". `getTransparentAppTheme()` deferred — transparent surfaces stay classic until v0.5.x. No layout files modified. |
| **v0.5.x Top-5 Surface Migration** — Phase 1 ✅ shipped 2026-05-02 | In Progress | Wired `activity_main_v2.xml` and `item_main_v2.xml` behind the toggle. MainActivity and MainRecyclerAdapter conditionally inflate v2 layouts. Phase 2: AppDetails / AppUsage / Settings surfaces. |
| **v0.6.x Default Flip** | Pending | Default `PREF_PREMIUM_PREVIEW_BOOL` to `true`; expose a "Use legacy design" escape hatch in Appearance for one release. |
| **v0.7.x Toggle Removal** | Pending | Delete the toggle, drop classic `AppTheme*` definitions, retire `colors-v2.xml`/`themes-v2.xml` filenames in favor of canonical `colors.xml`/`themes.xml`. |

---

## Rejected

| Item | Reason |
|------|--------|
| **Multi-device simultaneous ADB** (UAD-NG model) | UAD-NG is a desktop tool; AM/NG is on-device. This is a fundamentally different UX. WONTFIX. |
| **LSPatch integration** | LSPatch archived Dec 2023 (last release v0.6); no longer maintained; no viable path to integration ([S61]). WONTFIX. |
| **Play Store / APKMirror update tracking built-in** | Obtainium ([S26]) solves this with more sources and better maintenance. Out of scope; deep-link instead. |
| **Full on-device app store** | Aurora Store and F-Droid exist and are actively maintained. NG should complement, not replicate. |
| **App health score (single composite number)** | Collapses nuanced data into a single opinionated metric. AM's value is raw transparency; a score obscures it. Rejected — surface the component scores better instead. |
| **announceForAccessibility suppress-only fix** | Must do a real accessibility audit (T10), not suppress the deprecated API call without fixing the underlying UX. |
| **Backup of in-app purchase tokens / DRM state** | Not possible without root + OEM cooperation. Out of scope. |

---

## Engineering Debt Register

Issues catalogued in source that slow or block future work. Resolve before the feature theme that depends on them ships.

| File | Type | Issue | Severity | Theme |
|------|------|-------|----------|-------|
| `KeyStoreUtils.java` | FIXME | Keystore password stored in `String` (not clearable) | **High** | T3 |
| `CodeEditorViewModel.java` | FIXME | ABX→XML conversion is lossy; serializer needs update | **High** | T3, T12 |
| `DexClasses.java` | FIXME | No Smali/Baksmali for API < 26 | Medium | T12 |
| `FmProvider.java` | TODO | DocumentsProvider not properly implemented | Medium | T13 |
| `FinderViewModel.java` | TODO | Multi-user support + uninstalled app backups missing | Medium | T7 |
| `ActivityInterceptor.java` | TODO | Results not sent back to original calling app | Medium | T8 |
| ~~`Utils.java` (×5)~~ ✅ 2026-05-02 | FIXME (resolved) | Soft-input/service-flag/activity-flag/input-feature labels moved to `strings.xml`; protection-level tokens kept canonical (Android API identifiers) | Low–Med | T10 |
| ~~`VirusTotal.java`~~ ✅ 2026-05-02 | TODO (resolved) | New `computeInitialPollWait(fileSize)` ramps the *first* poll wait roughly +1 s per MB above a 10 MB threshold, clamped to [60 s, 240 s] — no more polling a 200 MB upload at 60 s and burning the 4 req/min rate-limit quota. Subsequent polls stay at the 30 s floor. | Low | Scanner |
| ~~`AppTypeOption.java`~~ ✅ 2026-05-02 (partial) | TODO (resolved 2/3) | **Play App Signing** + **Overlay app** flags now active in the AppType filter (`mFrozenFlags` + `withFlagsCheck` / `withoutFlagsCheck`); backed by new `IFilterableAppInfo.usesPlayAppSigning()` / `isOverlay()` methods on both `FilterableAppInfo` (uses `PackageUtils.usesPlayAppSigning` + `PackageInfoCompat2.getOverlayTarget`) and `ApplicationItem` (lazy `fetchPackageInfo()` paths). PWA and short-code deferred — TWA detection requires manifest service-tag sniffing and short-code is not a stable PackageManager signal; would ship a half-accurate heuristic. | Low | App Details |
| ~~`PermissionsOption.java`~~ ✅ 2026-05-17 | TODO (resolved) | Permission filter now exposes grant-state (`granted` / `denied`), custom-source (`custom`), fixed-state (`fixed`), and raw runtime-permission-flag (`with_flags` / `without_flags`) predicates via shared `FilterablePermissionInfo` state. | Low | T7 |
| `DataUsageOption.java` | TODO | Mobile/Wi-Fi split not shown | Low | T7 |
| `TrackersOption.java` | TODO | Regex + tracker-name search not implemented | Low | T7 |
| ~~`CodeEditorFragment.java`~~ ✅ 2026-05-02 | TODO (resolved) | Three pickers landed: **language** popup (java/json/kotlin/properties/sh/smali/xml from `assets/languages/` via `Languages.getLanguage()`), **tab-size** popup (2/4/8 with `mEditor.setTabWidth`), **go-to-line** dialog (`TextInputDialogBuilder` → `mEditor.setSelection(line - 1, 0)`). The previously hardcoded "tabs"/"spaces" suffix on the indent toolbar now reads from `R.plurals.editor_tab_size_option_{tabs,spaces}` so locales pluralise the unit. New `CodeEditorViewModel.setLanguage()` setter persists the choice across language switches. | Low | T14 |
| ~~BouncyCastle `1.83`~~ ✅ 2026-05-08 | Dependency (resolved) | Bumped 1.83 → 1.84 in [`versions.gradle:21`](versions.gradle#L21) closing CVE-2026-3505 / 5588 / 5598. Audit at [docs/audits/2026-05-08-bouncycastle-1-84-cve-bump.md](docs/audits/2026-05-08-bouncycastle-1-84-cve-bump.md). | — | Security |
| ~~libsu `6.0.0`~~ ✅ 2026-05-08 | API (resolved) | Audit clean: zero `Shell.sh(` / `Shell.su(` / `FLAG_REDIRECT_STDERR` matches across `app/`, `libcore/`, `libserver/`, `libopenpgp/`, `hiddenapi/`, `server/`. The single `Shell.cmd(` call site in [`RemoteShellImpl.java:25`](app/src/main/java/io/github/muntashirakon/AppManager/ipc/RemoteShellImpl.java#L25) implements the 6.0.0 idiom; all other privileged shell invocations route through NG's `Runner.runCommand` abstraction on top of it. Audit at [`docs/audits/2026-05-08-libsu-shell-cmd-migration.md`](docs/audits/2026-05-08-libsu-shell-cmd-migration.md). Reference: [S47]. | — | All root ops |
| ~~All layouts~~ ✅ 2026-05-01 | Compliance | Audit for `elegantTextHeight` attribute usage — ignored for targetSdk=36; text rendering affected for Arabic/Thai/Indic scripts ([S44]). **Audit clean — zero source matches.** See [docs/audits/2026-05-01-elegant-text-height.md](docs/audits/2026-05-01-elegant-text-height.md). | Low | T2 |
| WorkManager/JobScheduler | Compliance | Backup scheduler must be tested under Android 16 quota model; log stop reasons with `WorkInfo.getStopReason()` / `JobParameters.getStopReason()` and, on API 36+, collect `JobScheduler#getPendingJobReasonsHistory()` ([S45], [S125]). **⏸ Re-audited 2026-05-17: no WorkManager/JobScheduler implementation exists in source today — diagnostics are now an explicit acceptance criterion for T6 Scheduled Auto-Backup, not a standalone row.** | Low | T6 |
| Predictive Back / `onBackPressed` removal (API 36) | Compliance | For apps targeting API 36 (Android 16), `onBackPressed()` and `KEYCODE_BACK` are no longer dispatched; must migrate all back-press handling to `OnBackInvokedCallback` / `BackHandler` (Compose). Audit every `Activity.onBackPressed()` call and every `KeyEvent.KEYCODE_BACK` handler in NG before bumping `targetSdkVersion` to 36 ([S44]). v0.3.0 already ships edge-to-edge; predictive back gesture registration is the remaining compliance gap. | Medium | T2 |
| ~~Zip-slip protection in APK/backup extraction~~ ✅ 2026-05-08 | Security (resolved) | Audit clean: every disk-writing extraction path (`TarUtils.extract` ~L154, `AndroidBackupExtractor.extract` ~L72) carries both the pre-write `Paths.normalize` + `startsWith("../")` rejection and the post-create `realFilePath.startsWith(realDestPath)` containment check. Archive-to-archive converters (`SBConverter`, `OABConverter`, `TBConverter`) cache entries by extension/in-memory, never using the source entry name as a disk path. Audit at [`docs/audits/2026-05-08-zip-slip-protection.md`](docs/audits/2026-05-08-zip-slip-protection.md). Reference: [S01]. | — | T2, T6 |
| ADB mDNS backend change (platform-tools 37) | Compatibility | ADB platform-tools 37.0.0 deprecated the `openscreen` mDNS backend in favor of `libadbmdns` ([S74]). Verify that NG's wireless ADB pairing code path (T5) does not hard-depend on `openscreen` discovery signals; use `adb mdns check` to test backend availability on target device before connecting. | Low | T5 |
| JADX `1.4.7` | Dependency | Latest is `1.5.5` (7 releases behind [S60]); includes critical multi-thread UI fix (v1.5.5), `.apks` support, CJK font rendering, and an external plugin system (v1.5.4+). Upgrade before T12 work begins. | Medium | T12 |
| Apktool 2.x → 3.x migration | Future | Apktool 3.0.x ("Apktool Remastered" [S80], [S332]) is a complete rewrite with a new smali fork and rewritten ARSC/`BinaryDataInputStream` parsers; Apache-2.0 licensed. **2026-05-17 audit:** NG does not currently wrap Apktool 2.x or 3.x, so there is no dependency to migrate. If T12 adds an Apktool-backed APK decode/rebuild backend, target `org.apktool:apktool-lib:3.0.2+` and test duplicate-class behavior against the existing Google smali/baksmali dependency before enabling it in the Android app classpath ([S333]). | Low | T12 |
| ~~Gson `2.13.2`~~ ✅ 2026-05-08 | Dependency (resolved) | Bumped 2.13.2 → 2.14.0 in [`versions.gradle:26`](versions.gradle#L26). Audit at [docs/audits/2026-05-08-gson-2-14-0-bump.md](docs/audits/2026-05-08-gson-2-14-0-bump.md). | — | All |
| Material Components `1.13.x` | Ceiling | Material `1.14.0-rc01` raises `minSdkVersion` to 23 ([S57]); blocked until NG raises `minSdk` from 21. Action: document in `versions.gradle` comment; schedule minSdk raise to 23 before any Material 1.14.x adoption. | Medium | All |
| AGP `8.13.2` | Dependency | Latest stable is AGP `9.2`; requires Gradle 9.4.1 and SDK Build Tools 36 ([S59]). R8 tightens `-keepattributes *Annotation*` wildcard behavior in 9.x — auditing ProGuard rules required at upgrade. Not urgent but track for annual build-toolchain cycle. | Medium | Build |
| MagiskSU capability drop | Behavior | MagiskSU `≥v30.5` no longer drops Linux capabilities by default; explicit `--drop-cap` now required ([S52]). Audit all root shell invocations in NG that assumed automatic capability restriction before shipping T15 systemless ops. | Medium | T15 |
| ~~Android 17 targetSdk=37 compliance~~ ✅ audit batch closed 2026-05-17 | Compliance batch | All five sub-audits closed clean during iter-26 follow-through (see ROADMAP §"Iter-26 Closures"): (1) `ACCESS_LOCAL_NETWORK` — NG has zero LAN discovery surface ([docs/audits/2026-05-17-android17-access-local-network.md](docs/audits/2026-05-17-android17-access-local-network.md)); (2) static-final reflection ban — iter-20 audit landed with 1 fix + 1 deferred ([docs/audits/2026-05-08-android17-static-final-reflection.md](docs/audits/2026-05-08-android17-static-final-reflection.md)); (3) `MODE_BACKGROUND_ACTIVITY_START_ALLOWED` migration — zero matches in source ([docs/audits/2026-05-17-android17-bal-intentsender.md](docs/audits/2026-05-17-android17-bal-intentsender.md)); (4) `usesCleartextTraffic` enforcement — network-security-config rejects cleartext at base ([docs/audits/2026-05-17-android17-cleartext-traffic-enforcement.md](docs/audits/2026-05-17-android17-cleartext-traffic-enforcement.md)); (5) ECH default-on — all 3 endpoints handle cleanly ([docs/audits/2026-05-17-android17-ech-default-on.md](docs/audits/2026-05-17-android17-ech-default-on.md)). Bonus closure: ML-DSA OID display is forward-compatible and now has readable ML-DSA names ([docs/audits/2026-05-17-android17-ml-dsa-keystore-oid.md](docs/audits/2026-05-17-android17-ml-dsa-keystore-oid.md)). **Source-side compliance work for the targetSdk=37 bump is complete.** Remaining blockers: the 1 deferred finding from the iter-20 static-final-reflection audit + Android 17 device verification for Shizuku's fixed-version floor (warning mitigation already shipped). | (closed) | T5, T8, T9 |
| Android 17 memory limits | Compliance | All-apps change: Android 17 introduces RAM-based per-app `AnonSwap` memory limits; exit detected via `ApplicationExitInfo.REASON_OTHER` with `"MemoryLimiter:AnonSwap"` description ([S55]). JADX decompile and APK-signature parsing are the heaviest NG operations; profile under low-memory devices before T12 ships. | Medium | T12 |
| Android 17 `usesCleartextTraffic` deprecation | Compliance | Android 17 begins enforcement of `usesCleartextTraffic`; apps that omit the Network Security Config manifest attribute will trigger lint errors on targetSdk=37 ([S55]). Audit NG's `network_security_config.xml` and all `http://` URLs in manifests before bumping targetSdk. | Low | T9 |
| Android 17 ECH default-on | Compliance | Apps targeting API 37 get ECH (Encrypted Client Hello) applied to all TLS connections by default ([S54]). NG must add a `<domainEncryption>` element to `network_security_config.xml` if any network endpoint (e.g. wireless ADB LAN pair) must opt out of ECH to avoid negotiation failures on older firmware. | Low | T5, T9 |
| ~~Android 17 per-app Keystore key cap~~ ✅ 2026-05-02 | Compliance | NG creates ≤2 `AndroidKeyStore` aliases total (both static, both `containsAlias`-guarded in `CompatUtil`); backup-crypto routes through file-backed BKS (`am_keystore.bks`) which is outside the cap. Audit at [docs/audits/2026-05-02-android17-keystore-key-cap.md](docs/audits/2026-05-02-android17-keystore-key-cap.md). | Low | T2, T9 |
| ~~Android 18 implicit URI grant removal (planned)~~ ✅ 2026-05-02 | Compliance | All seven URI-bearing share-out paths now set explicit `ClipData` alongside `FLAG_GRANT_READ_URI_PERMISSION`; chooser targets receive read access without relying on the implicit auto-grant scheduled for removal in Android 18 ([S55]). Audit + remediation at [docs/audits/2026-05-02-android18-implicit-uri-grant.md](docs/audits/2026-05-02-android18-implicit-uri-grant.md). | Low | T3, T5 |
| GitHub Actions `release.yml` | Future | Android 17 introduces ML-DSA hybrid APK signing for post-quantum forward security ([S53]). Evaluate adding a PQC co-signer to the release pipeline when bumping targetSdk=37; fully backward-compatible with older Android versions and does not affect F-Droid or IzzyOnDroid builds. | Low | T1 |

---

## Upstream Sync Strategy

AppManagerNG is not a hard fork. The intent is to stay semantically close to upstream while diverging on UX.

**Pull policy:**
- Security fixes from upstream: pull immediately regardless of conflict cost.
- Bug fixes: cherry-pick within one upstream release cycle.
- Upstream "Upcoming Features" (Finder, APK editing, Routine Ops, Crash Monitor, Database Viewer, Terminal): pull when upstream ships rather than reimplement. NG reimplements only if upstream stalls for >12 months.

**Divergence tracking:**
- Weekly CI check comparing NG `main` against upstream `master`; generates cherry-pick candidate list.
- Maintained in `.github/upstream-sync.md` (to be created in v0.2.0).

**Current upstream "Upcoming Features" status:**

| Feature | Upstream Status | NG Action |
|---------|----------------|-----------|
| Finder | Partial code exists | Complete in NG (T7) |
| Basic APK editing | Committed, not shipped | Pull when ready (T12) |
| Routine operations | Spec published (Issue #61), not shipped | Implement in NG (T8) if stalled |
| Crash monitor | Mentioned only | Pull when ready |
| Database viewer/editor | Issue #14, not shipped | Pull when ready (T13) |
| Advanced terminal | Mentioned only | Pull when ready (T14) |
| Systemless ops | Issue #150, not shipped | Implement in NG (T15) |
| Import app list | Issue #122, not shipped | Implement in NG (T6) |
| Overlay management | Shipped v4.0.1 ([S39]) | Pull into T18 |
| app-manager:// deep link | Shipped v4.0.x ([S40]) | Pull into T18 |
| Operation history log | Shipped v4.0.0 ([S01]) | Implement in NG (T8) as medium-effort standalone feature — AM's implementation covers freeze, uninstall, backup; NG should match |
| Installer `package:` URI | Shipped v4.0.5 ([S01]) | Pull into T5 |
| Profile shortcut configured-state execution | Shipped v4.0.4 ([S01]) | Pull into T8 |
| v4.1.0 milestone (24 issues, 0 closed) | Due 2026-09-18; not yet started ([S40]) | Monitor; cherry-pick security/bug fixes when they land |
| v4.2.0 milestone (20+ issues confirmed open) | Issues #138, #150, #163, #221, #237, #256, #309, #321, #327, #333, #360, #385, #387, #402, #529, #568, #846, #1158, #1274, #1366, #1412, #1488 milestoned; none closed; upstream in-progress on #1017 | Implement #321 (Finder), #150 (Systemless), #568 (Backup extras), #163 (Crash monitor) in NG if upstream stalls past 12 months from milestone due date |

---

## Source Appendix

All external sources cited above.

| ID | URL | Used For |
|----|-----|----------|
| S01 | https://github.com/MuntashirAkon/AppManager | Upstream README, release notes v4.0.0–v4.0.5, upcoming features |
| S02 | https://github.com/MuntashirAkon/AppManager/issues/55 | Shizuku support (31 reactions) |
| S03 | https://github.com/MuntashirAkon/AppManager/issues/61 | Routine Operations full spec (21 reactions) |
| S04 | https://github.com/MuntashirAkon/AppManager/issues/321 | Finder cross-app search (9 reactions) |
| S05 | https://github.com/MuntashirAkon/AppManager/issues/122 | Export/Import App List (8 reactions) |
| S06 | https://github.com/MuntashirAkon/AppManager/issues/516 | DocumentsProvider (7 reactions) |
| S07 | https://github.com/MuntashirAkon/AppManager/issues/138 | APK Editor (7 reactions) |
| S08 | https://github.com/MuntashirAkon/AppManager/issues/464 | Aurora/F-Droid in-app unification (12 reactions) |
| S09 | https://github.com/MuntashirAkon/AppManager/issues/555 | Scheduled auto-backup (4 reactions) |
| S10 | https://github.com/MuntashirAkon/AppManager/issues/150 | Systemless features (4 reactions) |
| S11 | https://github.com/MuntashirAkon/AppManager/issues/14 | Database viewer (3 reactions) |
| S12 | https://github.com/MuntashirAkon/AppManager/issues/163 | Crash monitor (3 reactions) |
| S13 | https://github.com/MuntashirAkon/AppManager/issues/143 | Activity log (3 reactions) |
| S14 | https://github.com/MuntashirAkon/AppManager/issues/1029 | Clone applications (2 reactions) |
| S15 | https://github.com/MuntashirAkon/AppManager/issues/561 | Profile enhancements (2 reactions) |
| S16 | https://github.com/MuntashirAkon/AppManager/issues/1767 | Interceptor breaks OPEN_DOCUMENT (2 reactions) |
| S17 | https://github.com/MuntashirAkon/AppManager/issues/1671 | Install without staging APK (2 reactions) |
| S18 | https://github.com/Universal-Debloater-Alliance/universal-android-debloater-next-generation | UAD-NG: debloat list, multi-device model, dependency schema |
| S19 | https://github.com/samolego/Canta | Canta: rootless Shizuku debloater, UAD-NG list integration |
| S20 | https://github.com/NeoApplications/Neo-Backup | Neo Backup: backup UX reference, Weblate model, AES-256 encryption |
| S121 | https://github.com/RikkaApps/Shizuku/releases | Shizuku v13.6.0: trusted-WLAN auto-start without root on Android 13+, removed LEANBACK_LAUNCHER per Play policy |
| S122 | https://github.com/topjohnwu/Magisk/releases | Magisk v30.7: Android 16 QPR2 sepolicy, Android XR support, `--drop-cap` semantics inverted to opt-in |
| S123 | https://github.com/tiann/KernelSU/releases | KernelSU v3.2.x: adb root mode, sulog screen, per-app seccomp filter status |
| S124 | https://developer.android.com/about/versions/16/behavior-changes-16 | Android 16 platform changes: `SDK_INT_FULL`, `VERSION_CODES_FULL`, `Build.getMinorSdkVersion()`, JobScheduler quota tightening |
| S125 | https://developer.android.com/reference/android/app/job/JobScheduler#getPendingJobReasonsHistory(int) | JobScheduler `getPendingJobReasonsHistory()` API for surfacing scheduled-job stop reasons |
| S126 | https://github.com/termux/termux-app/releases | Termux v0.118.3: SE_PROCESS_CONTEXT/SE_FILE_CONTEXT/SE_INFO env surface, 16k page size detection |
| S127 | https://github.com/REAndroid/APKEditor/releases | APKEditor v1.4.7-8: smali comment level "basic", `-remove-annotation`, framework v36 support |
| S128 | https://github.com/iBotPeaches/Apktool/releases | Apktool v3.0.2 "Remastered": ResChunkPullParser refactor, headless build support, Guava 33.5 |
| S129 | https://github.com/skylot/jadx/releases | JADX v1.5.5: `.apks` ingestion, UI zoom factor, FlatLaf composite font for CJK, plugin exception reporting |
| S130 | https://github.com/aistra0528/Hail/releases | Hail v1.10.0: Auto-Freeze QS Tile, Digital Assistant launch, KernelSU App Profile, multi-tag, Xposed unfreeze hook |
| S131 | https://github.com/Hamza417/Inure/releases | Inure build106.5–107.0.1: AppOps IGNORE flag, MiUI ops mappings, native-lib sizes, batch installer, removed persistent search |
| S132 | https://github.com/zhanghai/MaterialFiles/releases | Material Files v1.7.2–v1.7.4: WebDAV with self-signed-cert trust, checksum properties tab, Save As, Android TV launcher icon |
| S133 | https://github.com/d4rken-org/sdmaid-se/releases | SD Maid SE v1.7.2-rc0: auto-fix battery optimization via root/ADB, warn-before-volume-scan, signing-cert fingerprints publish |
| S134 | https://github.com/Universal-Debloater-Alliance/universal-android-debloater-next-generation/releases | UAD-NG v1.2.0: cross-user package state verification, auto-fetch debloat definitions, selectable descriptions, copy-error button |
| S135 | https://github.com/NeoApplications/Neo-Backup/releases | Neo Backup 8.3.17: launcher shortcuts for schedules, backup sharing, KeepAndroidOpen banner, existing-tag suggestions |
| S136 | https://developer.android.com/jetpack/androidx/releases/glance | androidx.glance: Compose-style home-screen widgets, app widgets parity surface |
| S137 | https://github.com/MuntashirAkon/AppManager/issues/1940 | Android 16 QPR2 removed `clearApplicationUserData` hidden API breaking restore; upstream maintainer comment "80 hours per Android version migration" |
| S138 | https://github.com/MuntashirAkon/AppManager/issues/1958 | GCM cipher reuse on >2 GB OBB backup → IV reset mid-stream → tag verification fails on restore |
| S139 | https://github.com/samolego/Canta/issues/359 | Shizuku silently revokes permission grant when app data cleared; UI continues showing "authorized" until restart (security-relevant stale-trust window) |
| S140 | https://github.com/samolego/Canta/issues/350 | Cross-user / work-profile package reappearance; need single-action multi-user debloat |
| S141 | https://github.com/MuntashirAkon/AppManager/issues/1959 | Per-app rollback / "revert all changes" without backup restore |
| S142 | https://github.com/NeoApplications/Neo-Backup/issues/991 | Glob-based path exclusion patterns inside backup archives — `**/cache/**`, `**/.thumbnails/**`, custom |
| S143 | https://github.com/MuntashirAkon/AppManager/issues/1968 | Tasker / MacroDroid parameterized intents — `EXTRA_PACKAGE`, `EXTRA_DRY_RUN`, `EXTRA_PROFILE_OVERRIDES` |
| S144 | https://github.com/aistra0528/Hail/issues/388 | Freeze audit log request — chronological log of who-froze-what-when, no current feature |
| S145 | https://github.com/aistra0528/Hail/issues/392 | Accidental unfreeze of system component, no recovery path; motivates per-action revert UI |
| S146 | https://github.com/aistra0528/Hail/issues/390 | Settings import/export request for factory-reset / ROM-flash portability |
| S147 | https://github.com/MuntashirAkon/AppManager/issues/1835 | FireOS "SYSTEM USER" privilege escalation path (LM Toolbox model); ~1M Fire device users with no AM-class tool |
| S148 | https://github.com/MuntashirAkon/AppManager/issues/1948 | Android 17 / 16 KB page-size: NG main menu shows zero apps on Pixel 9a due to mis-aligned native libs |
| S149 | https://github.com/MuntashirAkon/AppManager/issues/1855 | App-list filter UX gap: install-date sort, date-range filter, "filter applied" indicator |
| S150 | https://github.com/MuntashirAkon/AppManager/issues/1944 | Home-screen widgets ignore Material You / Monet dynamic colors on Android 12+ |
| S151 | https://github.com/Universal-Debloater-Alliance/universal-android-debloater-next-generation/issues/1302 | Samsung timezone package → bootloop; not flagged dangerous in metadata |
| S152 | https://github.com/MuntashirAkon/AppManager/issues/948 | KernelSU / APatch / SukiSU root-mode detection compatibility (111 comments) |
| S153 | https://github.com/MuntashirAkon/AppManager/issues/1967 | KernelSU root-mode failures, fresh issue thread |
| S154 | https://github.com/MuntashirAkon/AppManager/issues/1850 | Launcher crash after removing packages tagged "safe to remove"; OEM enforcement layers (OplusForbidUninstallAppManager, MIUI `com.miui.core`) |
| S155 | https://github.com/lihenggui/blocker/releases | Blocker v2.0.6339 (Mar 2026): visual IFW rule editor, IFW + `pm disable` combined controller, community-curated debloat ruleset |
| S156 | https://github.com/AhmetCanArslan/ShizuWall/releases | ShizuWall v4.5: non-VPN per-app firewall via Shizuku, hybrid WiFi+mobile rules, screen-lock mode, focus tracker |
| S157 | https://github.com/dorumrr/de1984 | de1984: no-VPN per-app firewall + Shizuku package management |
| S158 | https://github.com/BinTianqi/OwnDroid/releases | OwnDroid v8.0 (Mar 2026): embedded Dhizuku DPM, `setPermissionGrantState()`, cross-profile intent filters |
| S159 | https://github.com/wxxsfxyzm/InstallerX-Revived/releases | InstallerX-Revived v2.3.2 (Feb 2026): biometric gate on install/uninstall, LSPosed install-progress hook |
| S160 | https://github.com/SanmerApps/PI | PI: package installer with requester/executor override (default-installer interception, backup-before-install, multi-user) |
| S161 | https://github.com/accrescent/accrescent/releases | Accrescent v0.27/0.28: signed protobuf store API, install-time feature modules, compatibility specifiers (min/max SDK, ABI, density), gentle updates |
| S162 | https://github.com/deltazefiro/Amarok-Hider | Amarok-Hider: per-app `pm hide` toggle (separate state from disable/freeze) |
| S163 | https://github.com/DUpdateSystem/UpgradeAll/releases | UpgradeAll v0.13-beta.4 (Apr 2026): pluggable getter-API for F-Droid/IzzyOnDroid/GitHub/APKPure/CoolApk/RSS update sources |
| S164 | https://github.com/VegaBobo/Language-Selector/releases | Language-Selector: per-app locale via `LocaleManagerService.setApplicationLocales()` Shizuku call (Android 13+) |
| S165 | https://github.com/timschneeb/awesome-shizuku | timschneeb/awesome-shizuku: meta-list including debuggable-app-data-backup (rootless `/data/data` backup for `android:debuggable="true"` apps) |
| S166 | https://github.com/tiann/KernelSU/releases | KernelSU v3.2.3 / v3.2.4 (Apr 2026): runtime "ADB Root" toggle, sulog audit screen, seccomp-status display |
| S167 | https://f-droid.org/en/2026/03/28/how-to-include-repos-in-rom.html | F-Droid 2.0 ROM pre-seeding: legacy XML format dropped; new `/system_ext/etc/fdroid/additional_repos.json` |
| S168 | https://f-droid.org/en/news/ | F-Droid 2.0 alpha8 (Apr 24, 2026): protobuf-based index v2 enters broad testing, incompatible with index v1 |
| S169 | https://developer.android.com/about/versions/17/behavior-changes-17 | Android 17 (API 37): `ACCESS_LOCAL_NETWORK` runtime permission, 50K Keystore key cap, static-final reflection ban, DCL extended to native `.so`, `IntentSender.sendIntent()` BAL hardening, ML-DSA Keystore algorithms |
| S170 | https://android-developers.googleblog.com/2026/04/the-fourth-beta-of-android-17.html | Android 17 Beta 4 (Apr 16, 2026): `MemoryLimiter:AnonSwap`/`SwapFull` exit reasons, `usesCleartextTraffic` deprecation announcement for Android 18, networking and security summary |
| S171 | https://support.google.com/googleplay/android-developer/answer/14905536 | Google Play Contacts/Location-button policy: enforcement begins ~2026-05-15; rationale dialog required before any UI surface that exposes contact info or precise location |
| S21 | https://github.com/SmartPack/PackageManager | SmartPack Package Manager: Shizuku feature comparison |
| S22 | https://github.com/RikkaApps/Shizuku/releases | Shizuku v13.6.0: Android 16 QPR1 support, auto-start on trusted WLAN |
| S23 | https://github.com/MuntashirAkon/android-debloat-list | android-debloat-list JSON schema (dependencies, required_by, removal safety) |
| S24 | https://github.com/d4rken-org/sdmaid-se | SD Maid SE: CorpseFinder, scheduler model, Shizuku integration |
| S25 | https://github.com/Aefyr/SAI | SAI: split APK installer (archived); installation method reference |
| S26 | https://github.com/ImranR98/Obtainium | Obtainium: multi-source update tracking, AppVerifier integration |
| S27 | https://github.com/topjohnwu/libsu/releases | libsu release history (6.0.0 is current; 5.x was prior) |
| S28 | https://developer.android.com/about/versions/16/summary | Android 16 behavior changes (edge-to-edge, predictive back, themed icons) |
| S29 | https://developer.android.com/about/versions/15/features | Android 15 features (16KB page size, PDF renderer, OpenJDK 17) |
| S30 | https://github.com/termux/termux-app | Termux: terminal emulator reference for advanced terminal feature |
| S31 | https://github.com/MuntashirAkon/AppManager/issues/1161 | Boot loop guard for critical system app removal |
| S32 | https://github.com/MuntashirAkon/AppManager/issues/660 | App shortcuts / Quick Settings tile |
| S33 | https://github.com/MuntashirAkon/AppManager/issues/387 | 1-click delete old backups |
| S34 | https://github.com/MuntashirAkon/AppManager/issues/568 | Backup extras (SMS/calls/Wi-Fi) |
| S35 | https://github.com/MuntashirAkon/AppManager/issues/447 | Greenify-like prescription rules |
| S36 | https://github.com/MuntashirAkon/AppManager/issues/75 | Intent intercept feature improvements |
| S37 | https://github.com/MuntashirAkon/AppManager/issues/1863 | AppOps precision control |
| S38 | https://github.com/MuntashirAkon/AppManager/issues/1718 | Custom filter presets / saved app lists |
| S39 | https://github.com/MuntashirAkon/AppManager/releases/tag/v4.0.1 | Upstream v4.0.1: overlay management, per-session installer options, market URL scheme |
| S40 | https://github.com/MuntashirAkon/AppManager/releases/tag/v4.0.5 | Upstream v4.0.5: reproducible builds, logcat omit-sensitive-info |
| S41 | https://github.com/NeoApplications/Neo-Backup/releases | Neo Backup 8.3.12–8.3.17: PGP encryption, backup sharing, launcher schedule shortcuts, tags filter |
| S42 | https://github.com/d4rken-org/sdmaid-se/releases | SD Maid SE 1.6.5–1.7.2: storage trend tracking, scheduler result notifications, deduplicator, scheduler battery optimization auto-fix |
| S43 | https://github.com/samolego/Canta/releases | Canta 3.0.0–3.2.2: Disabled filter, factory-reset before uninstall, debloat presets, Android 16 Shizuku fix |
| S44 | https://developer.android.com/about/versions/16/behavior-changes-16 | Android 16 targetSdk=36 behavior: adaptive layouts, `elegantTextHeight` deprecation, `scheduleAtFixedRate` change |
| S45 | https://developer.android.com/about/versions/16/behavior-changes-all | Android 16 all-apps: JobScheduler quota changes, abandoned job stop reason, broadcast priority scope |
| S46 | https://developer.android.com/about/versions/16/features | Android 16 new APIs: `SDK_INT_FULL`, `Notification.ProgressStyle`, two-release cadence for 2025 |
| S47 | https://github.com/topjohnwu/libsu/blob/master/CHANGELOG.md | libsu 6.0.0: `Shell.sh/su` removed, `Shell.cmd` API, `FLAG_REDIRECT_STDERR` deprecated |
| S48 | https://github.com/REAndroid/APKEditor/releases | APKEditor V1.4.8: Android 36 support, dex v042, `--remove-annotation`, smali performance |
| S49 | https://osv.dev/vulnerability/GHSA-8xfc-gm6g-vgpv | BouncyCastle CVE: affects <1.78; fixed in 1.78; NG uses 1.83 (clean) |
| S50 | https://www.bouncycastle.org/releasenotes.html | BouncyCastle 1.84 current; NG on 1.83 |
| S51 | https://github.com/ImranR98/Obtainium/releases | Obtainium v1.3.0–v1.4.3: certificate hash display; dynamic color fix API 34+; GitHub repo rename detection; Android TV remote focus |
| S52 | https://github.com/topjohnwu/Magisk/releases | Magisk v30.5–v30.7: MagiskSU no longer drops capabilities by default; Android 16 QPR2 Zygisk support; Rust migration |
| S53 | https://developer.android.com/about/versions/17/features | Android 17 features: ProfilingManager triggers, JobDebugInfo APIs, ECH, AAPM, PQC APK signing, contact picker |
| S54 | https://developer.android.com/about/versions/17/behavior-changes-17 | Android 17 targetSdk=37: static-final field restriction, ACCESS_LOCAL_NETWORK, BAL IntentSender, CT default, native DCL |
| S55 | https://developer.android.com/about/versions/17/behavior-changes-all | Android 17 all-apps: RAM-based memory limits (AnonSwap), Keystore per-app key cap, usesCleartextTraffic deprecation plan |
| S56 | https://github.com/tiann/KernelSU/releases | KernelSU v3.2.3: adb root feature; v3.2.4: seccomp status display, su log screen |
| S57 | https://github.com/material-components/material-components-android/releases | Material 1.14.0-rc01: minSdkVersion raised to 23 — blocks upgrade while NG remains on minSdk 21 |
| S58 | https://github.com/google/gson/releases | Gson 2.14.0: java.time adapters, stricter integer validation, Serializable removed from internal types |
| S59 | https://developer.android.com/build/releases/gradle-plugin | AGP 9.2 release notes: Gradle 9.4.1 required; R8 annotation keepattr wildcard tightened; unified test/coverage dashboards |
| S60 | https://github.com/skylot/jadx/releases | JADX 1.5.5: critical multi-thread UI fix; 1.5.4: plugin system (external ZIP artifacts), .apks support, CJK fonts, UI zoom |
| S61 | https://github.com/LSPosed/LSPatch/releases | LSPatch v0.6 — archived Dec 2023; no longer actively maintained; integration with NG deferred indefinitely |
| S62 | https://accrescent.app/features | Accrescent: key-pinning on first install, signed metadata, no TOFU, no accounts required, unattended updates, split APK support, min-version pinning |
| S63 | https://github.com/soupslurpr/AppVerifier/releases | AppVerifier v0.5.0–v0.8.2: APK file verification via VIEW/SEND intent, cert hash display, multi-signer support; v0.8.0 removed Material dep (-1 MB) |
| S64 | https://f-droid.org/docs/Inclusion_How-To/ | F-Droid inclusion requirements: FOSS-only deps, Fastlane metadata directory, CLI buildable, reproducible builds preferred |
| S65 | https://github.com/aistra0528/Hail/releases | Hail v1.9.0: Force Stop (Root + Shizuku), Reinstall uninstalled system apps via `pm install-existing` (Root + Shizuku), System App Disable mode; v1.10.0: multi-tag per app, URI schema for automation API, KernelSU App Profile support, Xposed auto-unfreeze hooks, Auto-Freeze QS tile |
| S66 | https://github.com/Hamza417/Inure/releases | Inure build106.3.3–build107.0.1: Levenshtein relevance search, native library sizes, MiUI AppOps support, permission policy flags display, batch APK installer, terminal security fix |
| S67 | https://github.com/Universal-Debloater-Alliance/universal-android-debloater-next-generation/releases/tag/v1.2.0 | UAD-NG v1.2.0: dynamically fetched package list (no app update needed for list changes), search in descriptions, package state + cross-user verification, selectable/copyable descriptions |
| S68 | https://github.com/timschneeb/awesome-shizuku | awesome-shizuku: curated list of 50+ apps using Shizuku for elevated operations — reference for rootless capability expansion, vendor-specific Shizuku tooling landscape |
| S69 | https://github.com/TrackerControl/tracker-control-android/releases | TrackerControl 2026040301: graduated blocking modes (minimal/standard/strict), DuckDuckGo Tracker Radar integration, multi-blocklist auto-updates, per-app tracker statistics, Show Unprotected Apps, Material 3 migration started |
| S70 | https://github.com/mirfatif/PermissionManagerX/releases | Permission Manager X v1.25–v1.31: Perm Watcher real-time permission change monitoring, 25+ language localizations, Android 16 target compliance |
| S71 | https://github.com/iamr0s/Dhizuku | Dhizuku: DeviceOwner permission sharing daemon (GPL-3.0); Android 8–16; activation via `adb shell dpm set-device-owner com.rosan.dhizuku/.server.Server`; Dhizuku-API library for third-party app integration; enables DPM-based freeze/hide/suspend without root |
| S72 | https://github.com/LibChecker/LibChecker/releases | LibChecker 2.5.2–2.5.3: APK signing scheme multi-flag display (V1/V2/V3/V4), 16 KB page-alignment badge per .so, app state snapshot + version-diff timeline, APK URL partial-download metadata preview, native library extraction to Downloads, permission provider attribution |
| S73 | https://github.com/vvb2060/KeyAttestation | KeyAttestation: hardware key attestation panel — KeyMint version, attestation level (software/TEE/StrongBox), certificate chain; Shizuku needed for device-unique-ID attestation |
| S74 | https://developer.android.com/tools/releases/platform-tools | ADB platform-tools 37.0.0: `openscreen` mDNS backend deprecated in favor of `libadbmdns`; affects wireless ADB pairing code paths |
| S75 | https://github.com/FreezeYou/FreezeYou/releases | FreezeYou v11.0–v11.1: self-diagnosis page (privilege health-check model); Profile Owner DPM path via `adb shell dpm set-profile-owner` (no factory reset needed, work-profile scope) |
| S76 | https://github.com/bmax121/APatch | APatch: kernel-based root via KernelPatch; ARM64 only, kernel 3.18–6.12; SuperKey (higher privilege than su); APM (Magisk-style) + KPM (kernel injection) modules; GPL-3.0; `/data/adb/ap/` detection marker |
| S77 | https://github.com/Dr-TSNG/ZygiskNext/releases | ZygiskNext v1.3.0–v1.3.4: anonymous memory module loading; multi-zygote support; KernelSU/APatch Zygisk compatibility layer; WebUI Zygisk module list; module error check mechanism (v1.3.4); Android 16 QPR2 + virtual-machine (Redroid) support |
| S78 | https://github.com/oasisfeng/island/releases | Island v6.3–v6.4: scoped settings (per-feature DPC capability toggle), expanded app details sheet (scoped storage access level, Android 11+ package visibility scope), app visibility filters (can-see / can-access-shared-storage toggles), Open DPC API for third-party DPC operations via Binder |
| S79 | https://github.com/XayahSuSuSu/Android-DataBackup | Android-DataBackup v2.x: Magisk/KernelSU/APatch root support, multi-user backup, cloud + SMB backup destinations, label-based app filtering, config export, GPL-3.0 |
| S80 | https://github.com/iBotPeaches/Apktool/releases | Apktool 3.0.x "Apktool Remastered": complete internal rewrite; new smali fork; rewritten ARSC + `BinaryDataInputStream` parsers; Apache-2.0; `main` branch is 3.x; `2.x` branch maintenance-only |
| S81 | https://github.com/Droid-ify/client/releases | Droid-ify v0.7.0–v0.7.1: SHA256 index-integrity verification (long-press refresh), custom actions, delete-APK-on-install option, fdroid.link URL parsing, incompatible-version disclaimer, signed releases |
| S82 | https://developer.android.com/reference/android/content/pm/ArchivedPackageInfo | Android 15 (API 35) `ArchivedPackageInfo`: archived-state class holding package name, signing info, and launcher activities; paired with `PackageInfo.isArchived` and `PackageInstaller.UnarchivalState` |
| S83 | https://developer.android.com/reference/android/content/pm/PackageManager#getInstallSourceInfo(java.lang.String) | `PackageManager.getInstallSourceInfo()` (API 30+): returns initiating package, originating package, and (API 34+) installer signing-cert digest — enables distinguishing who triggered vs. who declared the install source |
| S84 | https://github.com/iamr0s/Dhizuku/releases | Dhizuku v2.11.0–v2.11.2: minSdk bumped to 26 (Android 8.0), battery optimization bypass (`requestIgnoreBatteryOptimizations()`), whitelist mode, DhizukuService performance optimization (non-isolated binderWrapper), Android 16 support |
| S85 | https://github.com/topjohnwu/Magisk/releases | Magisk v30.5–v30.7: Rust codebase migration, Android 16 QPR2 support, Android XR support, `--drop-cap` explicit capability restriction for non-root UIDs, improved sepolicy handling, vendor_boot partition support |
| S86 | https://github.com/LawnchairLauncher/Lawnchair/releases | Lawnchair 15 Beta 2–Beta 3: Nova Launcher backup restoration (icon grid, widgets, folders), infinite scrolling, app-drawer folder reordering, launcher search backend redesign (IronFox + Kagi providers) |
| S87 | https://github.com/shadowsocks/shadowsocks-android/releases | shadowsocks-android v5.3.5: VPN flags opt-in for plugins, DNS resolution fixes (OnePlus 12 / Android 15), desugaring fixes, E2E test CI/CD workflow, eliminate gson dependency |
| S88 | https://github.com/M66B/FairEmail/releases | FairEmail v1.2315: active maintenance; email security improvements; Android 16 support; multi-release consistent updates
| S89 | https://github.com/REAndroid/APKEditor/releases | APKEditor v1.4.8: structured three-layer APK editing (manifest/resources/dex), framework v36 support, smali optimization; reference OSS implementation for full APK editing architecture
| S90 | https://github.com/soupslurpr/AppVerifier/releases | AppVerifier v0.8.2: APK verification UI with signature database, donation screen, Material 3 design, focus on package authenticity
| S91 | https://github.com/termux/termux-app/releases | Termux v0.118.3–v0.119.0-beta: Android 16 QPR1 HiddenAPI fix, MIME type recognition in ContentProvider, terminal pixel dimension reporting, AutoFill support; signal for file manager MIME detection
| S92 | https://github.com/LSPosed/LSPatch/releases | LSPatch v0.6 (archived Dec 2023): non-root Xposed framework via APK patching and modification, integrated mode, module selection, dynamic loader; reference for non-root code injection architectural patterns
| S93 | https://github.com/frida/frida/releases | Frida v17.9.4: dynamic instrumentation framework for real-time code patching on Android; root not strictly required (Frida Server model); runtime method hooking, memory inspection, SELinux bypass; advanced power-user tool
| S94 | https://github.com/skylot/jadx/releases | JADX v1.5.5: cross-platform GUI decompiler with split-view mode, CJK font support, .apks archive support, NPE fixes, generics preservation; reference implementation for APK analysis UI patterns
| S95 | https://github.com/mobile-shell/mosh/releases | Mosh v1.4.0: mobile shell with OSC 52 clipboard integration, prediction modes, true color support, syslog integration; terminal networking innovation for unreliable connections
| S96 | https://github.com/square/leakcanary/releases | LeakCanary v3.0-alpha series: memory leak detection library for Android apps; automated heap dump analysis and leak report; integrates into development/testing workflows
| S97 | https://github.com/google/perfetto/releases | Perfetto v54.0: comprehensive system profiling platform (tracing service, trace processor with SQL queries, UI); Android 16 support for heap profiling, jank metrics, CUJ analysis, deep-linking for public traces
| S98 | https://github.com/appium/appium/releases | Appium v3.3.1+: cross-platform test automation framework with plugin architecture; supports Android via UIAutomator and Espresso integrations; extensibility model for custom plugins
| S99 | https://github.com/google/play-services-plugins/releases | OSS Licenses Plugin v0.11.0: Gradle build-time dependency license scanning and in-app display; transparency pattern for third-party attribution
| S100 | https://github.com/google/bundletool/releases | Bundletool v1.18.3: APK/AAB generation, install simulation, device group targeting, sparse encoding (SDK 32+), .asar format support, uncompressed dex (Android Q+)
| S101 | https://github.com/JetBrains/compose-multiplatform/releases | Compose Multiplatform v1.11.0-beta03: cross-platform declarative UI framework; Material 3 Adaptive, drag-to-scroll, KSP v2 compatibility, navigation and lifecycle integrations
| S102 | https://github.com/raamcosta/compose-destinations/releases | Compose Destinations v2.2.0: Jetpack Compose navigation library with KSP v2 fixes, result delivery lifecycle control (resume vs. start), destination labels for logging/monitoring
| S103 | https://github.com/nextcloud/android/releases | Nextcloud Android v33.1.0: cloud storage sync client; auto-upload, unified search, smart folder refresh, chat/translation in Nextcloud Assistant; on-device sync model precedent
| S104 | https://github.com/Iamlooker/Droid-ify/releases | Droid-ify v0.7.1: F-Droid client with custom actions (shell scripts per app), SHA256 installer verification, delete-APK-on-install toggle, proxy icon/screenshots download, Material 3 design patterns
| S105 | https://github.com/oasisfeng/Island/releases | Island v6.4.2: device-admin app isolation and duplication framework; scoped settings (per-feature capability toggles), widget access filtering, custom image capture provider; architectural pattern for safe app containment
| S106 | https://github.com/Kotlin/kmp-production-sample/releases | Kotlin Multiplatform (KMM) Production Sample: Material Design 3 patterns across platform; Compose Multiplatform for cross-platform Material 3 UI consistency; reference implementation
| S107 | https://github.com/GoogleChrome/android-browser-helper/releases | android-browser-helper v2.7.0: Trusted Web Activity (TWA) framework; edge-to-edge splash screens, custom tabs, billing integration, web API bridges for native app features
| S108 | https://github.com/android/nowinandroid/releases | Now in Android v0.1.1: Material 3 reference app; search + recent searches, push-based notifications, unread badge pattern, undo bookmarks SnackBar, comprehensive Compose + Kotlin example
| S109 | https://github.com/OxygenCobalt/Auxio/releases | Auxio v4.0.10: Material Design refresh with Material You integration; fast scrolling, native tag parsing (Musikr), cover-art caching, adaptive/small-split-screen layouts, Android 15 support
| S110 | https://github.com/mullvad/mullvadvpn-app/releases | Mullvad VPN Android v2026.4: two-column landscape layout (list + detail), in-app language selector (Android 13+), split tunneling search, accessibility polish, reconnect action in notification
| S111 | https://github.com/ProtonMail/android-mail/releases | ProtonMail v7.9.5: premium email UX with improved PIN layout, push notification handling, encryption visibility toggle, discreet/generic icon mode for privacy, Material 3 patterns
| S112 | https://github.com/d4rken-org/sdmaid-se/releases | SD Maid SE v1.7.2-rc0: scheduler auto-fix battery optimization via root/ADB, publish APK signing cert fingerprints, Android TV/Google TV launcher support, deduplicator UX (similar audio/video files, accurate freeable space)
| S113 | https://github.com/Universal-Debloater-Alliance/universal-android-debloater-next-generation/releases | UAD-ng v1.2.0: package state verification, cross-user detection, searchable package descriptions, copy-error-message UI, auto-fetch package definitions on launch (decoupled from app version)
| S114 | https://github.com/NeoApplications/Neo-Backup/releases | Neo Backup v8.3.17: launcher shortcuts for schedules, KeepAndroidOpen banner, backup sharing button, existing tags suggestion, Compose-compiler-stable VM states migration, Nav3 bottom sheet navigation
| S115 | https://github.com/google/accompanist/releases | Accompanist v0.37.3: official Compose utilities (drawable painter, system UI controllers); navigation-material deprecation in favor of Compose Material3 navigation
| S116 | https://github.com/Aefyr/SAI/releases | SAI v4.5: split APK installer with Sui/Shizuku support, large APK install warning (>150MB), APKM unencrypted format, XAPK partial support, .apks parsing, managed backups (beta)
| S117 | https://github.com/gsantner/markor/releases | Markor v2.15.0: dyslexia-friendly font option, SHA-256 in file info dialog, per-folder sort order, ShareInto tracking parameter filtering (Amazon)
| S118 | https://github.com/SkyD666/PodAura/releases | PodAura v3.2: Material 3 Expressive design migration, persistent article filter state, long-press group reordering, font size tuning on reading page, fast forward/replay portrait player
| S119 | https://github.com/google/talkback/releases | TalkBack 6.1.1: Google's official Android screen reader and accessibility services; baseline for Compose Semantics audit and contentDescription validation
| S120 | https://github.com/zaneschepke/wgtunnel/releases | WG Tunnel v4.3.1: WireGuard Android client with endpoint latency filtering, traffic stats in notification, Doze mode handshake fix, SHA-256 4096-bit signing fingerprint |
| S172 | https://android-developers.googleblog.com/2026/03/android-developer-verification-rolling-out-to-all-developers.html | Android Developer Verification announcement: rollout to all developers; certified Android devices in BR/ID/SG/TH require apps to be from a verified developer to install/update; enforcement begins 2026-09-30 |
| S173 | https://developer.android.com/developer-verification | Android Developer Verification overview/timeline; certified devices in select regions must install apps registered by verified developers; advanced-flow path for power users |
| S174 | https://lobste.rs/s/xetwz2/google_wants_make_sideloading_android | Lobsters / HN macro-thread on Google's sideloading verification — dominant Android-management discussion thread through 2026 |
| S175 | https://source.android.com/docs/security/bulletin/2026/2026-05-01 | Android Security Bulletin May 2026: CVE-2026-0073 critical zero-click proximal RCE in `adbd` running as shell user; affects Android 14/15/16/16-qpr2; security patch level 2026-05-01 |
| S176 | https://github.com/bcgit/bc-java/blob/main/docs/releasenotes.html | BouncyCastle 1.84 (2026-04-14): fixes CVE-2026-3505 (PGP AEAD chunk-size DoS), CVE-2026-5588, CVE-2026-5598 (FrodoKEM non-constant-time compare) |
| S177 | https://github.com/google/gson/releases/tag/gson-parent-2.14.0 | Gson 2.14.0 (2026-04-23): built-in `java.time` adapters drop `--add-opens` requirement; duplicate-key strict mode (throws on duplicate JSON keys instead of silent overwrite) |
| S178 | https://github.com/XiaoTong6666/Sui | Sui: Magisk-module form of Shizuku (no separate app, zero-UI permission grants); Magisk-module marker `/data/adb/modules/sui/`; integrates with `libsu` |
| S179 | https://github.com/RikkaApps/Shizuku/issues/2048 | Shizuku #2048: 13.6.0 UserService silently killed via reflective `LoadedApk.makeApplication` NPE on Transsion (Infinix/Tecno/Itel) Android 15 ROMs (2026-05-01, 2 comments) |
| S180 | https://xdaforums.com/t/shizuku-not-working-correctly.4756808/ | XDA: Pixel 9 Pro stock Android 16 QPR1 — Shizuku 13.6.0 stops responding after a few minutes regardless of battery-restriction state; Mediatek users separately must downgrade to 13.5.4 |
| S181 | https://github.com/RikkaApps/Shizuku/issues/2052 | Shizuku #2052: KernelSU + Shizuku service started in Root mode crashes banking apps that hate root (Play Integrity strict) — leakage even when Shizuku permission never granted |
| S182 | https://github.com/MuntashirAkon/AppManager/issues/1956 | AM #1956 (2026-04-15): app stuck in "ignore battery optimization" state — write succeeds but Doze re-flags within 60s; OS-revert pattern |
| S183 | https://github.com/aistra0528/Hail/issues/387 | Hail #387 (2026-04-05, 2 comments): frozen-app launcher icon disappears, `/data/data/<pkg>` artifacts remain; unfreeze doesn't always re-show launcher icon |
| S184 | https://github.com/MuntashirAkon/AppManager/issues/1965 | AM #1965 (2026-05-02): Android 16 QPR2 (Poco F3 / Infinity-X 3.9 / Root) — `clearApplicationUserData` returns success but data partition isn't actually cleared |
| S185 | https://github.com/MuntashirAkon/AppManager/issues/1962 | AM #1962 (2026-04-30): root mode unusable on LineageOS 23.2 / Android 16 — root binder handshake never completes; SELinux denial in `system_server` |
| S186 | https://github.com/MuntashirAkon/AppManager/issues/1963 | AM #1963 (2026-04-30): Debloater shortcut crashes on Moto g22 (Unisoc T606 / Android 12) with `ActivityNotFoundException`; activity-alias on pre-A13 budget Unisoc devices not registered |
| S187 | https://github.com/MuntashirAkon/AppManager/issues/1966 | AM #1966 (2026-05-04): App Info popup density — SDK row at bottom forces scroll; trackers/SDK rows under-utilize horizontal space; signature row only shows hash with no Subject |
| S188 | https://github.com/Universal-Debloater-Alliance/universal-android-debloater-next-generation/issues/1394 | UAD-NG #1394 (2026-05-01): removing `com.samsung.android.smartsuggestions` crash-loops Settings → Mobile Networks on Galaxy A57 / One UI 8.5 |
| S189 | https://github.com/Universal-Debloater-Alliance/universal-android-debloater-next-generation/issues/1390 | UAD-NG #1390 (2026-04-26): restore path for `com.android.dialer` doesn't re-bind dialer Role — leaves user without a phone app |
| S190 | https://github.com/NeoApplications/Neo-Backup/issues/1034 | Neo-Backup #1034 (2026-04-23): scheduled "user apps" run skips packages that have *any* backup, even stale ones — should gate on age-of-newest-backup |
| S191 | https://github.com/NeoApplications/Neo-Backup/issues/1029 | Neo-Backup #1029 (2026-03-18): streaming write to SMB/CIFS providers corrupts archives with `\0` byte sequences — `WriteableByteChannel.write` short-write race on SAF SMB providers |
| S192 | https://github.com/NeoApplications/Neo-Backup/issues/1032 | Neo-Backup #1032 (2026-04-06): can't blocklist a package whose only on-disk trace is its backup folder; blocklist editor must enumerate `<backup-root>/*` |
| S193 | https://github.com/NeoApplications/Neo-Backup/issues/1033 | Neo-Backup #1033 (2026-04-12): saved Wi-Fi networks + passwords aren't preserved across factory resets / ROM flashes — system-data backup category gap |
| S194 | https://github.com/MuntashirAkon/AppManager/issues/1957 | AM #1957 (2026-04-21): AM-generated squashfs backup triggers Dolphin warning on open — magic-byte / header off vs `mksquashfs 4.6` reference |
| S195 | https://github.com/MuntashirAkon/AppManager/issues/1964 | AM #1964 (2026-05-01): built-in File Manager has no in-folder search/filter — large `/data/data/<pkg>/` trees unnavigable |
| S196 | https://github.com/yume-chan/VolumeManager | yume-chan/VolumeManager: per-app volume via Shizuku-driven AppOps `OP_AUDIO_VOLUME` write; reference impl for AppOps per-UID precision-control (closes upstream AM #1863) |
| S197 | https://github.com/wxxsfxyzm/InstallerX-Revived/releases/tag/26.05.d57ad8e | InstallerX-Revived 26.05 preview (2026-05-07): Miuix + M3 Expressive overhaul; install-flow privilege-elevation cascade Shizuku → root → Dhizuku → MIUI optimization-disable nudge |
| S198 | https://github.com/GrapheneOS/AppStore/releases/tag/36 | GrapheneOS App Store Release 36: A16 background-install confirmation crash fix; ignore PendingActions from untrusted MainActivity callers |
| S199 | https://github.com/pass-with-high-score/universal-installer | universal-installer: VirusTotal scanning + split-APK + silent-install via Shizuku; per-split cert-mismatch table dialog before commit (vs silent rejection) |
| S200 | https://github.com/ImranR98/Obtainium/issues/2911 | Obtainium #2911 (2026-05-06): cancelling in-progress predictive-back gesture on Android 16 / One UI 8 leaves embedded WebView unresponsive — broad regression |
| S201 | https://github.com/material-components/material-components-android/releases/tag/1.14.0-rc01 | Material Components 1.14.0-rc01 (2026-04-29): `FocusRingDrawable` (auto-construct from child shape, hide on no-window-focus); `SplitButton` RTL fix; minSdk 23 |
| S202 | https://developer.android.com/jetpack/androidx/releases/compose-material3 | Compose Material 3 1.5.0-alpha19 (2026-05-06): ToggleButtons / FAB / menu APIs promoted to stable in M3 Expressive |
| S203 | https://developer.android.com/build/releases/agp-9-0-0-release-notes | AGP 9.0 (Jan 2026), 9.1.1 (Apr 2026), 9.2.0 (Apr 2026): `targetSdk 37` support, R8 default-package class repackaging for DEX, AGP 10 cliff mid-2026 (deprecated DSL removal) |
| S204 | https://source.android.com/docs/compatibility/cts | AOSP source publishing on Q2 + Q4 cadence only (trunk-stable model live in 2026); consumers should target `android-latest-release` branch |
| S205 | https://security.googleblog.com/2026/03/post-quantum-cryptography-in-android.html | Google Security Blog (2026-03-26): Android 17 Keystore exposes ML-DSA-65 / ML-DSA-87 `KeyPairGenerator` algorithms via `KeyProperties.KEY_ALGORITHM_ML_DSA_*`; Play App Signing generates ML-DSA hybrid keys |
| S206 | https://developer.android.com/about/versions/17/behavior-changes-17 | Android 17 (API 37) behavior changes — apps targeting SDK 37: `System.load()` requires read-only native libs; static-final reflection ban (`Field.set` throws even with `setAccessible(true)`); BAL hardening on IntentSender; `MODE_BACKGROUND_ACTIVITY_START_ALLOWED` deprecated → `_ALLOW_IF_VISIBLE` |
| S207 | https://developer.android.com/about/versions/17/behavior-changes-all | Android 17 all-apps behavior changes (re-confirmed): static-final reflection ban; 50K Keystore key cap per-UID; implicit URI grants from `Send/SendMultiple/ImageCapture` removed in Android 18 |
| S208 | https://github.com/hddq/restoid/releases/tag/v0.5.0 | hddq/restoid v0.5.0 (2026-05-07): root + restic Android backup; chunked AEAD; dedup; snapshot metadata; leapfrog answer to GCM-cipher-reuse on >2 GB OBB and CIFS null-byte streaming |
| S209 | https://github.com/RikkaApps/Shizuku/issues/2044 | Shizuku #2044 (2026-05-01): persistent ADB TCP/IP port (5555) start path requested — Shizuku setup against existing `adb tcpip 5555` session as third path next to QR-pair / USB-ADB |
| S210 | https://github.com/kerneldroid/Shizuku-modern | kerneldroid/Shizuku-modern (early 2026): Compose Material 3 Expressive Shizuku fork targeting SDK 37; tracks Android 17 regressions; very early — watch-only |
| S211 | https://github.com/BugeStudioTeam/Buge-App-Manager | BugeStudioTeam/Buge-App-Manager (early 2026): direct name-collision competitor for AM/NG; very early — watch-only for differentiation |
| S212 | https://github.com/Universal-Debloater-Alliance/universal-android-debloater-next-generation | UAD-NG `resources/assets/uad_lists.json` (~1.54 MB, 5,357 entries, GPL-3.0): bidirectional `dependencies`/`neededBy` graph and 4-tier `removal` taxonomy that our upstream MuntashirAkon dataset does not populate densely |
| S213 | https://github.com/Universal-Debloater-Alliance/universal-android-preinstalled-lists | UAD-NG sister repo (GPL-3.0, last push 2025-01-06): per-OEM ground-truth corpus of "package X is known preinstalled on OEM Y, model Z, region R"; powers Finder OEM filter chips and App Info Provenance row |
| S214 | https://codeberg.org/Katastima/apkscanner-data | IzzyOnDroid `apkscanner-data` (GPL-2.0, migrated from GitLab early 2026): SDK / NLP / anti-feature signature definitions powering F-Droid IzzyOnDroid antifeature flags; complementary to εxodus tracker DB with ~30–40% additional library coverage |
| S215 | https://github.com/Achno/debloat-samsung-ADB-shizuku | Achno Samsung-specific debloat list with CSC/region annotations in prose (last push 2025-04-14): one-time validation cross-check against `oem.json` Samsung block |
| S216 | https://developer.android.com/design-for-safety/privacy-sandbox/sdk-runtime | Android Privacy Sandbox SDK Runtime (Android 14+, API 34+): `SdkSandboxManager.getSandboxedSdks()` enumerates advertising/analytics SDKs currently loaded into the sandbox process — runtime-truth column to complement Exodus-style static SDK detection |
| S217 | https://developer.android.com/reference/android/content/pm/verify/domain/DomainVerificationManager | `DomainVerificationManager.getDomainVerificationUserState()` (API 31+, expanded behaviour in 34): per-domain verification state for `<intent-filter android:autoVerify="true">` claims; cross-referencing all installed apps surfaces deep-link hijack conflicts |
| S218 | https://developer.android.com/reference/android/content/pm/PackageInstaller#requestArchive(java.lang.String,android.content.IntentSender) | `PackageInstaller.requestArchive()` / `requestUnarchive()` (Android 15+, API 35+): zero-privilege APK removal preserving user data — third state alongside Active / Frozen for the freeze infrastructure |
| S219 | https://source.android.com/docs/security/test/memory-safety/arm-mte | Android Memory Tagging Extension (MTE) reference (Android 14+ on Pixel 8+): per-app `android:allowNativeHeapPointerTagging` manifest property + runtime MTE state determines hardened heap protection; surface as App Info security chip alongside the iter-18 signing-cert SHA-256 chip |
| S220 | https://developer.android.com/health-and-fitness/guides/health-connect | Health Connect (Android 14 mainline / API 34): `HealthConnectClient.getGrantedPermissions()` enumerates per-app fitness/sleep/clinical-data permissions; Health Connect data is invisible to standard permission/AppOps audits |
| S221 | https://developer.android.com/training/sign-in/credential-manager | Android Credential Manager (Android 14+): apps register passkey / password / federated identity providers via `Settings.ACTION_CREDENTIAL_PROVIDER`; system Settings shows providers but not which credentials are stored or by whom |
| S222 | https://source.android.com/docs/security/features/restricted-settings | Android 13+ Restricted Settings: blocks sideloaded apps from being granted Accessibility / NotificationListener / DevicePolicy without a manual long-press unlock in Settings; recurring support pain for power-user sideloaded apps |
| S223 | https://github.com/MMRLApp/MMRL | MMRL (Magisk Module Repo Loader) + LSPosed module manager (`/data/adb/modules/` and `/data/adb/lspd/` — read `module.prop` metadata): module enumeration / toggle / install surface NG could absorb behind the Privilege Health-Check screen |
| S224 | https://github.com/M66B/NetGuard | NetGuard (M66B) + TrackerControl (OxfordHCC/tracker-control-android, GPL-3.0): local VpnService + DNS proxy + per-app rules architecture; effort 5/5 capability that we are deferring until T9 AppOps-based tracker blocking has shipped and we have user feedback on whether AppOp-layer enforcement is sufficient |
| S225 | https://developer.android.com/reference/android/os/IBinder.DeathRecipient | Android `IBinder.DeathRecipient` callback — fired when the process hosting the binder dies (Shizuku/Sui shell, libsu shell), enabling journal-and-replay recovery for interrupted batched ops |
| S226 | https://shizuku.rikka.app/guide/ | Shizuku setup + diagnostics pattern — manager exposes ADB/root mode, server PID, API version, Sui detection; pattern for an active-probe "Doctor" diagnostics screen that goes beyond the display-only T5 Privilege Health-Check |
| S227 | https://kernelsu.org/guide/what-is-kernelsu.html | KernelSU + boot-time kernel-compat probe; GKI vs non-GKI branch detection; SELinux + hook mode surfacing in About page — pattern for the OS-level capability probes inside Doctor |
| S228 | https://github.com/topjohnwu/Magisk | Magisk Manager boot-install log + module-compat probe at install — pattern for AppManagerNG's LocalServer Bootstrap Smoke Test on the success path |
| S229 | https://github.com/NeoApplications/Neo-Backup | Neo Backup support-info composer dumps device + ROM + perms + Shizuku/root state into shareable text — template for AppManagerNG's Support Info Bundle Composer (zero network, user-initiated) |
| S230 | https://netguard.me/ | NetGuard (M66B) "send logs to dev" intent + traffic log export — second template for the user-initiated support-bundle pattern |
| S231 | https://github.com/Universal-Debloater-Alliance/universal-android-debloater-next-generation | UAD-NG Selection-CSV export — pattern for AppManagerNG's Privileged Op Audit Log (SQLite append-only, viewer + JSON export, retention slider) |
| S232 | https://github.com/RikkaApps/Shizuku/blob/master/manager/src/main/java/moe/shizuku/manager/authorization | Shizuku per-app permission grant timestamps — minimal audit-log precedent for managers in this space; AM-NG's audit log is a strict superset |
| S233 | https://f-droid.org/docs/Mirrors/ | F-Droid mirror list and signed-metadata pattern — multi-mirror priority + auto-failover blueprint for AppManagerNG's debloat-defs auto-update path |
| S234 | https://github.com/SysAdminDoc/android-debloat-list | SysAdminDoc fork of MuntashirAkon/android-debloat-list (created 2026-05-08) — primary mirror for AppManagerNG's debloat-defs auto-update path; secondary mirror Codeberg pending |
| S235 | https://docs.ipfs.tech/concepts/ipfs-gateway/ | IPFS public-gateway list (cf-ipfs.com / dweb.link / w3s.link / ipfs.io) — fallback content-addressed mirror for debloat-defs in extreme network-partition scenarios |
| S236 | https://developer.android.com/reference/android/app/job/JobInfo.Builder#setRequiresBatteryNotLow(boolean) | `JobInfo.Builder.setRequiresBatteryNotLow()` + `setRequiresDeviceIdle()` — battery-aware deferral primitives for non-urgent batch freeze/disable ops |
| S237 | https://syncthing.net/ | Syncthing P2P file sync — AppManagerNG profile dir uses atomic-write + mtime semantics so Syncthing handles peer sync without app-side code; conflict files surface in NG's conflict-picker UI |
| S238 | https://github.com/ankidroid/Anki-Android | AnkiDroid `.colpkg` snapshot bundle precedent — schema-versioned ZIP with all user state for one-button export/import; template for AppManagerNG's pre-rename snapshot bundle |
| S239 | https://github.com/samolego/Canta/blob/main/data/uad_lists.json | Canta JSON preset format `{packages: [...]}` — importable into AppManagerNG with ~30 LoC parser, lets users migrating from Canta arrive with their curation intact |
| S240 | https://github.com/Universal-Debloater-Alliance/universal-android-debloater-next-generation/blob/main/src/core/save.rs | UAD-NG `uad_settings.json` per-device-serial selection state schema — well-documented format suitable for AppManagerNG import |
| S241 | https://github.com/aistra0528/Hail/blob/master/README_EN.md | Hail freeze tag-list export format (newline-separated package lists per tag) — trivial AppManagerNG importer |
| S242 | https://github.com/bitfireAT/dav4jvm | DAV4JVM — Apache-2.0 WebDAV client library used by DAVx⁵, suitable for AppManagerNG's WebDAV snapshot-bundle target |
| S243 | https://developer.android.com/reference/android/content/Intent#ACTION_SEND | Android `ACTION_SEND` + `application/vnd.android.package-archive` — APK share-target receiver pattern for AppManagerNG's installer with pre-scan |
| S244 | https://f-droid.org/docs/Anti-Features/ | F-Droid Anti-Features rules (`Tracking` / `NonFreeNet` / `NonFreeAdd` / `NonFreeAssets`) — the bright-line constraint that rules out Firebase/Crashlytics/cloud-Sentry and shapes AppManagerNG's `floss` vs `full` build flavor split |
| S245 | https://github.com/LibChecker/LibChecker | LibChecker's split build flavor (Play flavor with Firebase/Crashlytics/Analytics + AppCenter; F-Droid flavor strips them, gets `Tracking` antifeature flag) — inverse-pattern for AppManagerNG (floss-default, full-opt-in) |
| S246 | https://github.com/aistra0528/Hail/blob/master/README_EN.md#actions | Hail v1.10.0 broadcast intent API (10 actions) + `hail://` URI scheme — direct template for AppManagerNG's Broadcast Intent API + `am://` URI scheme |
| S247 | https://github.com/MuntashirAkon/AppManager/issues/1219 | AM upstream issue #1219 — clear-cache via Tasker (closed as duplicate, unresolved) — pain-point validation for the Tasker plugin row |
| S248 | https://tasker.joaoapps.com/plugins-intro.html | Tasker Plugin Introduction — Locale's twofortyfouram intent spec (proprietary spec but free, free SDK, GPL-3.0-compatible) |
| S249 | https://tasker.joaoapps.com/pluginslibrary.html | Tasker Plugin Library (Maven Central artifact `com.joaomgcd:taskerpluginlibrary`) — the SDK that lets AppManagerNG's Tasker plugin live inside the host APK |
| S250 | https://github.com/d4rken-org/sdmaid-se/issues/1439 | SD Maid SE #1439 (2024) "Tasker support" — 7+ years of "Tasker support" deferred across SD Maid + SD Maid SE; pain-point validation that the AM space has been waiting for this |
| S251 | https://developer.android.com/reference/android/service/quicksettings/TileService#requestAddTileService(android.content.ComponentName,java.lang.CharSequence,android.graphics.drawable.Icon,java.util.concurrent.Executor,java.util.function.Consumer%3Cjava.lang.Integer%3E) | `TileService.requestAddTileService()` (Android 13+) — one-tap install for Quick Settings tiles from AppManagerNG's onboarding flow |
| S252 | https://developer.android.com/develop/ui/views/launch/shortcuts/creating-shortcuts | Android `ShortcutManager` dynamic + pinned shortcuts — per-app Freeze / Force-Stop / Clear-Cache shortcut surface for the AM icon long-press menu |
| S253 | https://developer.android.com/training/sharing/direct-share-targets | Android Sharing Shortcuts API — replaces deprecated `ChooserTargetService` (gone SDK 31); publishes top-N apps as direct-share targets for sending backups / profiles |
| S254 | https://developer.android.com/guide/topics/providers/create-document-provider | `DocumentsProvider` API — exposes `am://backups` and `am://profiles` to any SAF picker (Files by Google, Material Files, Solid Explorer) without `MANAGE_DOCUMENTS` |
| S255 | https://developer.android.com/jetpack/androidx/releases/window | Jetpack WindowManager 1.4 (stable March 2025) — `WindowSizeClass` 5-breakpoint API, `WindowInfoTracker.windowLayoutInfo`, Activity Embedding |
| S256 | https://developer.android.com/develop/ui/compose/layouts/adaptive/list-detail | Material 3 Adaptive `ListDetailPaneScaffold` — canonical master/detail layout driven off `currentWindowAdaptiveInfo().windowSizeClass`; Compose-native, View equivalent is `SlidingPaneLayout` + `WindowMetricsCalculator` |
| S257 | https://developer.android.com/develop/ui/views/layout/sliding-pane-layout | `SlidingPaneLayout` — the View-based equivalent of `ListDetailPaneScaffold` for AppManagerNG's mixed Java/Kotlin/Views codebase |
| S258 | https://developer.android.com/develop/ui/views/layout/foldables/use-window-manager-with-foldables | `FoldingFeature` (state FLAT / HALF_OPENED, orientation, isSeparating) — Z Fold 7 / Pixel Fold 2 posture awareness |
| S259 | https://developer.samsung.com/sdp/blog/en/2017/05/11/samsung-dex-multi-instance-application-on-samsung-dex | Samsung DeX multi-instance docs — `Intent.FLAG_ACTIVITY_LAUNCH_ADJACENT \| NEW_TASK \| MULTIPLE_TASK` + `android:resizeableActivity="true"` for two NG instances side-by-side |
| S260 | https://chromereadiness.googleblog.com/ | ChromeOS ARCVM detection via `hasSystemFeature("org.chromium.arc.device_management")` — AppManagerNG can hide phone-only ops and surface ChromeOS-broker constraints |
| S261 | https://developer.android.com/develop/xr/package-and-distribute | Android XR developer docs — package & distribute with `<uses-feature android:name="android.software.xr.api.spatial" android:required="false"/>` |
| S262 | https://www.uploadvr.com/samsung-galaxy-xr-has-easy-sideloading-and-open-bootloader/ | Galaxy XR (Q4 2026) ships the most open sideload story of any XR platform — direct APK install from browser, no PC, no developer mode; AppManagerNG can land as plausibly the first FOSS app manager on Android XR |
| S263 | https://developer.android.com/training/wearables/data/data-layer | Wear OS DataLayer + `WearableListenerService` + `MessageClient` + `NodeClient.connectedNodes` — cross-device live state for the phone-side Wear OS package-manager companion |
| S264 | https://technastic.com/adb-commands-wear-os-watch/ | ADB-over-WiFi to Wear OS (Settings → Developer → Wireless Debugging → Pair) — phone-side ADB client is the bridge primitive |
| S265 | https://play.google.com/store/apps/details?id=com.geminiman.wearosmanager | GeminiMan WearOS Manager (closed-source, ad-supported, Play-only) — sole existing player in the phone-side Wear OS package manager space; AppManagerNG would be the first FOSS competitor |
| S266 | https://hosted.weblate.org/projects/app-manager/ | Upstream MuntashirAkon/AppManager hosted Weblate — bridge target for AppManagerNG localization (separate `app-manager-ng` component sharing upstream's glossary + TM) |
| S267 | https://github.com/TeamNewPipe/NewPipe/blob/dev/CONTRIBUTING_TRANSLATION.md | NewPipe + Tachiyomi pattern for hosted Weblate + locked `strings.xml` (Weblate-only writes) — closest analogue to AppManagerNG's positioning |
| S268 | https://developer.android.com/guide/topics/resources/pseudolocales | Android pseudolocales `en_XA` / `en_XB` — accented/bracketed (truncation) and RTL-mirror (RTL layout) — standard i18n CI hygiene |
| S269 | https://developer.android.com/guide/topics/resources/app-languages | Android per-app locale picker via `AppCompatDelegate.setApplicationLocales()` (Android 13+) + `AppLocalesMetadataHolderService` backport (API 26+) |
| S270 | https://inure.app/ | Inure built-in Logs screen + bug-report exporter (writes to `/Documents/Inure`) — pattern for AppManagerNG's structured log viewer with severity filter + redaction |
| S271 | https://github.com/ACRA/acra | ACRA — Apache-2.0 crash reporter, file-sink backend (no third-party SDK, no network), opt-in default-off — F-Droid-friendly path that avoids `Tracking` antifeature |
| S272 | https://developer.android.com/topic/performance/benchmarking/macrobenchmark-overview | Android Macrobenchmark + Baseline Profiles — pure local, no privacy cost; AM cold-start is dominated by `PackageManager` enumeration, exactly the workload Baseline Profiles optimize |
| S273 | https://github.com/d4rken-org/sdmaid-se/tree/master/.github/workflows | SD Maid SE workflows (`code-checks`, `gradle-wrapper-validation`, `test-reports`, `release-prepare`, `release-tag`) — template for AppManagerNG's CI smoke pack on emulator matrix |
| S274 | https://github.com/jeremylong/DependencyCheck | OWASP `dependency-check-gradle` — CVE scan for transitive deps in CI; pairs with GitHub `dependency-review-action` for PR-level vulnerability gating |
| S275 | https://issuetracker.google.com/issues?q=oem+behavior&type=issue | Per-OEM behavior quirks (Samsung Knox container freeze refusal, MIUI auto-restart of disabled apps, EMUI accessibility-permission revocation, HyperOS aggressive App Standby, ColorOS background-kill) — input set for the OEM Quirk Panel |
| S276 | https://github.com/MMRLApp/MMRL | MMRL (Magisk Module Repo Loader) — distribution channel for AppManagerNG as a `signature \| privileged` system-app via Magisk / KernelSU / APatch module; bypasses manual `/system` install |
| S277 | https://github.com/LSPosed/LSPosed/wiki/Develop-Xposed-Modules-Using-Modern-Xposed-API | LSPosed Modern Xposed API — separate `am-xposed-helpers` companion module for auto-unfreeze on launch (Hail did this), bypass component-blocking on whitelisted apps, hidden API call logging |
| S278 | https://github.com/KDE/kdeconnect-android | KDE Connect Android — custom `packetType: kdeconnect.appmanager.action` for desktop-triggered freeze/uninstall/backup-now; cross-device power-user surface no AM-class app has |
| S279 | https://github.com/termux/termux-api | Termux:API — Unix-socket dispatcher pattern from `TermuxApiReceiver`; template for AppManagerNG's optional `am-shell` Termux companion APK |
| S280 | https://source.android.com/docs/automotive/users_accounts/disable_packages | AAOS `disable_packages` doc — for OEMs building the OS, not third-party tools; rationale for rejecting Android Automotive OS package management |
| S281 | https://github.com/SysAdminDoc/AppManagerNG/blob/main/README.md | AppManagerNG README install/contributing claims; used to reconcile missing `CONTRIBUTING.md` and release-artifact promises |
| S282 | https://github.com/SysAdminDoc/AppManagerNG/blob/main/.github/workflows/tests.yml | AppManagerNG tests workflow branch filters; local copy omits `main` |
| S283 | https://github.com/SysAdminDoc/AppManagerNG/blob/main/.github/workflows/lint.yml | AppManagerNG lint workflow branch filters; local copy omits `main` |
| S284 | https://docs.github.com/en/actions/reference/workflows-and-actions/workflow-syntax | GitHub Actions workflow syntax for explicit branch filters on `push` / `pull_request` |
| S285 | https://github.com/SysAdminDoc/AppManagerNG/blob/main/.github/workflows/release.yml | AppManagerNG release workflow; local copy signs release builds but publishes only the universal APK artifact |
| S286 | https://github.com/SysAdminDoc/AppManagerNG/blob/main/versions.gradle | AppManagerNG dependency-version ledger; minSdk-related AndroidX pins and dependency-check `10.0.3` claim source |
| S287 | https://github.com/SysAdminDoc/AppManagerNG/blob/main/CHANGELOG.md | AppManagerNG changelog; source for dependency-check version drift and shipped-doc claims |
| S288 | https://plugins.gradle.org/plugin/org.owasp.dependencycheck | Gradle Plugin Portal entry for `org.owasp.dependencycheck`; authoritative current plugin version source before changing `versions.gradle` |
| S289 | https://github.com/SysAdminDoc/AppManagerNG/tree/main/.github/ISSUE_TEMPLATE | AppManagerNG issue templates; local copy still carries upstream App Manager routing/wording |
| S290 | https://github.com/SysAdminDoc/AppManagerNG/blob/main/.github/PULL_REQUEST_TEMPLATE/pull_request.md | AppManagerNG PR template; local copy points contributors toward upstream conventions instead of NG governance |
| S291 | https://github.com/MuntashirAkon/AppManager | Upstream App Manager GitHub repo; 2026-05-16 API snapshot: 8,025 stars, 213 open issues, pushed 2026-04-16 |
| S292 | https://github.com/samolego/Canta | Canta GitHub repo; 2026-05-16 API snapshot: 4,780 stars, 26 open issues, pushed 2026-05-12 |
| S293 | https://github.com/Universal-Debloater-Alliance/universal-android-debloater-next-generation | UAD-NG GitHub repo; 2026-05-16 API snapshot: 6,606 stars, 241 open issues, pushed 2026-05-11 |
| S294 | https://github.com/aistra0528/Hail | Hail GitHub repo; 2026-05-16 API snapshot: 5,821 stars, 144 open issues, pushed 2026-04-26 |
| S295 | https://github.com/Hamza417/Inure | Inure GitHub repo; 2026-05-16 API snapshot: 1,757 stars, pushed 2026-05-16; feature reference for logs, analytics, Shizuku/root app management |
| S296 | https://github.com/d4rken-org/sdmaid-se | SD Maid SE GitHub repo; 2026-05-16 API snapshot: 6,740 stars, pushed 2026-05-16 |
| S297 | https://github.com/NeoApplications/Neo-Backup | Neo Backup GitHub repo; 2026-05-16 API snapshot: 3,614 stars, 228 open issues, pushed 2026-05-03 |
| S298 | https://github.com/XayahSuSuSu/Android-DataBackup | Android-DataBackup GitHub repo; 2026-05-16 API snapshot: 6,690 stars, 180 open issues, pushed 2026-05-14 |
| S299 | https://github.com/RikkaApps/Shizuku | Shizuku GitHub repo; 2026-05-16 API snapshot: 25,130 stars; core rootless privilege provider signal |
| S300 | https://github.com/LibChecker/LibChecker | LibChecker GitHub repo; 2026-05-16 API snapshot: 6,866 stars, pushed 2026-05-16 |
| S301 | https://github.com/soupslurpr/AppVerifier | AppVerifier GitHub repo; 2026-05-16 API snapshot: 1,046 stars; APK verification UX analogue |
| S302 | https://github.com/ImranR98/Obtainium | Obtainium GitHub repo; 2026-05-16 API snapshot: 17,115 stars, 383 open issues, pushed 2026-04-16; reason to reject built-in update tracking |
| S303 | https://adbappcontrol.com/en/ | ADB AppControl commercial desktop Android app manager/debloater; signal for low-friction desktop flows but not NG's on-device architecture |
| S304 | https://appdash.app/ | AppDash commercial Android app manager; signal for app history, tags/notes, install analytics, and polished install surfaces |
| S305 | https://swiftapps.org/ | Swift Backup commercial Android backup tool; signal for scheduled backups and destination/provider UX users pay for |
| S306 | https://www.titaniumtrack.com/titanium-backup.html | Titanium Backup commercial/root backup tool; long-running reference for batch backup/restore scheduling and cloud-style destination demand |
| S307 | https://developer.android.com/jetpack/androidx/releases/activity | AndroidX Activity release notes; API floor and Activity Embedding / predictive-back upgrade pressure source for minSdk ledger |
| S308 | https://support.google.com/googleplay/android-developer/answer/10158779 | Google Play `QUERY_ALL_PACKAGES` policy; app-manager package-visibility justification source |
| S309 | https://developer.android.com/reference/android/content/pm/PackageInstaller | Android `PackageInstaller` API reference; installer session/dependency-resolution capability source plus Android 36.1 developer-verification failure-reason extras returned through install result intents |
| S310 | https://developer.android.com/developer-verification | Android Developer Verification docs; current policy/timeline and advanced-flow source |
| S311 | https://f-droid.org/docs/All_our_APIs/ | F-Droid API and index-v2 docs; read-only package provenance metadata source without duplicating an app-store client |
| S312 | https://github.com/SysAdminDoc/AppManagerNG/blob/main/docs/security-advisories/2026-05-08-cve-2026-0073-adb-mode.md | AppManagerNG local CVE-2026-0073 advisory; explicitly defers runtime warning banner to T5 onboarding |
| S313 | https://github.com/SysAdminDoc/AppManagerNG/blob/main/app/src/main/java/io/github/muntashirakon/AppManager/misc/DeviceInfo2.java | AppManagerNG device-info source exposing `Build.VERSION.SECURITY_PATCH` for local patch-level checks |
| S314 | https://github.com/SysAdminDoc/AppManagerNG/blob/main/app/src/main/java/io/github/muntashirakon/AppManager/apk/installer/PackageInstallerActivity.java | AppManagerNG installer source TODO for dependency resolution before install/commit |
| S315 | https://github.com/SysAdminDoc/AppManagerNG/blob/main/app/build.gradle | AppManagerNG Android app Gradle config; debug `pseudoLocalesEnabled true`, minSdk/targetSdk, ABI splits, applicationId |
| S316 | https://f-droid.org/en/news/ | F-Droid 2.0-alpha9 release (2026-05-08); successor to [S168] alpha8 (2026-04-24); no breaking change since alpha8 — protobuf index v2 work continues. Independently verified via WebSearch during 2026-05-17 research-run consolidation. |
| S317 | https://f-droid.org/2026/01/24/fdroid-basic-2.0-alpha.html | F-Droid Basic 2.0 alpha announcement (2026-01-24); context for the 2.0-alphaN cadence consumed by T11 row "F-Droid 2.0 Index v2 Protobuf". |
| S318 | https://www.apkmirror.com/apk/xingchen-rikka/shizuku-manager/shizuku-13-6-0-r1086-2650830c-release/ | Shizuku Manager 13.6.0 APK metadata (target SDK 36 / Android 16); independent confirmation of iter-18 [S121] / [S22] Android-16 QPR1 support claim during 2026-05-17 research-run consolidation. |
| S319 | https://github.com/SysAdminDoc/AppManagerNG/blob/main/.ai/research/2026-05-17/ | 2026-05-17 autonomous research-run audit trail (`PROJECT_CONTEXT.md` + 11 artifacts under `.ai/research/2026-05-17/`); the consolidation pass that produced this register-extension and the iter-24 backlog. |
| S320 | https://source.android.com/docs/whatsnew/android-16-release | AOSP Android 16 / Android 16 QPR1 / Android 16 QPR2 release-notes hub; primary reference cross-validating roadmap S124 / S44 / S45 platform-doc citations. |
| S321 | https://github.com/RikkaApps/Shizuku/issues/1965 | Shizuku #1965 (2026-03-27): Android 17 Beta 3 on Pixel 9 — Application Management page blank while the reporter says authorized apps still appear to function. Mixed-symptom compatibility risk for NG's Shizuku onboarding health signal. |
| S322 | https://github.com/RikkaApps/Shizuku/issues/1967 | Shizuku #1967 (2026-03-27): Android 17 Beta 3 — "The app won't get the shizuku and nothing in the app list." Companion broader-failure report to S321; justifies warning + Wireless ADB fallback until upstream fixed-version verification. |
| S323 | https://github.com/NeoApplications/Neo-Backup/releases | Neo-Backup 8.3.18 (2026-05-04): supersedes the 8.3.17 cited at iter-18 [S135] / iter-19 [S114]. No NG-action delta from 8.3.17 → 8.3.18 — the iter-18 / iter-19 rows derived from 8.3.17 remain accurate. |
| S324 | https://developer.android.com/about/versions/17/release-notes | Android 17 stable release notes scaffold: Beta 4 (2026-04-16); **Platform stability reached at Beta 3 (2026-03-26)** — API surface locked; stable rollout target **June 2026** to Pixel devices first. Anchors the targetSdk=37 audit batch sequencing. |
| S325 | https://github.com/material-components/material-components-android/releases | Material Components 1.14.0-alpha06/07/08 status as of 2026-05-17: **1.14.0 stable has not shipped**. Iter-18 [S57] / [S201] flagged 1.14.0-rc01's minSdk-23 raise as the dep ceiling; rc01 has not progressed to stable. minSdk-21 ceiling decision can stay deferred. |
| S326 | https://github.com/RikkaApps/Shizuku/issues/1988 | Shizuku #1988 (2026-04-05): Android 17 / Pixel 9 Pro XL repeats the empty managed-apps list symptom on Shizuku 13.6.0.r1086. Reinforces the Android 17 watch without proving all UserService operations fail. |
| S327 | https://github.com/thedjchi/Shizuku/issues/172 | thedjchi/Shizuku #172: fork-maintained Android 17 support tracker, assigned to v14.0.0 with `Status: Testing`; notes preliminary `ACCESS_LOCAL_NETWORK` pressure for pairing. Signal for NG's Hidden-Shizuku fork-detection row. |
| S328 | https://repo.maven.apache.org/maven2/io/github/iamr0s/Dhizuku-API/ | Maven Central index for `io.github.iamr0s:Dhizuku-API`; latest listed artifact is `2.5.4` (2025-10-11), so Dhizuku integration should target that API unless a newer primary artifact appears. |
| S329 | https://raw.githubusercontent.com/iamr0s/Dhizuku-API/main/build.gradle | Dhizuku-API upstream build config declares `MIN_SDK = 26`, `TARGET_SDK = 36`, and `API_VERSION = '2.5.4'`; this is the concrete minSdk conflict with AppManagerNG's API-21 floor. |
| S330 | https://raw.githubusercontent.com/RikkaApps/Shizuku-API/master/api/src/main/java/rikka/shizuku/Shizuku.java | Shizuku API source for `addBinderDeadListener()` / `removeBinderDeadListener()` and `OnBinderDeadListener`; primary source for NG's batch-journal Shizuku/Sui binder-death watch. |
| S331 | https://raw.githubusercontent.com/RikkaApps/Shizuku/v13.6.0/manager/src/main/AndroidManifest.xml | Shizuku Manager v13.6.0 manifest source checked during pass 25; confirms the release exposes launcher/settings/starter/permission activities but not a universally exported `AUTO_START` component, so NG's trusted-WLAN auto-start button needs a launcher/app-info fallback. |
| S332 | https://apktool.org/blog/apktool-3.0.2/ | Apktool 3.0.2 release blog (2026-04-19): CVE-2026-39973 path-name hardening, base/config APK split-library decode support, `ApkInfo` return after decode, and ARSC/disassembly performance improvements. |
| S333 | https://central.sonatype.com/artifact/org.apktool/apktool-lib | Maven Central metadata for `org.apktool:apktool-lib:3.0.2`: Apache-2.0 library artifact and transitive runtime deps including the iBotPeaches smali fork, Guava 33.5.0-jre, commons-io 2.21.0, and commons-text 1.15.0. |
| S334 | https://developer.android.com/develop/ui/views/quicksettings-tiles | Android custom Quick Settings tile guide: declares `TileService` manifest shape with label/icon, exported service, `android.permission.BIND_QUICK_SETTINGS_TILE`, and `android.service.quicksettings.action.QS_TILE`. |
| S335 | https://developer.android.com/reference/android/service/quicksettings/TileService | Android `TileService` reference: `unlockAndRun(Runnable)` for lock-screen taps and API 34 `startActivityAndCollapse(PendingIntent)` guidance. |
| S336 | https://developer.android.com/guide/practices/page-sizes | Android 16 KB page-size compatibility guide, rechecked 2026-05-17: verify ELF `PT_LOAD` alignment, use `-Wl,-z,max-page-size=16384` / `-Wl,-z,common-page-size=16384` when NDK r28+ defaults are not guaranteed, and keep testing on 16 KB devices/images. |
| S337 | https://developer.android.com/tools/zipalign | Android `zipalign` reference: `-P 16` aligns uncompressed `.so` files to a 16 KiB page boundary suitable for `mmap(2)` on both 16 KiB and 4 KiB devices. |
