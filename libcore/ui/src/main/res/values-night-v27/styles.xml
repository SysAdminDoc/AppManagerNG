<!-- SPDX-License-Identifier: GPL-3.0-or-later -->
<resources>
    <!-- The night qualifier outranks the platform-version qualifier, so without this
         file dark mode on API 27+ silently loses the cutout flag from values-v27. -->
    <style name="AppThemeOverlay" parent="Theme.Material3.Dark.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>
