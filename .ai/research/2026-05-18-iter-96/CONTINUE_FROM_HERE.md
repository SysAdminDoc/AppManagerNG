# Continue From Here

Iter-96 parked the active/paused schedule-list row.

Recommended next roadmap candidates:

1. T6 `WorkManager / JobDebugInfo Schedule Diagnostics`
   - Next actionable scheduled-backup tail.
   - Add WorkManager state/stop/defer diagnostics with API 36/37 guards.
2. T6 `Export/Import App List`
   - Medium-sized feature outside the scheduler tail.
3. T6 `SMB / WebDAV Network Backup Destination`
   - High-effort storage-provider work; defer until smaller scheduler tail is drained.

